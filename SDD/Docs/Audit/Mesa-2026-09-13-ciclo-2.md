# Registro de mesa de evaluación — Lab-Geometria, 2026-09-13, ciclo 2

**Producto:** Fábrica de Geometría
**Documento:** Mesa-2026-09-13-ciclo-2.md
**Versión:** 1.1
**Fecha:** 2026-09-13
**Instrumento:** `Mesa-Rules.md` **1.4**, convocada desde `Master-Prompt-Reanudacion.md` **1.14**, fase **R1.5** (octava reanudación)
**Base de la corrida:** `4fc2763bc8997259422adb3fb0714a38664c21eb` (merge del PR #207)
**Prefijo de familia de este ciclo:** `RN8-` (`grep -rc "RN8-" SDD/Docs/Audit/` → 0 antes de este registro; no reúsa `MN-`, `RS-`, `RE-`, `RG-` ni ninguna otra familia presente en `Audit/`)
**Sufijo de ciclo:** `-ciclo-2`, porque `Mesa-2026-09-13.md` ya existe (mesa de la fase M1 de la migración 13.7 → 13.16, ciclo 1 del día)
**Expediente:** ninguno abierto. `SDD/Expedientes/0001-Migracion-Normativa-A-13.16/` es forma histórica (framework 13.18) y no se toca
**Informe de estado que la convocó y la consume:** [`Estado-Del-Destino-2026-09-13.md`](Estado-Del-Destino-2026-09-13.md)

---

## 1. Cabecera y contrato de entrada (`Mesa-Rules.md` §4)

Convocada por la condición de §0.0: hay corpus previo (554 documentos vivos en `SDD/Docs/`), el estado está leído (R0 de la octava reanudación) y hay una decisión de alcance por tomar (R2: reparar, migrar, seguir, continuar). El contrato lo aportó el orquestador de reanudación; la mesa no relevó el estado por su cuenta.

| Campo | Contenido |
|---|---|
| `objeto` | `SDD/Docs/` (554 documentos fuera de `_legacy/`), `SDD/Intake/` (intake 5.0, manifiesto 7.1), `changelog.md`. Superficies del estado: `00-Contexto/Roadmap-Producto.md` 1.14, `SDD/Docs/README.md` 2.8, `Audit/Medicion-PT-05.md` 1.2, `Audit/Estado-Del-Destino-2026-09-12.md` §10, `Audit/Mesa-2026-09-13.md` §8, `Informe-Migracion-13.7-a-13.16.md` §9, las 118 filas de ítems diferidos de ocho documentos de 05/06/09 y los bloques de `CU-08001..08008` |
| `estado` | Las seis dimensiones del informe de estado §3, con las divergencias DIV-01 a DIV-05 de su §5 |
| `diff_normativo` | 13.16 → 13.18: informe de estado §6. Cero major; 13.17 sin impacto; 13.18 con impacto no retroactivo sobre este destino (EXP-0001 forma histórica; el próximo expediente numera 0002) |
| `restricciones_duras` | `RN-*` del intake 5.0 (en especial `RN-B1`); intake §10 y §15 (etapas en serie, sin OK no se avanza); `Master-Prompt.md` §13 (el intake no se escribe sin su autor); `Migracion-Rules.md` §4.1; `Root-Rules.md` §10 R1 (un dato derivado no se escribe en prosa); los ADR de apartamiento vigentes |
| `decisiones_cerradas` | OK del Product Owner a la fase `k` (2026-09-13); `ADR-00009`, `ADR-00010`, `ADR-00011`, MinVer (`D-03`); migración 13.7 → 13.16 completa y procedencia 13.16; retiro de `evidencia/` de la raíz (PR #207, hoy: **no se restaura**); decisiones del 2026-09-12 sobre `E-02`/`E-04`/`E-05`; topología real (contenedores en i7infra, túnel, dominio propio; FTP como alternativa retirada); `DD-1` a `DD-9` como deuda ya declarada |
| `fuera_de_alcance` | Código, scripts, despliegue e infraestructura (producción de sólo lectura); `SDD/Expedientes/0001-…`; `_legacy/`; el intake sin el Product Owner; migrar a 13.18 (decisión de R2); medir `PT-05` |
| `pendientes_declarados` | 23 filas abiertas según R0 (cifra que esta mesa corrige: ver C-11); 8 bloques `CU` abiertos; `DD-1..DD-9`; 4 ítems de `Mesa-2026-09-12.md` §8; `D-01` y el respaldo del backend; lote F y G de EXP-0001 act. 013 sin respuesta (defaults rigen); `M6-31`. Todo heredado entró como nivel `C` hasta que un especialista lo abrió |
| Tope | 6 hallazgos por especialista; 8 + 2 para el refutador; hasta tres «lo que revisé y está bien» |

---

## 2. Registro de convocatoria (`Mesa-Rules.md` §5, §6.2)

**Compuerta mecánica previa (§6.2 punto 1):** `scripts/verify-solution-tree.sh` → «El árbol de solución está completo», código 0. Enlaces markdown relativos en `SDD/` y raíz fuera de `_legacy/`: 6456 revisados, 0 rotos (17 no resueltos, los 17 marcadores genéricos dentro de backticks —`[texto](destino)`, `(ruta)`, fragmentos de regex en `DoR-Tramo-k-2026-09-12.md`—). **No mirado:** el contenido de los 554 documentos como conjunto (es lo que hace el panel), el código, el despliegue.

**Barrido de señales (§6.2 punto 2), con ubicación:**

| Rol | Señal | Ubicación | Decisión |
|---|---|---|---|
| Requisitos | núcleo | — | **Convocado** |
| Verificación | núcleo | — | **Convocado** |
| Lector sin contexto | núcleo | — | **Convocado** |
| Refutador | núcleo, entra último | — | **Convocado** |
| Trazabilidad documental (AG-00110) | Referencias en prosa a `evidencia/2026-09-1x-…/` sin destino desde el PR #207 | `ADR-10008` :38; `Plan-Migracion-13.7-a-13.16.md` :102; `changelog.md` :1113, :1147, :1590 | **Convocado** |
| Operación y entrega (AG-00090) | Fase `i` (despliegue real, `PT-05` `SIN MEDIR`) y fase `k` cerrada encima; `M6-31` | `Roadmap-Producto.md` §2.1 filas `i`/`k`, §4, §5.2; `Medicion-PT-05.md` :6; `README.md` §7.2 | **Convocado** |
| Formal | Cuantificadores: «estrictamente secuenciales», «el último que queda» (×8), recuentos 118/554/«ocho de diez», «se documenta sea cual sea» | `Roadmap-Producto.md` :118; ocho filas de Api/05, 06, 09; `Medicion-PT-05.md` §1 | **Convocado** (automático) |
| Arquitectura | Sin señal: el salto 13.16 → 13.18 no toca arquitectura y las superficies del estado son de planificación y registro | — | **Descartado** |
| Datos y dominio | Sin señal en las superficies del estado | — | Descartado |
| Seguridad | Señal sólo hipotética (ver solicitud de OPE en §4, refutada en §5) | — | Descartado |
| Concurrencia, Rendimiento, Interfaz externa, Cumplimiento, Uso y accesibilidad | Sin señal | — | Descartados |
| Ad hoc | ninguno | — | — |
| Postergados por cupo | ninguno (tres variables de cinco posibles) | — | — |

**Ronda de convocatoria en caliente:** los especialistas emitieron siete solicitudes (§4). Ninguna produjo convocatoria nueva: la de Seguridad cayó con la observación E1 del refutador (producción corre `v1.1.2`); las demás son preguntas al framework o a titulares de categoría y van a §8 o §9.

---

## 3. Informes del panel (P1, a ciegas y en paralelo)

Cada especialista entregó su informe con hallazgos anclados, solicitudes de convocatoria y «lo que revisé y está bien». Se transcriben los hallazgos consolidados en §5; acá el resumen por rol y lo que cada uno declaró bien.

| Rol | Hallazgos | Lo que revisó y está bien |
|---|---|---|
| Requisitos (`RN8-REQ-01..06`) | 6: secuencialidad §4 vs `k` cerrada (P1); fila `k` :66 delega criterios a un Mini-Plan que no los tiene, bitácora §9 «ninguna etapa abierta» (P1); README §7.2 y tres estados de `i` (P1); `verify-stage-i.sh` mide FTP (P2); aposiciones «el último que queda» ×8 + «no ocurrió» ×3 (P3); referencias a `evidencia/` y changelog sin #207 (P2) | `Medicion-PT-05.md` :20-27 acoplado a la puerta; disciplina de `Roadmap` 1.12→1.14 sobre el OK; `Mini-Plan.md` :863-864 con evidencia reproducible de `BT-00031/32/33` |
| Verificación (`RN8-VER-01..06`) | 6: puerta de `i` mide FTP y afirma «siete criterios» (P1); changelog sin #207 y 7 referencias, no 5 (P1); recuento de abiertos no reproducible 19/20 vs 21 vs 23 (P2); fila `k` :66 inverificable (P2); DIV-01/02/04 ancladas (P2); citas con versión por tercera vez (P3) | 554 docs; 118 filas por documento (1+12+8+1+31+16+16+33); 116 de `DD-8` reproduce con el comando de E-011; aritmética «ocho de diez» |
| Lector sin contexto (`RN8-LEC-01..06`) | 6: `README.md` raíz y `SDD/README.md` dicen «nada empezó» (P1); README §7.2 (P1); nadie explica por qué `k` cerró antes que `i` (P1); `evidencia/` sin destino ni asiento (P1); punto de continuación anterior cita versiones superadas y no conoce `k` (P2); «vigente» sin fecha en el manifiesto (P3) | `Roadmap` §7 permite reconstruir `i` y `k`; `Handoff-Checkout.md` se declara superado con tabla de destino; `Medicion-PT-05.md` no inventa datos |
| Trazabilidad documental (`RN8-TRZ-01..06`) | 6: 7 referencias huérfanas, 6 vivas, Plan :102 resuelve (P1); changelog sin #207 (P1); `DD-8` subcontada por la regex en negrita (P2); `Medicion-PT-05.md` :10 tercera deriva (P3); `11-Documentacion` con tres estados (P3); DIV-02 anclada (P2) | Rama `archivo/reanudacion-6-2026-09-12` resuelve; 6449 enlaces, 17 falsos positivos; citas por línea del Plan §6 resuelven |
| Operación y entrega (`RN8-OPE-01..06`) | 6: `I-1` mide FTP retirado (P1); `I-2` exige producción == `main`, falso por diseño (P1); el corpus no dice qué corre en producción (P1); §4 roto en la letra sin asiento (P1); superficies de `i` viejas (P2); forma del punto de control de `k` (P2) | Etiquetas `v0.1.0…v1.1.2` con huecos declarados; sellado de imágenes en los dos `Dockerfile`; separación `deploy/compose.yaml` vs composición del PO coherente, con `DD-4` llevando la deuda |
| Formal (`RN8-FOR-01..06`) | 6: «`PT-05` es lo único que `i` hace» vs 6 vs 7 criterios (P1); cita 1.10 por tercera vez (P2); `DD-8` 116 es cota inferior (P2); 21/20/23 (P2); fila `k` :66 y Mini-Plan :453 (P2); cuatro reglas categóricas con contraejemplo, «el último» ordinal envejecido (P1) | 554 y 118 reproducen; aritmética de la fila `k` (10 = 8 + 1 + 1; 9 = 8 + 1); «ocho escenarios» vs «diez casos» no se contradicen |

**Contradicciones entre especialistas elevadas como ítem (§6.3):** una — el complemento de `DD-8`: TRZ contó **6** cabeceras en texto plano, FOR contó **11**. Entró al jurado como C-10 y el refutador la resolvió por E1 (13, la unión).

---

## 4. Refutador (`RN8-REF`)

Entró último, con los seis informes y la consolidación de 14 ítems. Abrió cada ancla, reprodujo C-10 y C-11 y observó producción de sólo lectura: `GET https://api-geometria.aplicada.stream/salud` → `{"ready":true,"version":"1.1.2+b9675d87…"}` (2026-09-13 23:45 UTC; el juez de Evidencia lo reprodujo a las 23:55 UTC).

| Ítem | Veredicto del refutador |
|---|---|
| C-01 | Sostenido. Ajustes: «la justificación era falsa el día que se escribió» es excesivo (el despliegue existía, no estaba verificado); el intake §15 no enumera `k`, la dependencia vive sólo en el Roadmap; capa de origen: la apertura de `k` (2026-09-12) que no detuvo por §4. El orquestador no repara: lleva el texto a la salida A y el PO confirma |
| C-02 | Sostenido |
| C-03 | Nivel P2 y **origen mal calculado**: la base `4fc2763` **es** el merge de #207, las líneas huérfanas están en la base → **ajeno a la corrida** (`Master-Prompt.md` §8.1). Las tres del changelog no se editan (:1660, :1705); se resuelven con la entrada nueva de C-04 |
| C-04 | Sostenido en el hecho; la obligación no nace de `changelog.md` :8 (que gobierna la construcción; #188 tampoco tiene entrada) sino de las tres citas huérfanas. **Fusionar con C-03** |
| C-05 | Sostenido en la puerta; en el corpus P2; `changelog.md` :786 es entrada histórica correcta en su fecha → ancla mal elegida. El hueco nació en la salida A del 2026-09-12 que reparó el roadmap sin declarar la puerta como deuda |
| C-06 | **Refutado en la premisa** («sólo el PO sabe qué corre»): producción corre `v1.1.2`; la defensa por cuenta de `ADR-00011` está desplegada y la mitigación por `.env` sobra (`ADR-00011` :50). Cae la convocatoria a Seguridad. Queda el hueco de registro |
| C-07 | Sostenido en la atribución (:66, :453); bitácora §9 es fechada, no falsa → P3; forma del punto de control: decisión cerrada, no se reabre |
| C-08 | P2: §2.2 es tabla de puertas y «lo único» se lee como «única puerta»; `Medicion-PT-05.md` §1 :17 sí lo afirma como absoluto |
| C-09 | Sostenido, P3 |
| C-10 | **Refutados los dos**: ni 6 ni 11 → **13** (00-Contexto ×4 :9; 01-Necesidades ×2; 10-Examples ×7 `ejemplo-*.md` :12) |
| C-11 | Sostenido: 20 «Vigente» (19 con evento `i` + `PD-05`). Y tres de las 19 tienen evento que ya ocurrió (REF-02) → 16 útiles para el barrido |
| C-12 | Sostenido, P3 |
| C-13 | P2: no hay regla del framework sobre `README.md` raíz ni `SDD/README.md` (`Master-Prompt.md` §11 gobierna `SDD/Docs/README.md`); el agravante es extra-normativo (repo público de consulta) |
| C-14 | Capa equivocada en la raíz (es C-02), pero `Vision-Producto.md` §9.2 :217 «cada fase es una etapa del intake» es falso desde Roadmap 1.11: `k` no tiene etapa en el intake |

**Hallazgos propios:** `RN8-REF-01` (P3) — `changelog.md` :1691 dice «`main` = `v1.1.1`» sobre `b9675d8`, que es `v1.1.2`. `RN8-REF-02` (P2) — tres ítems diferidos «Vigente» (Api/09 :586 `PD-03`; Api/06 :760 `PA-03`, :772 `PA-04`) cuyo evento ya ocurrió y cuya decisión (`D-03`, MinVer, `BT-00033`) ya cerró a sus filas hermanas de Api/05 :1211 y :1224: propagación incompleta del PR #193.

**Descartado por el refutador, para que no vuelva:** el OK a `k` no debía asentarse en el intake por §13 (a) (`Master-Prompt.md` §13.1 lo excluye); manifiesto 7.1 «Reanudacion 1.13: no hubo reanudación en curso» era cierto en su momento; ningún evento de `DD-1..DD-9` ocurrió.

---

## 5. Tabla de veredictos (P2, jurado de cinco)

Jueces: Evidencia, Impacto, Costo y beneficio, Coherencia histórica, Riesgo e irreversibilidad. Cada uno votó a ciegas con fundamento en una línea (registro completo en la sesión; acá el reparto y el fundamento decisivo). Perito: ninguno hizo falta. **Resultado: 16 de 16 `PROCEDE`, todos 5-0.** La salvaguarda de homogeneidad de §6.4 (más del 80 % unánime) se declara disparada; el refutador debía revisar los `NO_PROCEDE` y **no hubo ninguno**: la salvaguarda queda sin materia y se registra así. Lo que sí hubo fue **desacuerdo de nivel** en tres ítems, resuelto por mayoría con el disenso anotado.

| Ítem | Hallazgo consolidado (raíz común) | Fuentes | Votos | Nivel | Ancla | Fundamento decisivo y disenso |
|---|---|---|---|---|---|---|
| **C-01** | `Roadmap-Producto.md` 1.14 §4 :118 «estrictamente secuenciales… no se empieza una antes de cerrar la anterior» y :132 «`k` depende de `i`: … topología que todavía no está desplegada de verdad» ⟷ §2.1 :66 y §3 :97: `k` **cerrada** con OK del PO el 2026-09-13, con `i` abierta y `PT-05` `SIN MEDIR` (:6 de `Medicion-PT-05.md`). El intake 5.0 §15 :590-601 no enumera `k`; `Apertura-Fase-k-2026-09-12.md` no nombra a `i` (grep → 0). `M6-31` («no concluyente») pasa a concluyente | REQ-01, LEC-03, OPE-04, VER-05, FOR-06 | 5-0 PROCEDE | **P1** | E1 + E2 | Contradicción interna al mismo documento vivo. **No reabre el OK** (decisión cerrada): el parche registra el acto, no lo revierte. Riesgo fija el borde: registrar el apartamiento es parche; **reescribir la regla de §4 sin el PO sería disparador 3** → escalada `E-1` con default (§9) |
| **C-02** | `SDD/Docs/README.md` 2.8 §7.2 :190 fila `i` «Su puerta está escrita y la fase no ocurrió … siete criterios … **Planificada, no ejecutada**», sin fila `k`; cabecera :14 «Refleja manifiesto **6.0** / intake **4.2**» vs §7.1 :176 (5.0, 7.1, 13.16); :191 «publica su resultado y no lo replica» y replica; §7.1 :172 `11-Documentacion` «Planificado» vs los tres README (`status: Vigente` / «Estado: Propuesto») | REQ-03, LEC-02, OPE-05, TRZ-05, TRZ-06, VER-05 | 5-0 PROCEDE | **P1** | E2 | El índice de entrada del corpus contradice a la fuente que declara única. Responsable declarado en :13: el orquestador de reanudación. Origen ajeno: `b1966a0` del 2026-09-12 reparó §7 y dejó la fila `i`; `k` abrió después ese día |
| **C-03** | Siete referencias a `evidencia/2026-09-12-estructura-solucion/` y `…-13-dc5-samples/` sin destino en el árbol desde #207 (`grep -rn "evidencia/2026-09-1[23]" --include=*.md .` → `changelog.md` :1113 :1147 :1590; `ADR-10008` :38; `Plan-Migracion-13.7-a-13.16.md` :102; `samples/README.md` :30 :38). Plan :102 **resuelve** (tabla anclada a `b9675d8`). Nadie dice que el contenido vive en `git show 3662bf9^:evidencia/…` | TRZ-01, LEC-04, REQ-06, VER-02 | 5-0 PROCEDE | **P2** (panel decía P1) | E1 | Sólo `ADR-10008` :38 sostiene un «Aprobado» en esa ruta; 0 enlaces rotos. **Origen: ajeno a la corrida** (la base es el merge de #207). Las tres del changelog no se editan (:1660, :1705): se resuelven con C-04. No se restaura nada |
| **C-04** | `changelog.md` sin entrada para el PR #207 (`grep -c '#207'` → 0; tampoco #203, #204, #206 por número; #204 tiene entrada :1646 sin número). R0 dim. 5 «changelog ↔ git coinciden … chore #207» refutado en su lectura amplia | TRZ-02, VER-02, REQ-06 | 5-0 PROCEDE | **P2** | E1 | La obligación nace de las tres citas huérfanas, no de :8. Coherencia: **quinta** vez que el changelog llega tarde a una fusión propia (DIV-02 del 2026-09-12 contaba cuatro), siempre reparada, nunca declarada como deuda con evento → §8 `DD-R8-2`. **Fusionado con C-03 y REF-01 en el parche PR-03** |
| **C-05** | La puerta de `i`, `scripts/verify-stage-i.sh` (último cambio `cddf9bb`, 2026-08-27): :3-4 «SIETE criterios … Roadmap **1.9**»; `I-1` :106-120 verifica `.github/workflows/deploy-front-ftp.yml`, hoy `workflow_dispatch` (manual, retirado el 2026-09-12) y da OK; `I-2` :186-195 exige revisión sellada == `origin/main`, y `Estrategia-Versionado.md` 6.0 §3.c hace que producción (`v1.1.2` = `b9675d8`) ≠ `main` (`4fc2763`) por diseño → **NO CONFORME por construcción**. Roadmap 1.14 §5.2 :163 tiene **seis** criterios y ninguno mide compose/túnel/imagen sellada. `Estado-Del-Destino-2026-09-12.md` :231 lo sabía y reparó el roadmap sin declarar la puerta como deuda; su §10 la sigue nombrando «Puerta de salida» | OPE-01, OPE-02, VER-01, REQ-04, FOR-01 | 5-0 PROCEDE | **P2** en el corpus; **deuda declarada** por el script | E1 | El script es código: **fuera de alcance**, va a §8 `DD-R8-1`. En el corpus queda README :190 «siete criterios» (cae en C-02) y `Medicion-PT-05.md` §1. `changelog.md` :786 es entrada histórica del 2026-08-18 y no ancla falsedad de hoy. **Disenso de nivel:** Impacto votó **P0 para la deuda** («bloquea el acto de cerrar `i`, que es el trabajo siguiente»); los otros cuatro no subieron de P2. Se asienta la deuda como **P1** («bloquea el cierre de `i`, no su ejecución: el trabajo de `i` es medir `PT-05`») con el disenso registrado |
| **C-06** | El corpus no dice qué corre en producción: `Roadmap` :66/:97 y `Mini-Plan.md` 3.5 :865 «`v1.1.0` en producción»; `ADR-00011` y `BT-00029` :61 declaran que `v1.1.0` choca con `RN-B1` y que la mitigación son dos variables del `.env` del PO; `v1.1.2` (#204, `b9675d8`) es la corrección y ni :1646 ni :1585 tienen sección «Desplegado» (:1367 y :1484 sí) | OPE-03 | 5-0 PROCEDE | **P2** (OPE decía P1) | E1 | **Premisa refutada por E1:** `/salud` informa `1.1.2+b9675d8`; la defensa por cuenta está desplegada; cae la convocatoria a Seguridad. Queda el hueco de registro y tres prosas con versión envejecida (R1: la versión la informa `/salud`). Fusionado con PR-03 (línea «Desplegado») y con PR-01/PR-05 |
| **C-07** | `Roadmap` :66 «los otros seis criterios de §5.1 los registran `Mini-Plan.md` de Api §3.5 y `changelog.md`»: Mini-Plan 3.5 §3.5 :449-494 registra 8 `BT` `Done`, 1 `Descartada` y el OK, ninguno de los seis; changelog :1689-1706 tampoco; no hay informe de cierre de `k` en `Audit/` (intake §15 :617 lo ubica en `Lab-Geometria.Documentacion/Avances/`, fuera del árbol); §5.2 sin fila `k`. Mini-Plan :453 «Roadmap §3 la declara “sin release comprometido: pendiente”» falso desde 1.12. Bitácora §9 :817-847 «Sin entradas al 2026-08-10. Ninguna etapa está abierta» (fechada). El OK llegó 10 h después de la última fusión de `k`, en la rama de migración, y §5.1 :146 dice «esa solicitud [PR] es el punto de control» | REQ-02, VER-04, FOR-05, OPE-06 | 5-0 PROCEDE | **P2** (:66, :453); **P3** (bitácora §9) | E2 | No reabre el OK. La sub-parte «forma del punto de control» (§5.1 punto 4) viaja con `E-1` de C-01: es la misma contradicción regla ⟷ acto; como decisión aparte sería reabrir §5.1 por mejora (Coherencia, Riesgo). Web/07 Mini-Plan §9 no se abre (Costo): deuda `DD-R8-4` |
| **C-08** | `Medicion-PT-05.md` §1 :17 «**`PT-05` es lo único que la fase `i` hace**» y su fila 1.2 («sigue siendo lo único… su criterio no varía con el cambio de topología») ⟷ Roadmap §2.1 :64 (entregable de `i`: contenedores + túnel + dominio + `PT-05`) y §5.2 :163 (seis criterios). Tres cardinalidades para lo que `i` exige: 1 / 6 / 7 | FOR-01 | 5-0 PROCEDE | **P2** (FOR decía P1) | E2 | En el Roadmap §2.2 la frase está en la fila de `PT-05` de una tabla de puertas y se lee «la única puerta»; en `Medicion` §1 está sin contexto y es absoluta. Se quita «lo único» por R1 en los dos lugares |
| **C-09** | Citas con número de versión que envejecen por tercera vez: `Medicion-PT-05.md` :10 «Roadmap **1.10**» (vigente 1.14; sus versiones 1.1 y 1.2 existen sólo para corregir esa cita); `verify-stage-i.sh` :4 «1.9»; `Mini-Plan.md` :470 «Roadmap **1.13**»; `Estado-Del-Destino-2026-09-12.md` :297; manifiesto §1.1 :39 «conjunto vigente» sin fecha | TRZ-04, VER-06, FOR-02, OPE-05, LEC-05, LEC-06 | 5-0 PROCEDE | **P3** | E1 | `Root-Rules.md` §10 R1: quitar el número, no actualizarlo. Estado-09-12 es informe fechado (no se reescribe); el manifiesto no se abre por una fecha (Costo) → deuda `DD-R8-5`; el script está fuera de alcance |
| **C-10** | `DD-8` subcontada: 116 sale de la regex de E-011 que exige `**N.N**`; hay **13** cabeceras «Trazabilidad upstream» con versión en texto plano no contadas: `00-Contexto/{Alcance,README,Roadmap,Vision}-Producto.md` :9, `01-Necesidades-Negocio/{Necesidades-Negocio.md :13, NB-00001 :11}`, `10-Examples/ejemplo-*.md` :12 (Api ×5, Web ×2). Entre ellas Roadmap **1.14**, escrito hoy: «PRODUCT-INTAKE 1.9» vs intake 5.0 | TRZ-03, FOR-03 (contradicción 6/11, resuelta por REF: 13) | 5-0 PROCEDE | **P2** | E1 | Complementa `DD-8`, no la modifica: el evento y el quién quedan. E-011 del expediente histórico no se reescribe. Se asienta en §8 como ampliación `DD-8` |
| **C-11** | Recuento de ítems diferidos abiertos no reproducible: R0/contrato «23 (22 con evento `i`)»; `Estado-Del-Destino-2026-09-12.md` :326 y `Mesa-2026-09-12.md` §8 «21»; `M6-31` «19 de 20». Mecánico sobre las 118 filas: `Estado` que empieza en «Vigente» = **20** (19 con evento `fase i` + `PD-05` de Api/09); «Cerrado en parte» = 3 (Web/09 `PD-01`, `PD-03`; Api/09 `PD-02`); `~~Vigente~~ CERRADO 2026-08-31` = 3 (Web/09 `PD-05`, Web/05 `PA-02`, Web/06 `PA-04`); dos cerradas el 2026-09-12 por `D-03` (Api/05 `PA-04`, `PA-06`) explican 21 → 19. `Root-Rules.md` §12.2 no fija conjunto cerrado para `Estado` | VER-03, FOR-04 | 5-0 PROCEDE | **P2** | E1 | Evidencia advierte que la cifra depende de la regex (obtuvo 18 con otra); por eso el informe de estado escribe **el criterio junto al número**. No contradice el «21» de Mesa-09-12 (fechado sobre `5c95dab`). El conjunto cerrado para `Estado` va como observación al framework (§8) |
| **C-12** | Aposiciones de estado en ítems diferidos, escritas el 2026-08-30 y falsas desde `k`: «punto de control de la fase `i` … **y es el último que queda**» en Api/05 :1222 :1235 :1238 :1240, Api/09 :597, Api/06 :774 :784 :787 (+ fila de control Api/05 :1261, + `Audit/Fase-i-Que-Contesta-Y-Que-No-2026-08-31.md` :35, histórico); «**Vigente.** La fase `i` está planificada y **no ocurrió**» en Api/05 :1196, Api/09 :562, Api/06 :749 | REQ-05, VER-05, FOR-06, LEC | 5-0 PROCEDE | **P3** | E1 | Dato derivado en prosa (R1): se conserva artefacto + sección, cae la aposición. El registro histórico del 2026-08-31 no se toca. Misma capa que `DD-9` (titulares de Api/05, 06, 09) |
| **C-13** | `README.md` raíz :1 es sólo «# Lab-Geometria» (`d97720a`, 2026-08-08); `SDD/README.md` :13 «`Docs/` está **vacía**: la documentación de especificación todavía no se generó», :19 «intake … 1.1, `Borrador`», :21 «Generación de `Docs/` \| Pendiente» (`0b61076`, 2026-08-09). Ningún documento vivo señala que están vencidos (grep → 0). Las dos primeras paradas de un repositorio **público de consulta** dicen «nada empezó» a un producto con `v1.1.2` en producción | LEC-01 | 5-0 PROCEDE | **P2** (LEC decía P1) | E2 | Sin regla del framework sobre esos dos archivos (`Master-Prompt.md` §11 gobierna `SDD/Docs/README.md`) → P2 por definición. El agravante es la finalidad declarada del repo. Parche: punteros sin estado |
| **C-14** | `Vision-Producto.md` §9.2 :216 «Etapa: cada uno de los tramos `a` a `h`» y :217 «cada fase **es** una etapa del intake, con el mismo identificador de letra» ⟷ Roadmap 1.11+ con `k`, que no tiene etapa en el intake §15. Y no hay conjunto cerrado de estados de fase (planificada / abierta / cerrada) | REQ-03 | 5-0 PROCEDE | **P2** por :217; **P3** el conjunto de estados | E2 | Capa corregida por el refutador: la raíz de los tres estados de `i` es C-02, no el glosario. Lo que el glosario tiene de falso es :217. El conjunto de estados no se inventa en una reanudación (Costo): deuda `DD-R8-6` |
| **RN8-REF-01** | `changelog.md` :1691 «(sobre `b9675d8`, `main` = `v1.1.1`)»; `git for-each-ref refs/tags/v1.1.*` → `v1.1.1` = `b58dec3`, `v1.1.2` = `b9675d8` | REF | 5-0 PROCEDE | **P3** | E1 | Dato derivado en prosa; :1691 no se reescribe: una línea en la entrada nueva (PR-03) |
| **RN8-REF-02** | Tres ítems diferidos «**Vigente.** Evento reasignado a la fase `i`» sobre «la herramienta que calcula la versión» (Api/09 `Pipeline-CI-CD.md` :586 `PD-03`; Api/06 `Product-Backlog.md` :760 `PA-03`, :772 `PA-04`) mientras sus filas hermanas de texto equivalente en Api/05 :1211 `PA-04` y :1224 `PA-06` están «**Cerrado** el 2026-09-12 · decisión `D-03` … MinVer … `BT-00033`». `v1.0.0` = `8e5e2f9` la calculó MinVer: el evento ocurrió | REF | 5-0 PROCEDE | **P2** | E2 | Aplica `D-03`, no la contradice: propagación incompleta del PR #193. **Disenso de nivel:** Coherencia histórica pidió **P1** por el precedente `M6-19` (ítem con evento ya ocurrido = P1 en la migración). Se asienta P2 **declarando por qué difiere**: el cierre ya está redactado con evidencia en la 05 y sólo falta replicarlo; `M6-19` era un ítem sin ningún registro de cierre. Con esto las «19 con evento `i`» útiles para el barrido son **16** |

**Descartados por `C`:** ninguno. Todo heredado que entró como `C` (`M6-31`, DIV-01..04, «21 filas») fue abierto y anclado o corregido con E1.

---

## 6. Parches aprobados (P3, texto exacto)

Cuerpo de parches distinto del jurado (§6.5): quien diseñó no votó. Compuerta de capa de origen aplicada: ningún parche corrige aguas abajo un defecto nacido aguas arriba (la única corrección aguas arriba que hace falta —§4 del Roadmap— va como escalada `E-1`). Parches que tocaban la misma sección se fusionaron. Todos reversibles por git: cada documento versionado deja su snapshot en `_legacy/2026-09-13/` y una fila de control de cambios que cita este registro. **Verificación de cada parche:** la corre quien lo aplica (R4, salida A), con el comando declarado.

| Parche | Ítems | Documento → versión | Texto exacto | Derivados a revalidar | Verificación | Reversión |
|---|---|---|---|---|---|---|
| **PR-01** | C-01 (registro del acto), C-06, C-07 (:66), C-08 (§2.2), C-09 (:9 sólo se anota en `DD-8`, no se toca) | `00-Contexto/Roadmap-Producto.md` 1.14 → **1.15** | **(a) §4, párrafo :118**, se agrega al final: « **Apartamiento registrado el 2026-09-13:** la fase `k` cerró con el OK explícito del Product Owner mientras la fase `i` seguía abierta (`PT-05` sin medir). La regla de esta sección no se reescribe acá: la decisión de mantenerla con este apartamiento o de retirar la dependencia `k` → `i` es del Product Owner (escalada `E-1` de `../Audit/Mesa-2026-09-13-ciclo-2.md` §9).» **(b) §4, fila `k` :132**, la celda «Naturaleza» pasa a: «`k` dependía del **despliegue real**, realizado el 2026-09-06, y no del cierre de `i`, que sigue abierta: así se ejecutó y así lo aprobó el Product Owner el 2026-09-13 (ver el apartamiento registrado arriba). `k` no depende de `j…` porque ninguno de sus diez ítems necesita las tres capacidades `Could Have` que `j…` planifica». **(c) §2.1 fila `k` :66**, «`v1.0.0` y `v1.1.0` en producción el 2026-09-13» → «`v1.0.0` y las correcciones `v1.1.x` publicadas el 2026-09-13 (la versión en producción la informa `/salud`, no este documento)»; y «Los otros seis criterios de §5.1 los registran `Mini-Plan.md` de `GeometriaFactory-Api` §3.5 y `changelog.md`; este documento sólo asienta el OK» → «De los otros seis criterios de §5.1, `Mini-Plan.md` de `GeometriaFactory-Api` §3.5 registra las tareas en `Done` y el OK; **los criterios 1 a 3, 5 y 6 no tienen asiento en este árbol** (el informe de cierre que el intake §15 ubica en `Lab-Geometria.Documentacion/Avances/` no se verificó desde acá); este documento sólo asienta el OK». **(d) §3 fila `k` :97**, «**`v1.0.0`** (contrato bajo `/v1/`) y **`v1.1.0`** (límite de tasa), en producción el 2026-09-13» → «**`v1.0.0`** (contrato bajo `/v1/`) y **`v1.1.x`** (límite de tasa y sus correcciones), publicadas el 2026-09-13». **(e) §2.2 fila `PT-05` :78**, «Fase `i`, y es **lo único que esa fase hace**» → «Fase `i`: es **la única puerta técnica** de esa fase; los demás criterios de la transición `i` → `j…` están en §5.2». **(f) Cabecera :9** no se toca (entra en la ampliación de `DD-8`, §8). **(g) Fila de control de cambios 1.15** citando este registro, y snapshot `_legacy/2026-09-13/Roadmap-Producto-v1.14.md` | `SDD/Docs/README.md` §7.2 (PR-02), `Medicion-PT-05.md` (PR-05), `Mini-Plan.md` de Api (PR-06) | `grep -c "Apartamiento registrado el 2026-09-13" Roadmap-Producto.md` → 1; `grep -c "lo único que esa fase hace" Roadmap-Producto.md` → 0; `grep -c "v1.1.0\` en producción" Roadmap-Producto.md` → 0; enlaces relativos del documento → 0 rotos | `git checkout 4fc2763 -- SDD/Docs/00-Contexto/Roadmap-Producto.md` y borrar el snapshot |
| **PR-02** | C-02, C-05 (corpus), C-14 (síntoma) | `SDD/Docs/README.md` 2.8 → **2.9** | **(a) Cabecera :14**: «`PRODUCT-MANIFEST-Fabrica-De-Geometria.md` **6.0**, derivado del intake **4.2**» → «el `PRODUCT-MANIFEST-Fabrica-De-Geometria.md` y el `PRODUCT-INTAKE-Fabrica-De-Geometria.md` vigentes en `../Intake/` (la versión la declara cada uno en su cabecera; este índice no la replica)». **(b) §7.2 fila `i` :190** → «\| `i` · Despliegue real \| Despliegue realizado el 2026-09-06 (contenedores en el servidor propio, túnel y dominio propio; `../../changelog.md`, entrada «Dockerizar el front»). Faltan los criterios de la transición `i` → `j…` de `00-Contexto/Roadmap-Producto.md` §5.2, entre ellos `PT-05` (`Audit/Medicion-PT-05.md`, `SIN MEDIR`). Su puerta `scripts/verify-stage-i.sh` mide los criterios de una versión anterior del roadmap y es deuda declarada (`DD-R8-1`) \| **Abierta** \|». **(c) Fila nueva `k`** a continuación: «\| `k` · Exposición pública de la API y versionado del contrato \| Contrato bajo `/v1/`, MinVer, límite de tasa, OpenAPI/Scalar publicados, sample de onboarding y política de deprecación (`v1.0.0` y `v1.1.x`) \| **Cerrada** con OK explícito del Product Owner el 2026-09-13 \|». **(d) §7.1 :172** «\| 11-Documentacion \| Producto y las dos \| **Planificado** \|» → «\| 11-Documentacion \| Producto y las dos \| El que declara cada `11-Documentacion/README.md` en su cabecera (este índice no lo replica) \|». **(e) :191** se conserva. **(f) Fila 2.9** del control de cambios y snapshot `_legacy/2026-09-13/README-v2.8.md` | ninguno | `grep -c "Planificada, no ejecutada" SDD/Docs/README.md` → 0; `grep -c "· Exposición pública" SDD/Docs/README.md` → 1; `grep -c "\*\*6.0\*\*" SDD/Docs/README.md` → 0 | `git checkout 4fc2763 -- SDD/Docs/README.md` |
| **PR-03** | C-03 (las tres del changelog), C-04, C-06 (registro), RN8-REF-01 | `changelog.md` (entrada nueva al final; **ninguna entrada anterior se edita**) | Entrada nueva: «## Se retira `evidencia/` de la raíz, y lo que el registro no había asentado — 2026-09-13 \n\n**Rama:** `chore/retirar-evidencia-de-la-raiz` (PR #207, `3662bf9`, fusionada en `4fc2763`). Sin cambio de código. \n\n### Retirado \n\n- La carpeta `evidencia/` de la raíz, con las corridas fechadas del 2026-09-01 al 2026-09-13, por decisión del Product Owner del 2026-09-13 tomada al reanudar el orquestador SDD. **El contenido no se pierde**: vive en el historial y se cita como `git show 3662bf9^:evidencia/<corrida>/<archivo>`. Las tres entradas anteriores de este registro que nombran `evidencia/2026-09-12-estructura-solucion/` y `evidencia/2026-09-13-dc5-samples/` no se reescriben; esta es su forma de resolverse. \n\n### Corregido \n\n- La entrada «Punto de control de la fase `k`» decía «`main` = `v1.1.1`» sobre `b9675d8`; `b9675d8` es **`v1.1.2`** (`git for-each-ref refs/tags/v1.1.2`). `v1.1.1` es `b58dec3`. \n\n### Desplegado \n\n- Las entradas de `v1.1.1` y `v1.1.2` no llevaron esta sección. Al 2026-09-13 23:45 UTC, `GET https://api-geometria.aplicada.stream/salud` informa `1.1.2+b9675d8`: la corrección del límite de canje por cuenta (`ADR-00011`) está en producción y la mitigación por variables de entorno que ese ADR describe queda sin efecto, como su §5.1 anticipa. La versión en producción la informa `/salud`; este registro asienta la observación con su fecha. \n\n**Asentado por:** la octava reanudación del orquestador SDD (`SDD/Docs/Audit/Estado-Del-Destino-2026-09-13.md`), que encontró el registro sin esta entrada: es la quinta vez que este documento llega tarde a una fusión propia, y por eso queda declarado como deuda con evento (`DD-R8-2`).» | ninguno | `grep -c "#207" changelog.md` → ≥ 1; `grep -c "3662bf9^" changelog.md` → ≥ 1 | Quitar la entrada (append puro) |
| **PR-04** | C-03 (prosa viva) | `Unidades-Entrega/GeometriaFactory-Web/05-Arquitectura-Tecnica/Adrs/ADR-10008-El-Bundle-Del-Visor-Lo-Genera-El-Proyecto-Del-Front.md` 1.x → +1 minor; `samples/README.md` (versión propia, +1 minor) | **ADR-10008 :38**: «(`evidencia/2026-09-12-estructura-solucion/`)» → «(`evidencia/2026-09-12-estructura-solucion/`, retirada del árbol el 2026-09-13 y conservada en el historial: `git show 3662bf9^:evidencia/2026-09-12-estructura-solucion/README.md`)». **samples/README.md :30**: «(`evidencia/2026-09-12-estructura-solucion/`)» → «(`git show 3662bf9^:evidencia/2026-09-12-estructura-solucion/`, en el historial desde el 2026-09-13)»; **:38**: «(`evidencia/2026-09-13-dc5-samples/`)» → «(`git show 3662bf9^:evidencia/2026-09-13-dc5-samples/`, en el historial desde el 2026-09-13)». Fila de control de cambios en cada uno; snapshot del ADR | ninguno | `grep -rn "evidencia/2026-09-1[23]" --include=*.md . \| grep -v _legacy \| grep -vc "3662bf9^"` → 4 (las tres del changelog + Plan :102, todas registros que no se reescriben) | `git checkout 4fc2763 -- <los dos archivos>` |
| **PR-05** | C-08 (§1), C-09 (:10), C-05 (corpus) | `Audit/Medicion-PT-05.md` 1.2 → **1.3** | **:10**: «(`Roadmap-Producto.md` **1.10** §2.2)» → «(`Roadmap-Producto.md` §2.2)». **:17**: «**`PT-05` es lo único que la fase `i` hace**, y valida `RN-B1`» → «**`PT-05` es la única puerta técnica de la fase `i`** —los demás criterios de la transición `i` → `j…` están en `Roadmap-Producto.md` §5.2— y valida `RN-B1`». **Párrafo nuevo al final de §1**: «La puerta `scripts/verify-stage-i.sh` que comprueba este formulario mide, además, los criterios de una versión anterior del roadmap (su `I-1` verifica el flujo de FTP retirado; su `I-2` exige que producción coincida con `main`, cosa que la estrategia de versionado impide por diseño). Hasta que se reescriba (`DD-R8-1`), esta medición se asienta en este documento y la puerta no certifica la transición.» **Fila 1.3** del control de cambios: «Tercera corrección de la misma cita (1.1: 1.8 → 1.9; 1.2: 1.9 → 1.10): esta vez se quita el número (`Root-Rules.md` §10 R1) para que no vuelva a envejecer». Snapshot | `SDD/Docs/README.md` §7.2 (PR-02) | `grep -c "lo único que la fase" Medicion-PT-05.md` → 0; `grep -c "\*\*1.10\*\*" Medicion-PT-05.md` → 0 | `git checkout 4fc2763 -- SDD/Docs/Audit/Medicion-PT-05.md` |
| **PR-06** | C-07 (:453, :470, :865), C-09 | `Unidades-Entrega/GeometriaFactory-Api/07-Plan-Sprint/Mini-Plan.md` 3.5 → **3.6** | **:453**: «y `Roadmap-Producto.md` §3 la declara «sin release comprometido: pendiente». Por esa razón» → «y `Roadmap-Producto.md` §2.1 y §3 registran su estado (este documento no lo replica). Por esa razón». **:470**: «asentado en `Roadmap-Producto.md` **1.13** §3 fila `k`» → «asentado en `Roadmap-Producto.md` §3 fila `k`». **§3.5, párrafo nuevo tras :470**: «**Los otros seis criterios de §5.1 del roadmap no están asentados en este documento**: esta sección registra las tareas del tramo y el OK. Si existe un informe de cierre del tramo (`PRODUCT-INTAKE` §15 lo ubica en `Lab-Geometria.Documentacion/Avances/`), vive fuera de este repositorio». **§9 bitácora**, encabezado: se agrega «**Desde el 2026-08-16 el avance por etapa lo registra `../../../../../changelog.md`** (`SDD/Docs/README.md` §7.2 lo declara única fuente); esta bitácora no se completó y no se completa retroactivamente». **Fila 3.6**; snapshot | Web/07 Mini-Plan §9 (deuda `DD-R8-4`, no se abre) | `grep -c "sin release comprometido: pendiente" Mini-Plan.md` → 0; `grep -c "\*\*1.13\*\*" Mini-Plan.md` → 0 | `git checkout 4fc2763 -- <archivo>` |
| **PR-07** | C-12, RN8-REF-02 | `Unidades-Entrega/GeometriaFactory-Api/05-Arquitectura-Tecnica/Arquitectura-Unidad-Entrega.md` 3.15 → **3.16**; `…/06-Backlog-Tecnico/Product-Backlog.md` 4.6 → **4.7**; `…/09-Devops/Pipeline-CI-CD.md` 3.10 → **3.11** | **(a)** En las 8 filas (Api/05 :1222 :1235 :1238 :1240; Api/09 :597; Api/06 :774 :784 :787) y en la fila de control Api/05 :1261: «, que `PRODUCT-INTAKE` §10 y §15 declaran bloqueante y es el último que queda» → «, que `PRODUCT-INTAKE` §10 y §15 declaran bloqueante». **(b)** En las 3 filas (Api/05 :1196, Api/09 :562, Api/06 :749): «**Vigente.** La fase `i` está planificada y **no ocurrió**» → «**Vigente.** El punto de control de la fase `i` no ocurrió». **(c)** Api/09 :586 `PD-03`, Api/06 :760 `PA-03` y :772 `PA-04`: la celda `Estado` pasa a «**Cerrado** el 2026-09-12 · **decisión `D-03` del Product Owner** en `../../../Audit/Mesa-2026-09-12-ciclo-2.md` §4: MinVer, realizada por `BT-00033` (`v1.0.0` = `8e5e2f9`, calculada por MinVer). Propagación asentada el 2026-09-13 desde `05` §11 `PA-04`/`PA-06`, que ya lo registraban», conservando el texto anterior tachado si el documento sigue esa convención. **(d)** Fila de control de cambios en los tres, citando este registro; snapshots | `Estado-Del-Destino-2026-09-13.md` §4 (recuento: 20 → 17 «Vigente», 16 con evento `i`) | `grep -rc "el último que queda" SDD/Docs --include=*.md \| grep -v _legacy \| grep -v ":0"` → sólo `Audit/Fase-i-Que-Contesta-Y-Que-No-2026-08-31.md` (histórico); `grep -c "planificada y \*\*no ocurrió" <los tres>` → 0 | `git checkout 4fc2763 -- <los tres>` |
| **PR-08** | C-14 (:217) | `00-Contexto/Vision-Producto.md` 1.x → +1 minor | **:216**: «Cada uno de los tramos `a` a `h` en que el intake descompone la construcción del producto» → «Cada uno de los tramos en que el intake §15 descompone la construcción del producto (`a` a `h` comprometidos, `i…` pendientes)». **:217**: «En `Roadmap-Producto.md`, cada fase **es** una etapa del intake, con el mismo identificador de letra. No es una agrupación distinta ni una unidad nueva» → «En `Roadmap-Producto.md`, cada fase que corresponde a una etapa del intake lleva su misma letra. **Desde la 1.11 del roadmap hay fases que el intake §15 no enumera** (`k`, por decisión del Product Owner posterior al handoff): son fases del roadmap sin etapa homónima en el intake, y el roadmap es su única fuente». Fila de control de cambios; snapshot | ninguno | `grep -c "cada fase \*\*es\*\* una etapa" Vision-Producto.md` → 0 | `git checkout 4fc2763 -- <archivo>` |
| **PR-09** | C-13 | `README.md` (raíz) y `SDD/README.md` (sin esquema de versión) | **README.md** pasa a: «# Lab-Geometria \n\nFábrica de Geometría: laboratorio didáctico de geometría analítica de la cátedra, público de consulta. \n\n- **Estado del producto y de su documentación:** `SDD/Docs/README.md` §7. \n- **Registro de cambios del código:** `changelog.md`. \n- **Cómo trabajar en este repositorio:** `AGENTS.md`. \n\nEste archivo es un puntero: no replica estado.» **SDD/README.md** pasa a: «# SDD — Fábrica de Geometría \n\nDocumentación del producto generada con el framework SDD. \n\n- El índice, el estado por categoría y por etapa, y la procedencia del framework están en `Docs/README.md`. \n- El intake y el manifiesto vigentes están en `Intake/`. \n- Los informes de auditoría, migración y reanudación están en `Docs/Audit/`. \n- Los expedientes de caso están en `Expedientes/`. \n\nEste archivo es un puntero: no replica estado. Su contenido anterior (2026-08-09) afirmaba que `Docs/` estaba vacía y quedó vencido sin que nada lo señalara; se reemplaza el 2026-09-13.» | ninguno | `grep -c "está \*\*vacía\*\*" SDD/README.md` → 0; enlaces de los dos archivos → 0 rotos | `git checkout 4fc2763 -- README.md SDD/README.md` |

**Ampliación de `DD-8` (C-10) y corrección del recuento (C-11):** no son parches sobre documentos vivos; se asientan en §8 de este registro y en el informe de estado.

---

## 7. Deuda declarada (`Root-Rules.md` §12.2)

Ítems que la mesa decidió **no corregir en esta corrida**, con sus campos y su ciclo de origen.

| Id | Qué falta | Por qué no se puede hoy | Quién lo cierra | En qué evento se cierra (artefacto y sección) | Ciclo de origen |
|---|---|---|---|---|---|
| `DD-R8-1` | Reescribir `scripts/verify-stage-i.sh` a los **seis** criterios de `Roadmap-Producto.md` §5.2 fila `i` → `j…`: `I-1` sobre la composición de contenedores y el túnel (no sobre `deploy-front-ftp.yml`); `I-2` contra la **última etiqueta `v*` alcanzable** o la revisión que la estrategia declare desplegada (no contra `origin/main`); cita al roadmap sin número. **Nivel P1** (Impacto votó P0: bloquea el acto de cerrar `i`) | Es código, fuera del alcance de la mesa y de la reanudación (`Migracion-Rules.md` §2.2 por analogía; `Master-Prompt-Reanudacion.md` §0) | Desarrollo de `GeometriaFactory-Web`/`Api`, por una `BT` del backlog técnico | Esa `BT` en `Done`, citada desde `Medicion-PT-05.md` §1 y desde `Roadmap-Producto.md` §2.2 fila `PT-05` | mesa (2026-09-13, ciclo 2) · producto · base `4fc2763`. El hueco lo abrió la salida A del 2026-09-12 (`Estado-Del-Destino-2026-09-12.md` :231), que reparó el roadmap sin declarar la puerta |
| `DD-R8-2` | El `changelog.md` llegó tarde a una fusión propia por **quinta** vez (#186 en la cuarta; #207 en ésta). Falta el mecanismo que lo haga subproducto del acto (`Master-Prompt-Reanudacion.md` §1.1 R3), o la comprobación en la compuerta de arranque T0 del destino | Diseñar el mecanismo es del equipo y del Product Owner, no de una reanudación | Product Owner, con el equipo | `changelog.md` cabecera (:3-6, «Quién lo actualiza / Cuándo») o `Estrategia-Versionado.md` §11, cuando declaren el mecanismo | mesa (2026-09-13, ciclo 2) · producto · base `4fc2763` |
| `DD-R8-3` | La regla de secuencialidad de `Roadmap-Producto.md` §4 :118 y la dependencia `k` → `i` :132 quedan con **apartamiento registrado** (PR-01) hasta que el Product Owner decida `E-1` | Reescribir la regla sin el PO es disparador 3 de `Mesa-Rules.md` §7 | Product Owner | Fila de control de cambios de `Roadmap-Producto.md` que asiente la respuesta a `E-1` (o, si el PO elige apartamiento formal, el ADR de nivel Producto de la familia `ADR-14001..14004`) | mesa (2026-09-13, ciclo 2) · producto · base `4fc2763`. El hecho es del 2026-09-12/13 (apertura y cierre de `k`), anterior a la base |
| `DD-R8-4` | `Unidades-Entrega/GeometriaFactory-Web/07-Plan-Sprint/Mini-Plan.md` §9 (bitácora) :390, :400 «Sin entradas al 2026-08-10. Ninguna etapa está abierta» — fechado, no falso, pero segunda fuente vacía del avance | Abrir Web/07 sólo por una bitácora fechada no justifica una versión (Costo) | Titular de la 07 de `GeometriaFactory-Web` | Próxima emisión de ese `Mini-Plan.md`, en su §9, con la misma nota de remisión al `changelog.md` que PR-06 pone en el de Api | mesa (2026-09-13, ciclo 2) · producto · base `4fc2763` |
| `DD-R8-5` | `PRODUCT-MANIFEST` 7.1 §1.1 :39 «leyeron del conjunto **vigente** en `IA.SDD` `main` `8c55a1e`» sin fecha: «vigente» envejece (13.18 existe desde hoy) | No se abre el manifiesto por una fecha; y la procedencia la escribe la migración o la salida C con verificación | Orquestador de migración o de reanudación, en la próxima escritura de §1.1 | `PRODUCT-MANIFEST` §1.1, en su próxima versión: «vigente al AAAA-MM-DD» | mesa (2026-09-13, ciclo 2) · producto · base `4fc2763` |
| `DD-R8-6` | Conjunto cerrado de **estados de fase** (planificada / abierta / cerrada con OK) en `Vision-Producto.md` §9.2, que hoy no existe y permitió tres estados para `i` | Inventar vocabulario en una reanudación es diseño, no reparación | Titular de la 00 (AG-00), con el Product Owner | `Vision-Producto.md` §9.2, en la emisión que siga a la respuesta de `E-1` | mesa (2026-09-13, ciclo 2) · producto · base `4fc2763` |
| **`DD-8` (ampliación)** | A las **116** cabeceras de `Mesa-2026-09-13.md` §8 se suman **13** que la regex de E-011 no ve porque citan la versión **sin negrita**: `00-Contexto/Alcance-Producto.md` :9, `00-Contexto/README.md` :9, `00-Contexto/Roadmap-Producto.md` :9, `00-Contexto/Vision-Producto.md` :9, `01-Necesidades-Negocio/Necesidades-Negocio.md` :13, `01-Necesidades-Negocio/NB-00001-…md` :11, y los siete `10-Examples/ejemplo-*.md` :12 de Api (5) y Web (2). Total **129**. Comando: `grep -rlE "Trazabilidad upstream.*PRODUCT-(INTAKE\|MANIFEST)\`? [0-9]+\.[0-9]+" SDD/Docs --include=*.md \| grep -v _legacy \| grep -v Audit` → 13 | El plan aprobado de la migración no las incluía; `Migracion-Rules.md` §4.1 | El mismo de `DD-8` | El mismo de `DD-8`; la repetición de E-011 debe correr **sin exigir negrita** | audit de cierre de M4 (2026-09-13) ampliado por esta mesa · producto · base `4fc2763` |
| **Ítem «barrido de las 21 filas» de `Mesa-2026-09-12.md` §8 (re-enunciado)** | La colección se nombra por criterio y no por número: «las filas de las tablas de ítems diferidos de 05/06/09 de Api y Web cuyo `Estado` empieza en **Vigente** y cuyo evento nombra la fase `i`». Al 2026-09-13, base `4fc2763`, antes de PR-07: **19**; después de PR-07: **16** | El evento (cierre de `i`) no ocurrió | El mismo | El mismo | mesa (2026-09-12) re-enunciado por esta mesa (2026-09-13, ciclo 2) · producto · base `4fc2763` |

**Observaciones al framework** (no son deuda del destino; van al informe de estado §12): `Root-Rules.md` §12.2 fija los campos del ítem diferido pero **no un conjunto cerrado para `Estado`** («Vigente», «Cerrado», «Cerrado en parte», «NO APLICA», tachado…), por lo que el recuento de abiertos depende de la regex de quien cuenta (C-11). `Master-Prompt-Reanudacion.md` §2 paso 4 podría exigir que R1 publique el criterio de cuenta junto al número.

---

## 8. Escaladas (`Mesa-Rules.md` §7), agrupadas

**Una.** Ninguna otra consulta cayó en los siete disparadores; todo lo demás lo resolvió la mesa y está en §5–§7.

```text
E-1 · La regla de secuencialidad del roadmap, después de que k cerró con i abierta
  QUÉ PASÓ         Roadmap-Producto.md 1.14 §4 dice «estrictamente secuenciales: no se empieza una antes de
                   cerrar la anterior» y «k depende de i». El 2026-09-12 se abrió k con i abierta, y el
                   2026-09-13 la cerraste con tu OK. Ninguna fila del roadmap ni ADR registra que la regla
                   cedió ni por qué. La dependencia k → i vive sólo en el roadmap (el intake §15 no enumera k).
  ORIGEN DEL HECHO  ajeno a la corrida · calculado contra 4fc2763: la apertura (9167e68…, 2026-09-12) y el OK
                   (2026-09-13) están en la base.
  DISPARADOR       3 (cambio de alcance: reescribir una regla de delivery) — por eso no lo hace la mesa.
  OPCIONES         (1) Mantener la regla y registrar k como APARTAMIENTO: PR-01 (a)(b) queda como está y se
                       emite un ADR de nivel Producto (familia ADR-14001..14004) que lo declare.
                   (2) Reescribir §4: k pasa a depender del «despliegue real realizado», no del cierre de i;
                       :118 pierde el «no hay paralelismo posible». El orquestador redacta el texto y vos lo
                       aprobás en el PR.
                   (3) Dejarlo como queda con PR-01 (apartamiento registrado en prosa, sin ADR ni reescritura).
  PROPUESTA        (2): describe lo que pasó de verdad —k dependía del despliegue, que existía desde el
                   2026-09-06— y deja j… con una regla que sí se va a cumplir. Alternativa razonable: (1),
                   si querés que el orden en serie siga siendo la regla y k conste como excepción.
  SI NO RESPONDÉS  rige (3): PR-01 (a)(b) registra el apartamiento y DD-R8-3 queda abierta con evento «fila
                   de control de cambios del roadmap que asiente tu respuesta».
  QUÉ NECESITO     «(1)», «(2)» o «(3)».
```

La respuesta se asienta con su literal (`Mesa-Rules.md` §7.1, `Expediente-Rules.md` §3.3) en el informe de estado §9 y en la fila de control de cambios del roadmap que la ejecute.

**Respuesta del Product Owner a `E-1`** (2026-09-13, Claude Code, consulta de R2): «(2) Reescribir §4 (Recommended)». Se ejecutó en el parche PR-01 con la opción (2) en lugar de (a)(b) de apartamiento: `Roadmap-Producto.md` 1.15 §4 rige la secuencialidad sobre `a`–`i` y la fila `k` depende del despliegue real. **`DD-R8-3` queda cerrada** por ese evento (fila 1.15 del control de cambios del roadmap). Testimonio completo en [`Estado-Del-Destino-2026-09-13.md`](Estado-Del-Destino-2026-09-13.md) §8.

---

## 9. Bloque de cierre (`Mesa-Rules.md` §6.7)

```text
CIERRE DE MESA — Lab-Geometria, 2026-09-13 (ciclo 2)

  PANEL
    Convocados:     Requisitos 6 · Verificación 6 · Lector sin contexto 6 · Trazabilidad documental 6 ·
                    Operación y entrega 6 · Formal 6 · Refutador 14 revisados + 2 propios
    Descartados:    Arquitectura, Datos y dominio, Seguridad, Concurrencia, Rendimiento, Interfaz externa,
                    Cumplimiento, Uso y accesibilidad: sin señal en las superficies del estado
    Ad hoc:         ninguno
    Postergados:    ninguno
    Aporte nulo:    ninguno (los tres variables produjeron PROCEDE)

  HALLAZGOS
    Detectados: 36 + 2   Consolidados: 16   Procedentes: 16 (16 de 16, todos 5-0)   Con parche: 15 en 9 parches
    Descartados por C: 0   Refutados en premisa o cifra: 3 (C-06 «sólo el PO sabe», C-10 «6» y «11», C-03 origen)
    Homogeneidad: 100 % unánime → salvaguarda §6.4 disparada; sin NO_PROCEDE que revisar. Disenso de nivel en
    C-05 (P0 vs P2), C-13 (P1 vs P2) y REF-02 (P1 vs P2), resuelto por mayoría y registrado

  COMPUERTA (§10.0)
    Resultado:      verify-solution-tree.sh conforme; 6456 enlaces relativos, 0 rotos
    No mirado:      el código, el despliegue, el contenido de 554 documentos como conjunto (lo miró el panel)

  ENTREGA AL ORQUESTADOR
    Parches:            9 (PR-01..PR-09), en 00-Contexto, Docs/README, changelog, Web/05 ADR, samples/README,
                        Audit/Medicion-PT-05, Api/05, Api/06, Api/07, Api/09, README raíz, SDD/README
    Deuda declarada:    6 nuevas (DD-R8-1..6) + 1 ampliación (DD-8, 116 → 129) + 1 re-enunciado (barrido)
    Capas a revalidar:  SDD/Docs/README.md §7.2 tras PR-01; Estado-Del-Destino-2026-09-13.md §4 tras PR-07
    Escaladas:          1 (E-1), agrupada, con default

  CIERRE
    Por decisión, con lo que quedó abierto: E-1 (del Product Owner), DD-R8-1..6. Una ronda de panel, una de
    refutación y una de jurado; el contador es propio de este ciclo y no acumula con las rondas de la
    migración del ciclo 1 de hoy. No se corrió una segunda ronda interpretativa: los 16 hallazgos ya tenían
    parche o deuda, y la siguiente reanudación vuelve a R0 sobre el árbol reparado (salida A).
```

---

## 10. Control de cambios

| Versión | Fecha | Cambios | Autor |
|---|---|---|---|
| 1.1 | 2026-09-13 | §8 asienta la respuesta del Product Owner a `E-1` (opción 2) y el cierre de `DD-R8-3` por PR-01. | AG-00970, Presidente de mesa (orquestador de reanudación SDD) |
| 1.0 | 2026-09-13 | Emisión. Segundo ciclo de mesa del día (el primero, `Mesa-2026-09-13.md`, fue la M1 de la migración 13.7 → 13.16). Convocada desde R1.5 de la octava reanudación sobre la base `4fc2763`. Seis especialistas a ciegas, un refutador con observación E1 de producción, jurado de cinco: 16 de 16 procedentes, nueve parches con texto exacto, seis deudas nuevas, una ampliación de `DD-8`, una escalada. | AG-00970, Presidente de mesa (orquestador de reanudación SDD) |
