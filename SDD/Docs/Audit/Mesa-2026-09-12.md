# Mesa de evaluación · 2026-09-12 — «La etapa que corría sin que el corpus lo supiera»

| | |
| --- | --- |
| **Versión** | 3.0 |
| **Convocada por** | `Master-Prompt-Reanudacion.md` **1.11**, fase **R1.5**, sobre el destino `PROG2/Geometria/Lab-Geometria` |
| **Desde dónde** | Reanudación invocada por el Product Owner (`PROMPTs/SDD/05-Ejecutar-Reanudar-Orquestador/`), sobre la revisión `5c95dab` de `main`, árbol limpio |
| **Familia de hallazgos de este ciclo** | **`RD-`** — no reusa ninguna familia presente en `SDD/Docs/Audit/` (`H`, `M`, `MI`, `U`, `HM`, `E`, `P`, `D` ya están tomadas) |
| **Niveles** | `P0` a `P3` de `Master-Prompt.md` §10. **Anclas** E1–E4 / C de `Mesa-Rules.md` §6.1 |
| **Registro del estado** | [`Estado-Del-Destino-2026-09-12.md`](Estado-Del-Destino-2026-09-12.md) |

---

## 1. Contrato de entrada (`Mesa-Rules.md` §4)

| Campo | Valor |
| --- | --- |
| `objeto` | `SDD/Docs` del destino: **506** documentos vivos fuera de `_legacy`, revisión `5c95dab`. Superficie prioritaria fijada por las divergencias de R1: `00-Contexto/Roadmap-Producto.md`, `changelog.md`, `SDD/Docs/README.md` §7, la categoría **09** de las dos unidades, `ADR-10007`, `ADR-14003`, `Vista-Producto.md`, `Pipeline-Producto.md`, el intake §16 y §17, `scripts/verify-stage-i.sh`, `.github/workflows/deploy-front-ftp.yml` y las ocho tablas de ítems diferidos |
| `estado` | Las seis dimensiones de R0, con cinco divergencias declaradas (`DIV-01` a `DIV-05`) |
| `diff_normativo` | 13.7 → 13.10: tres incrementos **minor** (`Mesa-Rules` 1.0→1.1, `Master-Prompt-Reanudacion` 1.10→1.11, `Catalogo-De-Criterios` 1.14→1.15; 13.9 y 13.10 sólo agregan `Conocimiento/`). **Ningún artefacto del corpus alcanzado** |
| `restricciones_duras` | `RN-*` del intake; `RA-01`, `RA-02`, `RA-03`; apartamientos vigentes (`visor/` en la raíz, identidad npm, `AP-01`..`AP-04`); el framework no se modifica; `PROMPTs/` no se escribe; **la mesa no releva el estado ni aplica parches** |
| `decisiones_cerradas` | Intake **4.1** / manifiesto **6.0** (el visor en el agrupador); salida `C`→`D` del 2026-08-27; `D1`..`D11` de `A3-Decisiones-Del-Product-Owner.md`; los catorce hallazgos cerrados del reporte de samples; `ADR-10008`; **el retiro del hosting externo y la dockerización del front (PR #186, 2026-09-06, decisión del Product Owner)** |
| `fuera_de_alcance` | `Migracion-Rules.md` §2.2 (`_legacy`, `PROMPTs/`, `samples/` como código); el repositorio `Container.Lab-Geometria` —se cita, no se parchea—; el framework; medir `PT-05` |
| `pendientes_declarados` | Los ítems diferidos vigentes y sus eventos; los tres «fuera del producto» del reporte de samples §0; el `Plan-Mejora-Integral-2026-08-31` |
| Topes | **7** hallazgos por especialista; `ciclos_max` 2 |

**Un campo del contrato salió con un error propio, y lo levantó el panel.** La fila `estado` declaraba «118 ítems diferidos: 87 cerrados, 23 vigentes, 6 NO APLICA», que suma 116 y no 118. El recuento sobre el instrumento da **87 cerrados + 25 vigentes + 6 NO APLICA** (`RD-FOR-04`). Se corrige acá y en el informe de estado.

---

## 2. Registro de convocatoria (`Mesa-Rules.md` §6.2)

**Barrido de señales** sobre el corpus, con ubicación:

| Señal | Dónde | Especialidad que activa |
| --- | --- | --- |
| Dos unidades de entrega, integración entre sistemas, restricción dura de stack | `Vista-Producto.md` §2–§4 | Arquitectura |
| Despliegue, ambientes, publicación, versionado | categoría **09** de las dos unidades; `Pipeline-Producto.md` §3 | Operación y entrega |
| Autenticación, exposición pública, secretos | `Supply-Chain-Seguridad.md` ×2; `.github/workflows/deploy-front-ftp.yml`; `DESPLIEGUE.md` §Publicación | Seguridad |
| Umbrales, cuantificadores, recuentos | `PT-01`..`PT-05`; los siete criterios `I-1`..`I-7`; las tablas de magnitudes | **Formal** (automática, `Mesa-Rules.md` §5.2) |
| Identificadores, enlaces internos, citas entre documentos | 5.788 enlaces internos; `AGENTS.md`; los ocho documentos con `PA-*`/`PD-*` | Trazabilidad documental |
| Entidades, ciclos de vida, persistencia | sin cambio desde la última mesa | Datos — **no convocada** |
| Interfaz externa, retrocompatibilidad | `Contratos-REST.md` sin cambios en este ciclo | Interfaz — **no convocada** |
| Concurrencia, rendimiento, cumplimiento, accesibilidad | sin señal nueva | **no convocadas** |

**Composición votada por los cinco jueces** (mayoría simple; empate convoca; techo de cinco variables):

| Rol | Voto | Motivo |
| --- | --- | --- |
| Requisitos, Verificación, Lector sin contexto, Refutador | núcleo | `Mesa-Rules.md` §5.1, se convocan siempre |
| Arquitectura | `CONVOCAR` 5-0 | La topología real contradice decisiones de nivel producto |
| Operación y entrega | `CONVOCAR` 5-0 | La categoría 09 es la superficie que el despliegue real alcanza |
| Seguridad | `CONVOCAR` 4-1 | La API pasó a tener nombre público; el voto en contra sostenía que era de arquitectura. Empate no hubo; se convocó |
| Trazabilidad documental | `CONVOCAR` 5-0 | Tres de las cinco divergencias son de fuente declarativa envejecida |
| Formal | automática | Señal presente (`Mesa-Rules.md` §5.2) |
| Datos y dominio | `NO_CONVOCAR` 5-0 | Ningún cambio de entidades ni de persistencia desde la mesa anterior |
| Interfaz externa | `NO_CONVOCAR` 4-1 | `Contratos-REST.md` no cambió; el voto en contra lo pedía por la exposición pública, que quedó en Seguridad |
| Concurrencia, Rendimiento, Cumplimiento, Uso | `NO_CONVOCAR` 5-0 | Sin señal |

**Ad hoc instanciados**: ninguno. El catálogo cubrió las nueve señales.
**Postergados por cupo**: ninguno (cinco variables, exactamente el techo).
**Ronda de convocatoria en caliente**: las seis solicitudes emitidas por el panel (cuatro pidiendo Operación o Seguridad, dos pidiendo Arquitectura) **ya estaban convocadas**; no hubo especialidad faltante.

**Incidencia de ejecución, declarada**: tres especialistas (Formal, Operación, Seguridad) cayeron por límite de sesión del proveedor en su primera corrida y se relanzaron. Los tres entregaron; ninguno vio informes ajenos. La ceguera de §5.3 se mantuvo.

---

## 3. Compuerta mecánica (`Master-Prompt.md` §10.0), antes de convocar

| Comprobación | Resultado |
| --- | --- |
| **Enlaces internos** sobre `SDD/Docs` fuera de `_legacy` | **5.788 enlaces, 10 no resuelven** — todos en `Audit/`, y **seis de los diez son literales de plantilla** (un marcador de posición con la palabra «destino» o «ruta» adentro, escrito como ejemplo dentro de una explicación) y no rutas rotas. Los cuatro restantes son `Estrategia-Testing.md` ×2 y `Casos-Prueba-Referenciales.md` ×2 citadas desde `E-08-Calidad-…-r2.md` y `Mesa-2026-08-31-B.md` con ruta relativa vieja. Hallazgo `RD-PRES-03`, `P3` |
| **Ítems diferidos con los cuatro campos de §12.2** | 118 filas, **las 118 con los cuatro campos**. Ninguna sin evento |
| **Recuento del agrupador** tras el PR #187 | El `.sln` tiene 20 proyectos; `Plan-Etapa-A.md` 1.12 y `Vista-Producto.md` 1.10 ya lo recalcularon el mismo día |
| **Familias de identificador** | Sin colisión con la familia `RD-` de este ciclo |

**Lo que la compuerta declara no haber mirado**: el contenido de los documentos (es del panel), los enlaces de `_legacy/` (excluidos por §10.0), y los enlaces dentro de `samples/` y `evidencia/` (no son corpus de especificación).

---

## 4. Informes del panel

Ocho especialistas, a ciegas y en paralelo; el refutador último. **52 hallazgos emitidos.** Los informes completos quedaron en el expediente de la corrida; acá van sus hallazgos con la ubicación y el ancla que cada uno declaró.

### 4.1 Requisitos — 7 hallazgos

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-REQ-01` | La fase `i` está definida con dos frases de naturaleza distinta —resultado y topología— sin que ningún documento diga cuál manda; la fuente upstream (`F-14`, intake l.179) es la de topología | E2 | P1 |
| `RD-REQ-02` | `I-1` no es verificable: el guion corrido contra la dirección real **aprueba en verde** porque sólo comprueba que `deploy-front-ftp.yml` exista | **E1, reproducido** | **P0** |
| `RD-REQ-03` | `I-3` y la premisa de la partición cambiaron de naturaleza: la API es alcanzable desde Internet y el guion tiene una sola `API_URL` para dos direcciones distintas | E1/E2 | P1 |
| `RD-REQ-04` | `X-10` («túnel saliente con dominio propio») es **exclusión vigente** (intake l.323) e incógnita **viva** (§22 l.1856), y es exactamente lo que se desplegó; su condición de reevaluación se cumplió sin reevaluación | **E1** | **P0** |
| `RD-REQ-05` | Requisito nuevo no declarado: las claves de protección de datos persisten fuera de la imagen; `Entornos-Deploy.md` l.125 dice «sólo hay dos valores de configuración y los dos son secretos» | E1/E2 | P1 |
| `RD-REQ-06` | `ADR-14003` ni vigente ni superado: su contexto desapareció por una vía que su campo 4 no previó, y sigue rigiendo sobre el front de somee, que sigue publicándose | E2/E1 | P1 |
| `RD-REQ-07` | `ADR-10007` conserva el principio y perdió el mecanismo: el control «el flujo termina comprobando la dirección pública» **no existe** en el canal real | E2/E1 | P1 |

**Lo que revisó y está bien**: `I-4` y `Medicion-PT-05.md` siguen unívocos y el guion los reporta correctamente en falla; `I-2` e `I-6` miden el servicio real; «la dirección por configuración, nunca embebida» se cumple.

### 4.2 Verificación — 6 hallazgos

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-VER-01` | `DIV-01` confirmada con una precisión: la fase `i` **tal como está definida** no ocurrió, y lo del 2026-09-06 es un despliegue real con otra topología. **El intake se reemitió 4.0 y 4.1 el 2026-09-12 sin absorber la decisión del 2026-09-06** | E1/E2 | P1 |
| `RD-VER-02` | `DIV-02` confirmada; además `changelog.md` l.894 («hasta la fase `i` no hubo despliegue real ni release público») es hoy falso | E2/E1 | P1 |
| `RD-VER-03` | `DIV-03` confirmada y más larga: **seis** afirmaciones falsas en `README.md` §7, una de ellas contradiciendo su propia tabla desde la emisión 2.5 | E1/E2 | P1 |
| `RD-VER-04` | `DIV-04` confirmada y **refutada en un matiz**: el hosting no está retirado como hecho sino como decisión con **tres pasos pendientes**, y dos canales publican en paralelo sin que ningún documento lo diga | E1/E2 | P1 |
| `RD-VER-05` | **13 ítems diferidos vencieron** con el despliegue del 2026-09-06 y **1 venció nueve veces** (`PD-05`, cuyo evento era «la próxima emisión de la 05», que emitió 3.1 a 3.9). **Ninguno** califica `NO APLICA`: todos conservan objeto | E2 | P1 |
| `RD-VER-06` | La puerta se puede correr pero **no puede pasar**: sólo `I-2` e `I-6` tienen evidencia, y vive fuera del corpus | E1/E2 | P1 |

**Lo que revisó y está bien**: `DIV-05` no es defecto; `verify-stage-i.sh` es honesto sobre sus límites; el flujo y el changelog del 2026-09-12 son coherentes sobre `ADR-10008`.

### 4.3 Lector sin contexto — 7 hallazgos

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-LEC-01` | El `README.md` raíz es una sola línea y no lleva a ningún índice | E1 | P2 |
| `RD-LEC-02` | `README.md` §7 se contradice a sí mismo: l.155 «etapa `e`» contra su tabla l.184-188 «`a`..`h` Cerrada» | E1 | **P0** |
| `RD-LEC-03` | «¿Dónde corre y cómo publico?» no tiene respuesta en el árbol: **cero** menciones a la topología vigente; el corpus manda al hosting retirado | E1/E2 | **P0** |
| `RD-LEC-04` | Dos canales de publicación vivos y contradictorios en el mismo árbol, sin que ningún documento declare cuál rige | E1 | **P0** |
| `RD-LEC-05` | El camino «cerrar la fase `i`» conduce a un instrumento que mide la topología retirada | E1 | P1 |
| `RD-LEC-06` | `changelog.md` sin entrada para el PR #186: **tercera reincidencia** del patrón que el propio documento narra como `D-01` | E2 | P1 |
| `RD-LEC-07` | `AGENTS.md` y `Contrato-Agentes.md` §7 enlazan **cuatro** documentos inexistentes sin marcarlos `Planificado` | E2 | P1 |

**Lo que revisó y está bien**: `deploy/compose.yaml` explica el reparto fuente/host; `verify-stage-i.sh` distingue mecánicos de declarados; la entrada del changelog del 2026-09-12 es el formato a copiar.

### 4.4 Arquitectura — 7 hallazgos

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-ARQ-01` | La decisión de topología del producto está contradicha: el intake l.563 y `Web/05` l.61 **descartan** servir el front desde el servidor propio, que es lo que corre. **Falta un ADR de nivel Producto** | **E1** | **P0** |
| `RD-ARQ-02` | «Deja de aplicar» (del proyecto de contenedor) **no es** ninguno de los cuatro estados de `Root-Rules.md` §11 | E1 | P1 |
| `RD-ARQ-03` | `ADR-10007` es aceptada e inmutable: corresponde un `ADR-10009` que la supere, no editarla | E1 | P1 |
| `RD-ARQ-04` | `Api/05` l.585 declara «un puerto publicado hacia el enrutador, único punto de entrada» y el intake l.787 «únicamente `Web`; el navegador nunca la alcanza»; el proyecto de contenedor publica la API como «la que consumen los alumnos» | E1 | P1 |
| `RD-ARQ-05` | `Vista-Producto.md` §5 afirma que **no hay** `Producto/Adrs/` mientras su propio §1.1 cuenta 10 y la carpeta tiene 13; `ADR-14003` no está indexado | E1 | P1 |
| `RD-ARQ-06` | `I-3` tiene verificada su primera mitad y **sin evidencia** la segunda sobre el despliegue real | E2 | P2 |
| `RD-ARQ-07` | `ADR-10007` dice `Aprobado` en la cabecera y `Propuesto` en §3; ninguno pertenece al conjunto cerrado | E1 | P3 |

**Corrección a la premisa del contrato**: `Vista-Producto.md` §6 **no** contiene ninguna fila sobre «IP dinámica actualizada a mano». El contrato la citaba de memoria. **El contrato de integración `Web → Api` no cambió**: cambió el canal.

**Lo que revisó y está bien**: `Program.cs` l.173 sostiene el principio de `ADR-10007`; `Api ADR-00008` (despliegue conjunto) queda reforzado; la ausencia de `depends_on` respeta `ADR-10002`/`ADR-10005`.

### 4.5 Operación y entrega — 5 hallazgos

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-DEVOPS-01` | La 09 de Web describe como vigente la publicación retirada, y `Pipeline-CI-CD.md` **3.8 se emitió el 2026-09-12** sin tocar la topología | E1 | **P0** |
| `RD-DEVOPS-02` | El flujo se dispara sobre **exactamente las rutas** que tocó el PR #187: el mismo `main` se publicó dos veces, a dos topologías, el mismo día | **E1** | **P0** |
| `RD-DEVOPS-03` | `deploy/compose.yaml` **ya declara** que el intake §16, `Pipeline-Producto.md` §4 y `Api/Entornos-Deploy.md` §3 lo describen mal, y que la corrección está elevada al Product Owner y sin resolver | E1 | P1 |
| `RD-DEVOPS-04` | `Estrategia-Versionado.md` §12 declara que la regla de etiquetar «rige desde la fase `i` en adelante»; `git tag` corta en `v0.8.0` y ningún `.csproj` declara `<Version>` | E1 | P1 |
| `RD-DEVOPS-05` | La matriz de publicación de `Pipeline-Producto.md` §3 sigue declarando el canal FTP para Web y no tiene fila para el canal por imagen | E1 | P1 |

**Respuestas al mandato**: `Rules-Devops.md` §0 y §4.4 **sí** obligan a que `Entornos-Deploy.md` de cada unidad liste los canales vigentes. `deploy/compose.yaml` y el compose real **no son redundantes**: el primero verifica `PT-04` y es del fuente, el segundo despliega y es del proyecto de contenedor; lo que falta es que la 09 lo diga.

**Lo que revisó y está bien**: la separación fuente/host está bien razonada; el sellado por `SourceRevisionId` es sólido en los dos `Dockerfile`.

### 4.6 Seguridad — 5 hallazgos

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-SEG-01` | El propio despliegue verifica `GET /salud` público con certificado válido, contra un intake que declara «el navegador nunca la alcanza» y «CORS no hace falta»; el compose no declara `Cors` ni `RateLimit` | **E1** | **P0** |
| `RD-SEG-02` | El punto de canje de credenciales queda alcanzable desde Internet sin límite de tasa; el diseño asumía un consumidor único y confiable | E1/E2 | P1 |
| `RD-SEG-03` | Cinco secretos de FTP vivos y un flujo armado escribiendo en un hosting declarado retirado: superficie de compromiso sin dueño | E1 | **P0** |
| `RD-SEG-04` | Ningún documento declara ciclo de vida ni verificación del certificado del hosting; el flujo comprueba código 200, no vigencia | **C** | — |
| `RD-SEG-05` | El bind mount de claves coincide con el código, pero `DESPLIEGUE.md` §Respaldos dice «el front no guarda nada que no se pueda regenerar» y perder ese volumen reproduce un incidente ya documentado | E1 | P1 |

**Lo que revisó y está bien**: la guarda `:?` de `API_BASE_URL`; la coincidencia claves/compose/`Status.razor`; la superposición de transición está declarada como temporal con pasos de retiro.

### 4.7 Formal — 5 hallazgos

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-FOR-01` | `PT-05` **no tiene umbral numérico ni instrumento** en ningún documento vivo, y la categoría 08 de las dos unidades ni la cita. Contraejemplo: `PT-01` sí los tiene | E1 | P1 |
| `RD-FOR-02` | «Desde la red de la facultad» **ya no discrimina el riesgo que la motivó**: con dominio público, medir desde la facultad da lo mismo que desde cualquier lado | E3 | P1 |
| `RD-FOR-03` | `I-1` es **indecidible**: hay dos direcciones candidatas y el guion acepta cualquiera que responda 200 | E4 | P1 |
| `RD-FOR-04` | Recuento vivo: **118 = 87 + 25 + 6**, no 23 vigentes. Y las categorías «Vencido» y «Sin evento» del informe del 2026-08-27 (20 filas) **desaparecieron del vocabulario vivo**: 6 → `NO APLICA`, 13 → `Vigente`, 1 → `Cerrado` | E1 | P2 |
| `RD-FOR-05` | `Vista-Producto.md` §1.1 declara **50** ADR y el árbol tiene **54**; faltan `ADR-14003`, `14004`, `14005` y `ADR-10008` | E1 | P1 |

**Clasificación de los siete criterios**: `I-1` enumerable-automatizado **con término indecidable**; `I-2`, `I-3` enumerables-automatizados; `I-4` **interpretativo** (el guion sólo verifica que el registro exista y no diga `SIN MEDIR`); `I-5` enumerable-manual; `I-6` automatizado por delegación en `I-2`; `I-7` enumerable-manual (sólo comprueba que los ocho guiones existan y sean ejecutables).

**Lo que revisó y está bien**: `Matriz-Sensado-Deriva.md` §4.1 cierra exacto en 62 filas; los 35 `TC-` coinciden; los nueve `Sample.*.csproj`, `E2ETests` y el nodo del visor están en el agrupador y `Plan-Etapa-A.md` 1.12 lo anota el mismo día.

### 4.8 Trazabilidad documental — 7 hallazgos

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-TRZ-01` | `README.md` §7 publica cinco estados envejecidos y **ningún responsable está nombrado** (`Master-Prompt-Reanudacion.md` §1.1 R1 incumplido) | E1 | P1 |
| `RD-TRZ-02` | La regla de la cabecera del changelog se incumplió por tercera vez; el «equipo de desarrollo» **sí** resuelve (intake §2: un docente más un agente). El hueco es que #186 no fue «etapa» | E2 | P1 |
| `RD-TRZ-03` | El roadmap declara quién cierra la fase `i` sólo por la regla común §5.1; la fila `i` de §5.2 ya no es verificable | E1 | P1 |
| `RD-TRZ-04` | Tras el PR #187 quedan falsas: `README.md` raíz l.70 («el visor no tiene solución de código»), la cabecera de `Vista-Producto.md` (fecha y manifiesto), `Plan-Etapa-A.md` l.612. **«Siete proyectos de código» sigue siendo verdadero** | E2 | P2 |
| `RD-TRZ-05` | La «pasada de diseño» quedó vigente en **quince** lugares: doce contratos con `evidencia: No verificado — sin código`, `Vista-Producto.md` l.79 y los dos README de la categoría 10 | E2 | P1 |
| `RD-TRZ-06` | De las 22 filas vigentes, **21 nombran el mismo evento** (la fase `i`), no ocho; y ocho usan la forma «punto de control», que `Root-Rules.md` §12.2 pone como ejemplo de lo que **no** es un evento abrible | E2 | P1 |
| `RD-TRZ-07` | **214** líneas con «hosting» en 47 archivos y **42** con «FTP» en 15; **25 afirman hoy en presente algo falso**. Ningún documento vivo describe la topología de producción | E2 | **P0** |

**Lo que revisó y está bien**: el changelog nombra responsable y verificador, y su entrada del 2026-09-12 cumple la regla; las 118 filas tienen los cuatro campos y ninguna usa «hosting» o «FTP» como evento; el PR #187 sí recalculó `Plan-Etapa-A.md` l.279 y `Vista-Producto.md` l.262.

### 4.9 Refutador — 6 hallazgos

| Id | Contra-argumento | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-REF-01` | **La disyuntiva del contrato es falsa.** La etapa `i` ni «no ocurrió» ni «ocurrió con otra topología»: **está abierta desde el 2026-08-27** y nunca cerró | **E1** | **P0** |
| `RD-REF-02` | La mayor parte de la lista de trabajo **no** es escalada: `ADR-14003`, `ADR-10007`, la 09 de Web, `Pipeline-Producto.md`, el README y el changelog son corpus, cerrables por la mesa. Sólo `X-10` y `F-14` tocan el intake | E1 | P1 |
| `RD-REF-03` | Operación se equivoca sobre `ADR-14003`: **«superado» ni siquiera es un valor válido** del conjunto cerrado, el campo 5 dice `vigente`, la cabecera `Aceptado`, y **ninguna** de las tres entradas de su control de cambios nombra el 2026-09-06 | **E1** | **P0** |
| `RD-REF-04` | `RD-SEG-01`/`RD-SEG-03` identifican hechos reales pero prescriben el remedio: `Mesa-Rules.md` §7 disparador **5** los saca de la potestad de la mesa | E1 | P1 |
| `RD-REF-05` | `RD-DEVOPS-04` extiende una regla de «etapa cerrada» a PR que el roadmap no tipifica como etapa: fijar la versión sería una decisión de política nueva | E1 | P1 |
| `RD-REF-06` | `RD-LEC-03` pide que el corpus reproduzca conocimiento de host que la regla rectora del ecosistema reparte al otro repositorio | E1 | P1 |

**Lo que del panel considera correcto**: `DIV-02`, `DIV-03` y `DIV-04` confirmadas con evidencia exacta; el campo 5 de `ADR-14003` dice `vigente` contra lo que sostuvo Operación; el flujo se disparó de nuevo el 2026-09-12.

### 4.10 Hallazgos de la presidencia (`AG-00970`), al consolidar

| Id | Afirmación | Ancla | Nivel |
| --- | --- | --- | --- |
| `RD-PRES-01` | **Colisión de vocabulario entre «Fase I» y «etapa `i`».** `git log` tiene **22** ramas `codigo/fase-i-*` que son la **Fase I del framework** (la pasada de ejecución de la categoría 10, cuyo informe es `I-1-10-Examples-…-r1.md`), no la **etapa `i` del roadmap**. El refutador las leyó como lo segundo. `Vocabulario-Rules.md` §9 obliga a desambiguar una palabra con más de un sentido en el mismo ámbito | E1 | P2 |
| `RD-PRES-02` | **La etapa `i` arrancó el 2026-08-27 y el corpus lo registra**: `Estado-Del-Destino-2026-08-27.md` §10.3 declara la salida `D` **arrancada**, con «Etapa siguiente: Fase `i` · Despliegue real» y su entregable escrito: «Front publicado por FTP en el hosting». El defecto no es que el despliegue esté fuera del roadmap: es que **el entregable declarado de una etapa abierta cambió y el documento no** | E1 | **P0** |
| `RD-PRES-03` | Diez enlaces internos no resuelven, todos en `Audit/`; seis son literales de plantilla y cuatro son rutas relativas viejas a `Estrategia-Testing.md` y `Casos-Prueba-Referenciales.md` | E1 | P3 |

---

## 5. Consolidación: contradicciones entre especialistas

`Mesa-Rules.md` §6.3: una contradicción entre dos miradas independientes es, por sí sola, evidencia `E2` de que el texto admite dos lecturas. Se elevan como ítem separado.

| Id | Entre quiénes | Sobre qué | Resolución del jurado |
| --- | --- | --- | --- |
| **X-1** | Operación (`RD-DEVOPS-04`, nota final) contra Arquitectura (`RD-ARQ-02`), Trazabilidad (`RD-TRZ-07`) y Requisitos (`RD-REQ-06`) | Si `ADR-14003` está «correctamente declarado como superado» o sigue `vigente` | **Resuelta contra Operación**, por `RD-REF-03` con cita textual: el campo 5 dice `vigente`, la cabecera `Aceptado`, «superado» no es un valor del conjunto y ninguna entrada del control de cambios nombra el 2026-09-06 |
| **X-2** | Verificación (`RD-VER-01`: «la fase `i` no ocurrió») contra el resto (que la daba por ocurrida de hecho) | Si la etapa `i` ocurrió | **Ninguna de las dos**: `RD-REF-01` y `RD-PRES-02` establecen que está **abierta** desde el 2026-08-27. Las dos lecturas eran ciertas en su mitad y falsas en la otra |
| **X-3** | Operación (`RD-DEVOPS-04`) contra el Refutador (`RD-REF-05`) | Si corresponde etiquetar `v0.9.0` retroactivamente | **A escalada**: el hecho (dos despliegues sin etiqueta, regla que dice regir desde la fase `i`) es real; **qué cuenta como evento de etiquetado** es decisión del Product Owner |
| **X-4** | Lector (`RD-LEC-03`) contra el Refutador (`RD-REF-06`) | Si el corpus debe documentar la topología de host | **Parcial para los dos**: el corpus no reproduce red, IP ni secretos —eso es del otro repositorio—, pero **sí** debe dejar de afirmar un canal retirado como vigente y **sí** debe nombrar dónde vive la constancia del despliegue |

---

## 6. Veredictos del jurado (`Mesa-Rules.md` §6.4)

Cinco jueces con función objetivo diferenciada. Voto por hallazgo, con fundamento. Los hallazgos se agruparon por raíz común; el voto se aplica al grupo.

| Grupo | Hallazgos | Resultado | Conteo | Fundamento por juez |
| --- | --- | --- | --- | --- |
| **G-01 · La etapa `i` está abierta y su entregable cambió** | `RD-PRES-02`, `RD-REF-01`, `RD-REQ-01`, `RD-VER-01`, `RD-TRZ-03`, `RD-LEC-05` | **PROCEDE** | 5-0 | *Evidencia*: `E1` sobre `git log` y sobre el informe del 2026-08-27. *Impacto*: 21 ítems diferidos cuelgan de ese evento. *Costo*: reescribir dos secciones cuesta menos que una etapa mal cerrada. *Coherencia*: no reabre la salida `D`, la continúa. *Riesgo*: reversible |
| **G-02 · El canal de publicación vigente no está en ningún documento** | `RD-TRZ-07`, `RD-LEC-03`, `RD-LEC-04`, `RD-VER-04`, `RD-DEVOPS-01`, `RD-DEVOPS-05`, `RD-REQ-06`, `RD-REQ-07`, `RD-ARQ-02`, `RD-ARQ-03` | **PROCEDE**, acotado por `RD-REF-06` | 5-0 | *Evidencia*: 25 líneas que afirman en presente algo falso. *Impacto*: un agente publica hoy por el canal retirado. *Costo*: el parche es en la 09 y en `Producto/Adrs`, no en los 25 lugares. *Coherencia*: no reabre la decisión del PO, la registra. *Riesgo*: reversible |
| **G-03 · `ADR-14003` y `ADR-10007`** | `RD-ARQ-02`, `RD-ARQ-03`, `RD-ARQ-05`, `RD-ARQ-07`, `RD-REF-03` | **PROCEDE** | 5-0 | *Evidencia*: `E1` textual. *Impacto*: el contador de apartamientos corre sobre una decisión sin objeto. *Costo*: bajo. *Coherencia*: `ADR-10007` es inmutable, se supera y no se edita. *Riesgo*: el estado de `ADR-14003` **no** pasa a `retirado` hasta que los tres pasos de retiro ocurran |
| **G-04 · Fuentes declarativas envejecidas** | `RD-LEC-02`, `RD-VER-03`, `RD-TRZ-01`, `RD-TRZ-04`, `RD-TRZ-05`, `RD-FOR-05`, `RD-LEC-01`, `RD-LEC-07` | **PROCEDE** | 5-0 | *Evidencia*: cada afirmación contra su instrumento. *Impacto*: es el defecto que originó el prompt de reanudación. *Costo*: bajo. *Coherencia*: `Root-Rules.md` §10 lo prohíbe. *Riesgo*: nulo |
| **G-05 · El changelog sin el PR #186** | `RD-LEC-06`, `RD-VER-02`, `RD-TRZ-02` | **PROCEDE** | 5-0 | *Evidencia*: `git show --stat`. *Impacto*: tercera reincidencia del patrón `D-01`. *Costo*: una entrada. *Coherencia*: la regla está en la cabecera del propio documento. *Riesgo*: nulo |
| **G-06 · Ítems diferidos vencidos y mal contados** | `RD-VER-05`, `RD-TRZ-06`, `RD-FOR-04` | **PROCEDE** | 4-1 | *Evidencia*: recuento sobre el instrumento. *Impacto*: el producto corre con vigencia de credencial, versión e inventario decididos por omisión. *Costo*: un barrido. *Coherencia*: ⚠ el voto en contra sostiene que **depende de G-01**: si la etapa `i` sigue abierta, los 13 no vencieron. **Se acepta la objeción**: el barrido se hace **después** de cerrar G-01, y la mesa entrega las dos lecturas. *Riesgo*: bajo |
| **G-07 · La puerta de la etapa `i` mide el canal retirado** | `RD-REQ-02`, `RD-VER-06`, `RD-FOR-01`, `RD-FOR-02`, `RD-FOR-03`, `RD-ARQ-06` | **PROCEDE** | 5-0 | *Evidencia*: `I-1` reproducido en verde contra la dirección real. *Impacto*: la etapa se podría cerrar con un criterio que no midió nada. *Costo*: reescribir un criterio y su guion. *Coherencia*: el guion es fiel al roadmap; el parche va en el roadmap. *Riesgo*: bajo |
| **G-08 · Claves de protección de datos sin respaldo ni requisito** | `RD-SEG-05`, `RD-REQ-05` | **PROCEDE** | 5-0 | *Evidencia*: `E1` sobre el compose y el código. *Impacto*: perder el volumen expulsa a la comisión entera, y ya pasó una vez. *Costo*: bajo. *Coherencia*: no reabre nada. *Riesgo*: **el juez de riesgo pide que el parche declare, no que ejecute respaldos** |
| **G-09 · La API quedó pública** | `RD-SEG-01`, `RD-SEG-02`, `RD-ARQ-04`, `RD-REQ-03`, `RD-REQ-04` | **ESCALADA** | 5-0 | `Mesa-Rules.md` §7 disparadores **3** (cambio de alcance: `X-10` es una exclusión vigente que se realizó) y **5** (consecuencia externa: datos de alumnos). `RD-REF-04` tiene razón: la mesa no prescribe el remedio |
| **G-10 · Secretos de FTP vivos y doble publicación** | `RD-SEG-03`, `RD-DEVOPS-02` | **ESCALADA**, con parche de corpus aparte | 5-0 | Disparador **5** (obligaciones con un tercero: el hosting) e **irreversibilidad acotada** (revocar credenciales). Lo que **sí** es de la mesa: que la 09 declare el período de doble publicación |
| **G-11 · Etiquetas de versión** | `RD-DEVOPS-04`, `RD-REF-05` | **ESCALADA** (`X-3`) | 5-0 | El hecho es real; el criterio de etiquetado para trabajo post-`h` es decisión del Product Owner |
| **G-12 · Enlaces rotos y colisión de vocabulario** | `RD-PRES-03`, `RD-PRES-01` | **PROCEDE** | 5-0 | Enumerables de la compuerta; costo trivial |
| **G-13 · `deploy/compose.yaml` ya declaró la divergencia y nadie la resolvió** | `RD-DEVOPS-03` | **PROCEDE** como parte de G-02 | 5-0 | Es la misma raíz, con la particularidad de que el código lo dijo primero |
| `RD-SEG-04` | certificado del hosting | **DESCARTADO por nivel C** | 5-0 | Conjetura sin ancla propia; se registra como descartado (`Mesa-Rules.md` §6.1) |

**Homogeneidad**: 12 de 14 ítems votados 5-0 (**86 %**, por encima del 80 % de `Mesa-Rules.md` §6.4). El ciclo se marca **sospechoso de homogeneidad** y el refutador revisó los `NO_PROCEDE` y el descarte: sostuvo el descarte de `RD-SEG-04` y no encontró ningún `NO_PROCEDE` mal fundado —de hecho **no hubo ninguno**: todo lo que no procede se fue a escalada, que es una salida distinta—. Se declara.

---

## 7. Parches aprobados (`Mesa-Rules.md` §6.5)

Diseñados por el cuerpo auditor. **Ninguno se aplica en esta fase** (`Master-Prompt-Reanudacion.md` §3.1.3): son la lista de trabajo de la salida que el Product Owner elija.

| Id | Capa de origen | Qué cambia | Verificación | Derivados a revalidar |
| --- | --- | --- | --- | --- |
| **P-01** | `SDD/Docs/00-Contexto/Roadmap-Producto.md` §2.1 y §5.2 | La fila `i` deja de nombrar el mecanismo de transporte y enuncia el resultado; el criterio `I-1` pasa a «el front corre desde su imagen en el servidor propio, sellado con la revisión, y su publicación es reproducible». Se registra que la etapa **está abierta desde el 2026-08-27** y que el despliegue del 2026-09-06 es un hecho suyo | `grep -c "FTP" Roadmap-Producto.md` = 0 fuera del control de cambios; `verify-stage-i.sh` corrido contra la dirección real **no** aprueba `I-1` por la existencia del flujo | `scripts/verify-stage-i.sh`, `SDD/Docs/README.md` §7, las 21 filas de ítems diferidos |
| **P-02** | `scripts/verify-stage-i.sh` | `I-1` mide el canal vigente; `API_URL` se desdobla en dirección interna y pública | Dos corridas: contra el despliegue real da `I-1` conforme; contra el hosting retirado, no | `Roadmap-Producto.md` §5.2 |
| **P-03** | `SDD/Docs/Producto/Adrs/` — **ADR nuevo de nivel Producto** | Declara la topología vigente: dos contenedores en el servidor propio, red compartida, resolución por nombre, publicación por túnel con dominio propio, qué se publica y para quién, y qué premisa reemplaza a `RN-B1`. Supera a `ADR-14003` | El ADR existe, está indexado en `Vista-Producto.md` §5 y ningún documento de la 09 contradice su topología | `Vista-Producto.md` §5 y §6, `Api/05` §5.1, intake §14 (escalada `E-01`) |
| **P-04** | `ADR-14003` §6 | Entrada 1.4: el disparador efectivo **no fue** ninguno de los dos del campo 4; el campo 5 pasa a `retirado` **sólo cuando** se cumplan los tres pasos de retiro, y hasta entonces se registra el período de doble publicación | El ADR nombra el 2026-09-06 y el ADR que lo supera | `Medicion-PT-05.md`, `Pipeline-CI-CD.md` (Web) |
| **P-05** | `Web/05-Arquitectura-Tecnica/Adrs/` — **ADR-10009** | Hereda el principio de `ADR-10007` (dirección por configuración, nunca embebida) y reescribe mecanismo, reversión y `R-03`; decide si la comprobación final de la dirección pública se recupera. `ADR-10007` pasa a `Superado por ADR-10009` | `ADR-10007` §3 con un estado del conjunto cerrado; la métrica «0 flujos sin comprobación final» o se cumple o se declara retirada | `Web/09 Entornos-Deploy.md`, `Pipeline-CI-CD.md`, `Vista-Producto.md` §5 |
| **P-06** | `Web/09-Devops/Entornos-Deploy.md`, `Pipeline-CI-CD.md`, `Guia-Publicacion-Front-Ftp.md`; `Producto/Pipeline-Producto.md` §3 | Declaran **los dos canales** —el vigente por imagen y el retirado por FTP, con su fecha y su decisión— y la matriz de publicación suma la fila del canal por imagen para Web. Nombran dónde vive la constancia del despliegue **sin reproducir** red, IP ni secretos (`RD-REF-06`) | `Rules-Devops.md` §4.4: cada canal con propósito, dirección y aprobador; `grep` de «hosting» en presente = 0 | `Api/09 Entornos-Deploy.md` §3, `deploy/compose.yaml` |
| **P-07** | `changelog.md` | Entrada **repuesta y marcada como repuesta** para el PR #186, escrita desde `6fa6844` y del archivo; corrige l.894 | `grep "#186"` resuelve; la entrada nombra el retiro del hosting y lo que quedó vivo | `SDD/Docs/README.md` §7 |
| **P-08** | `SDD/Docs/README.md` §7 y cabecera | Deja de enunciar estado y remite a sus fuentes; **nombra a su responsable**; corrige las seis afirmaciones | Ninguna cifra de estado sin fuente citada; cabecera con manifiesto 6.0 e intake 4.1 | — |
| **P-09** | `Vista-Producto.md` §1.1 y §5; `Plan-Etapa-A.md` l.612; `README.md` raíz | Recuento de ADR sobre el árbol (54); §5 indexa `Producto/Adrs/`; el README raíz deja de decir que el visor no está en la solución y lleva al índice | Cada recuento reproducible con un comando | — |
| **P-10** | Los doce contratos de la categoría 10 con `evidencia: No verificado — sin código`; `Vista-Producto.md` l.79; los dos README de la 10 | Registran su corrida con fecha y salida; la «pasada de diseño» deja de enunciarse como presente | `grep -c "No verificado — sin código"` = 0 para los samples que corren | `Matriz-Sensado-Deriva.md` |
| **P-11** | Las 21 filas de ítems diferidos cuyo evento es la fase `i`, más `PD-05` | **Después de P-01**: se reabren con el hecho que las venció citado, o se re-anclan a un evento abrible. `PD-05` se cierra: la 05 emitió 3.1 a 3.9 | Ninguna fila vigente con evento ya ocurrido; ninguna con la forma «punto de control» sin artefacto | `Estado-Del-Destino` siguiente |
| **P-12** | `Contrato-Agentes.md` §7 y `AGENTS.md` | Cada fila cuyo destino esté `Planificado` lo dice y ofrece el sustituto vigente | Los cuatro enlaces resuelven o declaran su estado | — |
| **P-13** | `Audit/E-08-Calidad-…-r2.md`, `Mesa-2026-08-31-B.md` | Cuatro rutas relativas recalculadas | La compuerta de enlaces da 0 rotos fuera de los literales de plantilla | — |
| **P-14** | `Vocabulario-Rules.md` §9 aplicado al destino: `Roadmap-Producto.md`, `changelog.md` | Desambiguar «Fase I» (framework) de «etapa `i`» (roadmap) allí donde conviven | Ninguna sección usa las dos formas sin calificarlas | — |

**Parches fusionados**: `RD-DEVOPS-03` entra en P-06 (misma sección); `RD-LEC-01` y `RD-TRZ-04` entran en P-09 (mismo documento).

---

## 8. Deuda declarada (`Root-Rules.md` §12.2)

| Qué se decidió no corregir | Motivo | Evento de cierre |
| --- | --- | --- |
| El estado del campo 5 de `ADR-14003` no pasa a `retirado` hoy | Los tres pasos de retiro de `DESPLIEGUE.md` no ocurrieron: el front de somee sigue activo, la superposición de transición sigue puesta y la regla 10 del router sigue abierta | Los tres pasos cumplidos, con su constancia |
| El barrido de las 21 filas de ítems diferidos | Depende de `P-01`: si la etapa `i` sigue abierta con criterios nuevos, los 13 «vencidos» no vencieron | `P-01` aplicado |
| `RD-SEG-04`, el certificado del hosting | Nivel `C` sin ancla propia; **descartado y registrado** | Si asciende de nivel en el ciclo siguiente |
| Reproducir la topología de host en el corpus | La regla rectora del ecosistema la reparte al proyecto de contenedor (`RD-REF-06`) | Ninguno: es decisión de diseño, no deuda |
| La ausencia de límite de tasa en la API | Es escalada `E-02`, no parche | Respuesta del Product Owner |

---

## 9. Escaladas (`Mesa-Rules.md` §7)

Cinco, agrupadas, con default declarado. Ninguna es de tipo 2 o 3 bloqueante salvo `E-01`, que se marca como tal por cambio de alcance.

### E-01 · La etapa `i`: qué es su entregable ahora — **disparador 3, cambio de alcance**

**Qué pasó.** La etapa `i` está abierta desde el 2026-08-27 con el entregable «front publicado por FTP en el hosting, servicio de datos en el servidor propio». El 2026-09-06 desplegaste las dos piezas en contenedores del servidor propio y retiraste el hosting. El intake `F-14` y el roadmap siguen declarando el entregable viejo, y **el intake se reemitió dos veces el 2026-09-12 sin absorberlo**.

| Opción | Consecuencia |
| --- | --- |
| **A** · `F-14` y el roadmap se reescriben en términos del resultado; la etapa `i` sigue abierta y lo que falta es `PT-05`, `I-5` e `I-7` | Una escritura controlada sobre el intake (major) y el roadmap re-derivado. Las 21 filas de ítems diferidos conservan su evento |
| **B** · La etapa `i` se declara cerrada con lo hecho y `PT-05` pasa a `j…` | Contradice `Roadmap-Producto.md` §2.2 («`PT-05` es lo único que esa fase hace») |
| **C** · Se deja como está | El corpus sigue describiendo un entregable retirado y 21 ítems cuelgan de un evento inalcanzable |

**Recomendación de la mesa: A** (5-0). **Si no respondés**: la mesa aplica A como supuesto declarado en el informe de estado, **no** toca el intake —es documento humano— y deja el roadmap sin parchear; queda bloqueado P-01 y, con él, P-02 y P-11.

### E-02 · La API quedó públicamente alcanzable — **disparador 5, consecuencia externa**

**Qué pasó.** `api-geometria.aplicada.stream` responde desde Internet y el proyecto de contenedor la rotula «la API que consumen los alumnos». El intake declara «únicamente `Web`, servidor a servidor; el navegador nunca la alcanza» y «CORS no hace falta». De los dieciséis puntos, cinco responden sin credencial —incluido el canje— y no hay límite de tasa declarado. La exclusión `X-10`, que preveía exactamente esto, sigue **vigente** y su §22 la registra como incógnita **viva**.

| Opción | Consecuencia |
| --- | --- |
| **A** · La API se reserva al front: el túnel deja de publicarla | `RA-01` vuelve a ser estructural; hay que verificar que nada externo dependa de esa dirección |
| **B** · Se declara pública con propósito: `X-10` se cierra como realizada, se declara el consumidor y se decide sobre límite de tasa y CORS | El alcance del producto crece; es tu decisión, no del método |
| **C** · Se deja sin declarar | El corpus afirma una propiedad que el despliegue contradice |

**Recomendación: B si los alumnos efectivamente consumen la API; A si no** (5-0 en que la mesa no puede elegir). **Si no respondés**: la mesa registra el hecho en el informe de estado como divergencia abierta y **no** propone parche sobre la exposición.

### E-03 · Los secretos de FTP y el flujo que sigue publicando — **disparador 5, obligaciones con un tercero**

**Qué pasó.** `deploy-front-ftp.yml` se dispara con cada fusión que toque el front, el visor o los contratos —lo hizo el 2026-09-12— y escribe con cinco secretos vivos en un hosting que declaraste retirado.

| Opción | Consecuencia |
| --- | --- |
| **A** · Retirar el flujo y revocar los secretos ahora | Corta el front de somee, que `DESPLEGUE.md` dice que todavía alguien podría estar usando |
| **B** · Retirarlo cuando se cumplan los tres pasos de retiro, y mientras tanto declarar el período de doble publicación en la 09 | Es lo que el propio `DESPLIEGUE.md` planificó |
| **C** · Dejarlo | Credenciales de escritura sobre un tercero, sin dueño que las mire |

**Recomendación: B** (5-0). **Si no respondés**: la mesa deja el parche de corpus (declarar el período de doble publicación) en la lista y **no** toca el flujo ni los secretos.

### E-04 · Qué evento dispara una etiqueta de versión — **disparador 1, ambigüedad de intención**

**Qué pasó.** `Estrategia-Versionado.md` dice que la regla de etiquetar «rige desde la fase `i` en adelante», `git tag` corta en `v0.8.0`, hubo dos despliegues reales sin etiqueta y `/salud` informa `1.0.0+<sha>`, que es el valor por omisión del SDK. Operación propone `v0.9.0` retroactiva; el refutador sostiene que la regla habla de **etapa cerrada** y ninguno de los dos PR lo es.

**Recomendación: definir qué cuenta como evento de etiquetado para trabajo posterior a `h`** y, con eso, etiquetar o no. **Si no respondés**: nada se etiqueta y la fila queda como deuda con evento «cierre de la etapa `i`».

### E-05 · El estado durable del front — **disparador 4, irreversibilidad acotada**

**Qué pasó.** Las claves de protección de datos viven en un bind mount y `DESPLIEGUE.md` §Respaldos dice que «el front no guarda nada que no se pueda regenerar». Perder ese directorio expulsa a la comisión entera en medio de una clase, y el producto ya pagó ese costo una vez.

**Recomendación: declarar el requisito** («una reconstrucción de la imagen no invalida las sesiones abiertas») y decidir si las claves se respaldan o si se acepta el riesgo con constancia. **Si no respondés**: la mesa deja el requisito escrito en el informe de estado como deuda con evento «cierre de la etapa `i`», sin tocar el procedimiento de respaldo.

---

## 10. Bloque de cierre (`Mesa-Rules.md` §6.7)

```text
CIERRE DE MESA — Fábrica de Geometría (Lab-Geometria), 2026-09-12

  PANEL
    Convocados:     requisitos: 7 · verificacion: 6 · lector-sin-contexto: 7 ·
                    arquitectura: 7 · operacion-y-entrega: 5 · seguridad: 4 ·
                    formal: 5 · trazabilidad-documental: 7 · refutador: 6 ·
                    presidencia (consolidación): 3
    Descartados:    datos-y-dominio: sin cambio de entidades ni persistencia ·
                    interfaz-externa: Contratos-REST sin cambios ·
                    concurrencia, rendimiento, cumplimiento, uso: sin señal
    Ad hoc:         ninguno; el catálogo cubrió las nueve señales
    Postergados:    ninguno (cinco variables, exactamente el techo)
    Aporte nulo:    ninguno; los nueve produjeron hallazgos procedentes

  HALLAZGOS
    Detectados: 55   Procedentes: 42   Con parche: 14   En escalada: 12   Descartados por C: 1

  COMPUERTA (§10.0)
    Resultado:      10 enlaces no resuelven sobre 5.788 (6 son literales de plantilla);
                    118 ítems diferidos con sus cuatro campos; sin colisión de familias
    No mirado:      el contenido de los documentos (es del panel); _legacy/; samples/ y evidencia/

  ENTREGA AL ORQUESTADOR
    Parches:            14, en roadmap · guion de puerta · Producto/Adrs · Web/05 Adrs ·
                        Web/09 · Pipeline-Producto · changelog · README ×2 ·
                        Vista-Producto · categoría 10 · ítems diferidos · AGENTS · Audit
    Deuda declarada:    5, con su evento de cierre
    Capas a revalidar:  intake §14 y F-14 (escalada E-01) · Api/05 §5.1 ·
                        Matriz-Sensado-Deriva · Medicion-PT-05
    Escaladas:          5, agrupadas, con default declarado

  CIERRE
    Por criterio §10.1: NO. Cerró en UNA ronda, por decisión, con lo que queda abierto
    declarado: las cinco escaladas y la deuda de §8. El ciclo se marca SOSPECHOSO DE
    HOMOGENEIDAD (86 % de votos 5-0) y el refutador revisó los descartes.
```

**Por qué cerró en una ronda y no por criterio.** `Master-Prompt.md` §10.1 pide dos rondas seguidas sin hallazgos interpretativos. Esta mesa corrió **una**, y lo declara: la segunda ronda no cambiaría la lista de trabajo porque **doce de los cincuenta y cinco hallazgos son escaladas** y ninguna se resuelve leyendo más el árbol. `Mesa-Rules.md` §6.7 lo admite con una condición que se cumple acá: *«cerrar por decisión es una salida legítima; lo que no es legítimo es cerrar por decisión sin declararlo»*.

---

---

## 12. Ciclo 2 — el testimonio del Product Owner, y lo que cambia

**Convocado por**: la respuesta del Product Owner a las cinco escaladas del ciclo 1, que es **evidencia
nueva de nivel E4** —declaración del responsable del producto— y cambia la naturaleza del caso.

**Lo que el testimonio establece, y deja de discutirse:**

1. **El cambio de topología no es deriva: es una alternativa ya evaluada que se realizó.** El intake
   había declarado `X-10` con su condición exacta —«reevaluar si aparece un dominio propio»—, apareció
   el dominio, y el túnel **satisface mejor la razón que originó la partición**.
2. **`deploy-front-ftp.yml` está vivo por decisión**, como canal alternativo y antecedente de
   despliegue. **El defecto no es que exista: es que ningún documento declara que existe por eso.**
3. **El planteo de fondo del Product Owner**: el framework **no es cíclico**; cuando el producto
   evoluciona, esa evolución no se registra como un ciclo nuevo de backlog funcional, técnico y de
   sprint.

**Panel del ciclo 2**: tres agentes ad hoc con carta de mandato —**AH-001** metodologías ágiles y
evolución de producto, **AH-002** gestión de proyecto y ciclo de vida, **AH-003** normativa del ciclo—
más el refutador. **26 hallazgos**, familia `RE-`.

### 12.1 Lo que encontraron

| Id | Hallazgo | Ancla | Nivel |
|---|---|---|---|
| `RE-AGIL-01` | `Master-Prompt.md` §12 dice «el handoff no cierra el alcance de SDD», pero las Fases I y J sólo tocan la 10 y la 11: **ninguna reabre 06 ni 07** | E1 | P1 |
| `RE-AGIL-03` | Desde el cierre de `h` los cuatro backlogs y los dos planes **sólo recibieron correcciones administrativas**; **cero altas**. La decisión de retener el FTP no tiene ni una tarea técnica que la sostenga: vive en un testimonio y en un archivo | E2 | P1 |
| `RE-AGIL-06` | **La pieza clave**: `Rules-Backlog-Tecnico.md` §3.4 **ya sabe qué hacer** cuando el alcance cambia —el backlog sube a `v2.0`—. Lo que falta es **el disparador** | E1 | P1 |
| `RE-GP-01` | `X-10` declaró su condición de reevaluación y se cumplió, y **ningún instrumento de gestión registró la reevaluación** antes de ejecutar el cambio | E1 | P1 |
| `RE-GP-02` | Las dos decisiones del Product Owner quedaron asentadas **en el repositorio del framework**, no en el registro de decisiones del destino | E1 | P0 |
| `RE-GP-04` | El instrumento que planificó la reestructuración escribió «no se fusionó… el paso 0 reescribe el intake» y **el PR se fusionó sin que el paso 0 se ejecutara** | E1 | P0 |
| `RE-GP-07` | **El riesgo ya se materializó**: la decisión «canal alternativo, no productivo» **no se tradujo a un control ejecutable**, y por eso la doble publicación ocurrió | E1 | P0 |
| `RE-NORM-01` | Las Fases I y J son un ciclo **de documentación**, no del producto: sus siete actividades no invocan ninguna regla de backlog ni emiten trabajo nuevo | E2 | P1 |
| `RE-NORM-03` | Tres cardinalidades declaradas; **no existe «una vez por incremento de alcance»** | C | — |
| `RE-NORM-06` | **La asimetría exacta**: hay mecanismo para cuando avanza el framework (`Migracion-Rules.md`) y **ninguno para cuando avanza el producto** | E2 | P1 |

### 12.2 Lo que el refutador corrigió, y ordena la intervención

| Id | Corrección | Efecto |
|---|---|---|
| `RE-REF-04` | **El disparador no va en el backlog.** `Master-Prompt.md` §13 cierra en **dos** los casos de escritura del intake y ninguno es «el alcance cambió después del handoff». Enganchar el disparador al backlog **invierte la trazabilidad** —el backlog es aguas abajo de la `US`, que lo es del caso de uso, que lo es del intake— y produce un backlog nuevo sobre un intake que sigue afirmando el alcance viejo | **P0.** Cambia dónde nace el parche |
| `RE-REF-05` | **Son tres cosas, no una**: (i) el ciclo evolutivo ausente es de framework; (ii) «la decisión sin control ejecutable» es trabajo de la categoría 09 **del destino** —el framework no distribuye código y está decidido—; (iii) el registro de decisiones en el repositorio equivocado es un error **de esta corrida**, no normativo | P1 |
| `RE-REF-02` | **El eje de extensión ya existe y nadie lo corrió**: `SDD-Development-Guide.md` §III.4 resolvió este mismo tipo de problema al agregar las Fases I y J | P2 |
| `RE-REF-03` | El reporte `21` es **antecedente obligatorio** y hay que citarlo: enuncia en general el patrón de la decisión que barre incompleto | P2 |
| `RE-REF-07` | **Recalibra**: «el método no gobierna el ciclo de construcción» es una **frontera declarada con honestidad**, no un defecto. El hueco no es que no lo gobierne: es que **no declara qué hace cuando ese ciclo le devuelve una decisión que invalida sus propios artefactos** | De P0 a C |

### 12.3 Veredictos del ciclo 2

| Grupo | Resultado | Conteo | Fundamento |
|---|---|---|---|
| **El hueco es real y reportable** | **PROCEDE** | 5-0 | La asimetría está probada con cita literal, y el propio corpus la declara |
| **Dónde nace el parche: `Master-Prompt.md` §13, no `Rules-Backlog-Tecnico.md` §3.4** | **PROCEDE** (corrección del refutador) | 5-0 | Empezar aguas abajo reproduce el defecto un nivel más arriba |
| **Son tres cosas y se reportan por separado** | **PROCEDE** | 5-0 | Tres correcciones incompatibles bajo un título producen una intervención que nadie puede auditar |
| **El testimonio cierra `E-01` y `E-03` del ciclo 1** | **PROCEDE** | 5-0 | La decisión tiene fundamento; lo que falta es asentarla, no revisarla |
| **La reestructuración de la solución es cambio de alcance, no ajuste** | **PROCEDE** | 4-1 | Reordena la composición que el roadmap y las épicas usan como unidad de planificación. El voto en contra sostenía que sin cambio de capacidad no hay cambio de alcance |

### 12.4 Lo que el ciclo 2 entrega

- **Reporte `25`** del framework: `IA.SDD.Documentacion/Reportes/25-El-Producto-Evoluciona-Y-El-Metodo-No-Tiene-Donde-Registrarlo.md`, con su fila en el índice de la serie.
- **Prompt de intervención `03`**: `IA.SDD.Documentacion/PROMPTs/Fixs/03-Fix-Reporte-25/`, con su fila en el README de la serie.
- **Dos parches nuevos para el destino**, que se suman a los catorce de §7:

| Id | Capa | Qué cambia |
|---|---|---|
| **P-15** | `SDD/Docs/Audit/A3-Decisiones-Del-Product-Owner.md` | Suma las dos decisiones del Product Owner —topología del 2026-09-06 y composición del 2026-09-11— **con su fundamento**: la condición de reevaluación de `X-10` se cumplió, y el canal anterior se conserva como alternativa y antecedente. El destino deja de depender de un repositorio ajeno para su propia auditoría |
| **P-16** | `Web/09-Devops/Entornos-Deploy.md` y el flujo | Declara el canal de FTP como **alternativa documentada, no productiva**, y le da el correlato que hoy no tiene: sin disparador automático sobre las rutas del front. Es lo que vuelve la decisión distinguible de un descuido |

- **Tres escaladas del ciclo 1 quedan respondidas por el testimonio** (`E-01`, `E-03` y, parcialmente, `E-02`: la exposición de la API sigue abierta porque el testimonio no la menciona).

### 12.5 Cierre del ciclo 2

```text
CIERRE DE MESA — ciclo 2, 2026-09-12
  PANEL        AH-001 agilidad: 6 · AH-002 gestión: 7 · AH-003 normativa: 6 · refutador: 7
  HALLAZGOS    Detectados: 26   Procedentes: 21   Con parche: 2 (destino) + 1 reporte (framework)
  ENTREGA      Reporte 25 + prompt de intervención 03 + P-15 y P-16
  CIERRE       Por criterio: el ciclo 2 corrió sobre una pregunta acotada y la respondió;
               el refutador corrigió la dirección del parche antes de que se escribiera
```

---

---

## 14. Ciclo 4 — la evaluación del intake en la migración, la correlación y la disciplina de escalada

**Convocado por** tres planteos del Product Owner, que se tratan como tema central:

> 1. «En la fase de reanudación y **en especial en la fase de migración** se debe evaluar **si se requiere reestructurar el intake**. Por eso se debe entender **si hay nuevas funcionalidades y/o decisiones de diseño añadidas durante el ciclo de especificación y/o codificación**.»
> 2. «Si se reestructura y surgen **huecos de especificación**, se debe evaluar dejarlos de tal forma que sean **correlacionables con el ciclo de diseño y/o codeo en su historial**.»
> 3. «Lo que no quiero es que, cuando migre, me llene de preguntas fuera de contexto: **los agentes se encuentran con situaciones que han generado ellos y de golpe me descargan a mí problemas que han generado ellos y que pudieron haber resuelto si el análisis se hubiese hecho sobre todo el conjunto y luego afinando donde corresponda**. Acá es importante **la habilidad de correlacionar**.»

**Panel**: tres agentes ad hoc —**AH-006** migración y reestructuración del intake, **AH-007** correlación con el historial, **AH-008** disciplina de escalada— más el refutador, con el mandato ampliado de **evaluar el plan por su aplicación**. Familia `RG-`. 23 hallazgos.

### 14.1 Lo que estableció cada especialista

| Id | Hallazgo | Ancla | Nivel |
|---|---|---|---|
| `RG-IM-01` | **M2 re-expresa forma, no contenido, y lo declara**: la propuesta de intake de una migración real dice "las secciones §1 a §12 **no cambian, ni de número, ni de nombre, ni de contenido**". Todo el contenido de negocio —priorización, casos límite, exclusiones— queda fuera **por diseño**. No hay que ensanchar M2 | E1 | P1 |
| `RG-IM-02` | El principio de estado objetivo es **ciego por construcción al eje de contenido**: presupone que el documento de origen es la mejor fuente del estado real, y esa presunción no se verifica en ningún paso | E1 | P1 |
| `RG-IM-03` | **Siete migraciones pasaron por este intake y ninguna tenía cómo levantar la pregunta.** El insumo que faltaba cruzar ya existe y está disperso | E1/E4 | P1 |
| `RG-IM-04` | La forma correcta **no es un artefacto ni una fase nueva**: es una **fila-tipo en el plan que M1 ya emite**, con su fuente, su sección candidata y una propuesta — no una pregunta abierta | C | — |
| `RG-AH007-01` | **Sólo 15 de 191 fusiones tocan el registro de cambios (7,9 %)**: es un documento de hitos, no un índice de fusiones | E1 | P1 |
| `RG-AH007-03` | La correlación del caso más difícil **salió bien porque el autor escribió un mensaje de veinticinco líneas**, no porque el método lo exija | E1 | P1 |
| `RG-AH007-05` | **D9 ya sirve sin cambios**: el tipo `ejecucion` cubre los comandos del historial. No hace falta notación nueva | — | — |
| `RG-AH007-06` | **Ningún rol del catálogo cubre la correlación con el historial**: el más cercano declara su alcance como «citas entre documentos del corpus» | E1 | P1 |
| `RG-AH008-01` | **La pregunta previa corta por un solo eje** y ninguno de los siete disparadores pregunta quién generó la situación. Un estado que el propio agente dejó a medias **no tiene respuesta en el árbol por construcción**, y con el criterio literal cae del lado de «se detiene» | E2 | P1 |
| `RG-AH008-03` | **Las dos mediciones que el framework ya tiene son de otras dos causas.** La que el Product Owner describe es **una cuarta, sin medir** | E2 | P2 |
| `RG-AH008-04` | **La mesa mira el conjunto en puntos fijos**; dentro del bucle de fases, cada detención sale en el momento en que se tropieza. El registro agrupa lo que el humano **ya difirió**, no agrupa **antes** de la primera vez que se le presenta | E2 | P1 |

### 14.2 Lo que el refutador corrigió, evaluando el plan por su aplicación

| Id | Corrección | Efecto |
|---|---|---|
| `RG-REF-02` | **El cruce que proponía la parte de migración da CERO filas hoy sobre el caso real**: todos los ADR de producto son anteriores a la última entrada del intake. Comparar contra **una fecha global del documento** no puede detectar un hueco en una sección que ninguna entrada posterior tocó | **P0.** Recalibrar a fecha de último toque **por sección**, no por documento |
| `RG-REF-05` | **El campo de procedencia lo declara el propio agente**, y uno que no se dio cuenta de que generó el problema va a declarar «preexistente» de buena fe. **Pero el framework ya tiene la pieza que lo vuelve verificable**: el archivo del estado previo antes de despachar. La procedencia **se deriva, no se declara**; y ante la duda se declara «propia», por la misma asimetría de costos que el método ya usa para decidir si detiene | **P0.** Cambia el mecanismo de autoinformado a calculado |
| `RG-REF-03` | **No hace falta una figura nueva** para el hueco con evento en el pasado: el propio conjunto normativo ya unificó dos figuras que «son la misma con el evento de cierre en distinto lugar». Alcanza un campo de dirección —pasado o futuro— en la figura existente | P1 |
| `RG-REF-04` | **La condición de disolución del rol nuevo sí es medible**, y con maquinaria que ya existe y que la propuesta no citaba: el contador de dos ciclos y su registro, que ya lleva el presidente de mesa. Reinventarlo sería la segunda fuente que el catálogo prohíbe por nombre | P2 |
| `RG-REF-06` | **La parte de migración se pliega al reporte 25** por el criterio del ciclo 3 —misma corrida **y** artefacto compartido, las dos condiciones—: comparte el artefacto donde el reporte declara que el hueco nace. **Las otras dos no cumplen ninguna de las dos y van separadas** | P1 |
| `RG-REF-01` | Siete artefactos tocados, **ninguno por dos partes a la vez**: no hay colisión textual. Pero hay dependencia: la migración cita por referencia la forma de detención que la otra parte está reescribiendo | P1 |

**Y una corrección sobre el propio reporte 25, que el refutador levantó verificando la fuente.** El
reporte decía que `ADR-08007` «agrega el séptimo miembro del contrato de la fachada». **El ADR decide lo
contrario**: su título dice «no en una séptima función» y su tabla de alternativas descarta esa salida
con fundamento. El hueco existe igual —el intake no tiene ninguna ocurrencia del aviso ni del tipo de
opciones— pero es más fino: lo que no se absorbió es que **el contrato ganó una dirección de vuelta y
subió de versión**. Se corrigió en el reporte `25` **1.2**, y **es la medida de cuán fino tiene que ser
el mecanismo**: un cruce que sólo mire recuentos no lo encuentra.

### 14.3 Veredictos del ciclo 4

| Ítem | Resultado | Conteo | Fundamento |
|---|---|---|---|
| Los tres planteos del Product Owner son huecos reales | **PROCEDE** | 5-0 | Los tres se verificaron con cita; ninguno es una impresión |
| Orden de aplicación **E → C → M** | **PROCEDE** | 5-0 | La migración cita por referencia la forma de detención que la escalada reescribe, y la propia parte de migración se subordina por escrito a la de correlación |
| Procedencia **derivada del estado previo**, no declarada | **PROCEDE** | 5-0 | Es lo que vuelve el requisito verificable en vez de depender de la honestidad del que lo llena |
| Cruce por **sección**, no por fecha global del documento | **PROCEDE** | 5-0 | Cero filas sobre el caso real es la prueba de que la calibración estaba mal |
| **Campo de dirección** en la figura existente, no figura nueva | **PROCEDE** | 4-1 | El voto en contra sostenía que un hueco con evento pasado es otro objeto. La mayoría: el propio conjunto normativo ya resolvió un caso formalmente idéntico unificando |
| **Rol nuevo** para correlación, **sin** rol nuevo para escalada | **PROCEDE** | 5-0 | No es contradicción: son dos capacidades distintas. La correlación no la cubre nadie; el filtro del lote ya es mandato del orquestador y del presidente |
| La parte de migración **se pliega al reporte 25**; las otras dos van **separadas** | **PROCEDE** | 5-0 | Criterio del ciclo 3 aplicado con el AND, no con el OR |

### 14.4 El plan corregido, y qué falla si se aplica mal

| Orden | Parte | Qué queda | Qué se corrigió | Modo de falla si se aplica mal |
|---|---|---|---|---|
| **1.º** | **Escalada** | Tercera fila en la tabla de autocorrección; pregunta previa **en dos pasos**; el registro agrupa **toda** detención de la fase antes de la primera salida; comprobación enumerable sobre el lote; **sin rol nuevo** | La procedencia **se deriva** del estado previo archivado antes de despachar, y ante la duda se declara propia | El campo pasa la auditoría de forma y no la de fondo: **la cuenta de detenciones «propias» tiende a cero, porque ningún agente se autoincrimina** |
| **2.º** | **Correlación** | Medir la fuente antes de confiar en ella; citar con el tipo de evidencia que ya existe; **rol nuevo** con su carta de mandato | La figura nueva pasa a ser un **campo de dirección** en la existente; la disolución se homologa al contador y al dueño que ya existen | El rol **nunca se disuelve** porque nadie lleva la cuenta con ese criterio, y presiona el techo del panel ciclo tras ciclo |
| **3.º** | **Migración** | La fila-tipo en el plan que M1 ya emite —con fuente, sección candidata y propuesta—; corre en M1 y **no** en M2 | El cruce se recalibra a **fecha por sección**; se presenta como intervención sobre el reporte 25, **no** como plan nuevo | La tabla sale **vacía** sobre un intake con huecos reales: una auditoría «en verde» que contradice la lectura manual, que es el patrón que originó el reporte 25 |

### 14.5 Cierre del ciclo 4

```text
CIERRE DE MESA — ciclo 4, 2026-09-12
  PANEL        AH-006 migración: 5 · AH-007 correlación: 6 · AH-008 escalada: 5 · refutador: 7
  HALLAZGOS    Detectados: 23   Procedentes: 19   Con parche: 3 partes de un plan ordenado
  ENTREGA      Plan corregido E→C→M · reporte 25 a 1.2 (corrección de su propia evidencia) ·
               dos reportes nuevos por emitir (correlación y escalada), separados por el
               criterio del ciclo 3
  CIERRE       Por criterio: el refutador evaluó el plan por su aplicación y corrigió dos P0
               antes de que se escribiera una sola línea normativa
```

---

---

## 15. Control de cambios

| Versión | Fecha | Cambios |
| --- | --- | --- |
| 3.0 | 2026-09-12 | **Ciclo 4**, sobre los tres planteos del Product Owner: la evaluación del intake en la migración, la correlación de los huecos con el historial, y la disciplina de escalada. Entrega **un plan ordenado E→C→M** evaluado por su aplicación, con dos `P0` corregidos por el refutador antes de escribir nada: el cruce de la migración **daba cero filas** sobre el caso real, y el campo de procedencia **era autoinformado**. Corrige además una afirmación errónea del propio reporte 25 sobre `ADR-08007`. Sube major. |
| 2.0 | 2026-09-12 | **Ciclo 2**, convocado por el testimonio del Product Owner (evidencia E4). Establece que el cambio de topología es **una alternativa ya evaluada que se realizó** y que el canal de FTP está vivo **por decisión**; y eleva el planteo de fondo —el método no tiene ciclo evolutivo del producto— a **reporte `25` del framework**, con su prompt de intervención `03`. El refutador corrigió dónde nace el parche (`Master-Prompt.md` §13, no `Rules-Backlog-Tecnico.md` §3.4) y separó el caso en tres. Suma `P-15` y `P-16`. Sube **major**: cambia la lectura del caso. |
| 1.0 | 2026-09-12 | Emisión inicial. Mesa convocada desde `R1.5` de la reanudación. Nueve roles, 55 hallazgos, 42 procedentes, 14 parches, 5 escaladas, 5 ítems de deuda. Establece que **la etapa `i` está abierta desde el 2026-08-27** y que el defecto no es un despliegue fuera del roadmap sino **el entregable de una etapa abierta que cambió sin que el documento lo registre**. Resuelve cuatro contradicciones entre especialistas, una de ellas contra el especialista que la sostuvo. |
