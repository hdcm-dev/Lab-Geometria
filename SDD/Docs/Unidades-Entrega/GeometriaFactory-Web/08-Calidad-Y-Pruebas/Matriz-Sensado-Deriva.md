# Matriz de sensado de deriva — GeometriaFactory-Web

**Proyecto de código:** GeometriaFactory-Web
**Documento:** Matriz-Sensado-Deriva.md
**Versión:** 1.5
**Estado:** Aprobado
**Fecha:** 2026-08-11
**Autor:** Maquetador de validación visual (AG-03M); alta de la sonda `VER-XX` por Developer Advocate / Sample Engineer Senior (AG-10)
**Variante:** Calidad y pruebas
**Trazabilidad upstream:** `../03-UX-UI-DX/Linea-Base-Visual.md` íntegra —`SUP-10001` a `SUP-10011`, `CMP-10001` a `CMP-10073`, `EST-10001` a `EST-10074`, `NAV-10001` a `NAV-10024`—; `../03-UX-UI-DX/Contrato-Datos-Maqueta.md` —`DM-10001` a `DM-10029`, §3 y §6—; `../03-UX-UI-DX/Bitacora-Validacion-Maqueta.md`; `SDD/Maquetas/GeometriaFactory-Web/`; `../../GeometriaFactory-Visor/02-Especificacion-Funcional/Definicion-Contrato-De-Fachada.md` §3.2, §5.5 y §6; `Deriva-Rules.md` §2.3, §3 y §4
**Trazabilidad upstream de la sonda `VER-XX`:** [`../10-Examples/README.md`](../10-Examples/README.md) 1.1 §3 y el contrato de verificación de [`../10-Examples/ejemplo-01-datos-seed.md`](../10-Examples/ejemplo-01-datos-seed.md) 1.1 §9
**Trazabilidad downstream:** el resto de la categoría `08-Calidad-Y-Pruebas` cuando se genere en la Fase E, que incorpora esta matriz a la estrategia de testing; el resumen ejecutivo del handoff, que la entrega con el estado de cada fila

---

## Tabla de contenido

- [1. Qué es esta matriz y quién la mueve](#1-qué-es-esta-matriz-y-quién-la-mueve)
- [2. Estado de la categoría 08 al emitirse este documento](#2-estado-de-la-categoría-08-al-emitirse-este-documento)
- [3. Cómo se agrupan las sondas](#3-cómo-se-agrupan-las-sondas)
- [4. La matriz](#4-la-matriz)
- [5. Umbrales de deriva aplicados](#5-umbrales-de-deriva-aplicados)
- [6. Control de cambios](#6-control-de-cambios)

---

## 1. Qué es esta matriz y quién la mueve

Convierte la línea de base aprobada en una lista de comprobaciones que el humano o un agente pueden correr en cualquier momento de la codificación. La emite AG-03M al cerrar la Fase B2, con **todas las filas en `Sin verificar`**, porque todavía no hay sistema construido contra el cual verificarlas.

Los momentos en que se mueve, de `Deriva-Rules.md` §4:

| Momento | Quién | Qué pasa con esta matriz |
| --- | --- | --- |
| Cierre de la Fase B2 | AG-03M | **Es este documento.** Sesenta y una filas, todas en `Sin verificar` |
| Cierre de la fase que genera la categoría 10 | AG-10 | **Hecho el 2026-08-11.** Alta de una fila `VER-XX` por cada contrato de verificación declarado: la categoría 10 de este proyecto de código declara **uno**, y entra como **`SD-10062`**, en `Sin verificar`, con el comando del contrato como método. Al emitirse esta matriz en 1.0 esa categoría no existía, y esta fila registraba esa ausencia |
| Cierre de la Fase E | AG-08 | Resolución del método de verificación por fila: cuáles se cubren con test automatizado y cuáles quedan como inspección |
| Cierre de cada sprint de codificación | El humano, asistido por el orquestador | Verificación de las filas cuyos elementos toca el sprint. Estado y fecha actualizados; derivas mayores escaladas |
| Regeneración parcial | El orquestador | Las filas que dependen de lo regenerado vuelven a `Sin verificar` |

**Ninguna fila de esta matriz afirma nada sobre el estado del sistema construido.** Todas declaran qué tendría que ser cierto. Por eso la regla D9 no exige evidencia todavía: la columna «Evidencia esperada» dice qué artefacto o ejecución la va a producir, y se completa recién en el cuarto momento.

## 2. Estado de la categoría 08 al emitirse este documento

La categoría `08-Calidad-Y-Pruebas` de este proyecto de código **no se generó todavía**: se genera en la Fase E. Esta carpeta existe únicamente porque `Deriva-Rules.md` §2.3 fija acá la ubicación de la matriz y la Fase B2 la emite al cerrar.

**Este archivo es el único artefacto de la categoría emitido por la Fase B2.** No se anticipó ningún otro documento de 08 —ni estrategia de testing, ni casos de prueba, ni plan de automatización—, y su ausencia no es una omisión de esta fase. Cuando AG-08 genere la categoría, incorpora esta matriz en lugar de crear una nueva.

## 3. Cómo se agrupan las sondas

La línea de base tiene doscientos once elementos identificados —once superficies, setenta y tres componentes, setenta y cuatro estados, veinticuatro rutas y veintinueve campos—. Los identificadores `SD-XX` son de **dos dígitos** como el resto de los identificadores del template, de modo que una fila por elemento no cabe y, sobre todo, no serviría: una matriz de doscientas filas se abandona en dos sprints, que es el anti-patrón que `Deriva-Rules.md` §7 nombra.

La agrupación aplicada, y su criterio:

1. **Una fila por superficie** (`SD-10001` a `SD-10011`). La superficie es la unidad que alguien abre y mira; su fila arrastra sus componentes y su disposición.
2. **Una fila por familia de estados**, agrupada por lo que la produce y no por la superficie que la presenta, porque el umbral es el mismo dentro de la familia.
3. **Una fila por familia de rutas**, agrupada por recorrido.
4. **Una fila por entidad del contrato de datos**, más filas propias para los campos cuyo **formato** es contrato.
5. **Una fila por invariante transversal** —accesibilidad, tokens, reglas de arquitectura, contrato de fachada, movimiento automático—, que no pertenece a ninguna superficie en particular.
6. **Una fila por familia de componentes transversales**, para los componentes que no son propios de una superficie y que las filas por superficie —agrupadas por lo que cada superficie exhibe como suyo— dejaban sin recoger. Hoy hay una sola: `SD-10061`, los cuatro componentes del recorrido de acceso.

Cada fila declara en su columna de elemento **todos los identificadores que cubre**. Ningún identificador de la línea de base queda fuera de la matriz: la cobertura se verifica sumando las columnas de elemento, expandiendo los rangos.

## 4. La matriz

| ID | Elemento de línea de base | Afirmación a verificar | Método de verificación | Evidencia esperada | Umbral de deriva | Estado | Última verificación |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `SD-10001` | `SUP-10001`, `CMP-10001` a `CMP-10003`, `CMP-10006`, `CMP-10010` | La superficie de aprovisionamiento inicial existe con su tarjeta acotada, su identidad, su encabezado de alcance, sus campos de contraseña y su superficie de resolución de destino | Inspección visual contra `Aprovisionamiento-Inicial.html` | `[EV-XX \| artefacto \| ruta de la superficie construida \| sección de la vista \| fecha]` | **Menor**: cambia el nombre, se reordena el contenido. **Mayor**: falta la superficie, o el aprovisionamiento deja de ser la única puerta del primer arranque | `Sin verificar` | — |
| `SD-10002` | `SUP-10002`, `CMP-10052` a `CMP-10055` | La superficie de registro existe, con su subtítulo de expectativa, sus campos, su única salida y su bloque de éxito | Ídem contra `Registro-De-Cuenta.html` | Ídem | Ídem §5, dimensión de superficies | `Sin verificar` | — |
| `SD-10003` | `SUP-10003`, `CMP-10030` a `CMP-10033` | La superficie de ingreso existe, con su tarjeta de acceso, su campo de contraseña, su enlace a registro y la nota inerte sobre la contraseña olvidada | Ídem contra `Ingreso.html` | Ídem | Ídem. **Mayor** si la nota sobre la contraseña olvidada pasa a ser un enlace que dispara algo: el producto no tiene canal de correo | `Sin verificar` | — |
| `SD-10004` | `SUP-10004`, `CMP-10011` a `CMP-10015` | La superficie de credencial propia existe con sus **dos cursos**, establecimiento y cambio, y el campo de contraseña actual sólo en el segundo | Ídem contra `Credencial-Propia.html`, alternando los dos cursos | Ídem | **Mayor**: falta uno de los dos cursos, o el campo de contraseña actual deja de ser obligatorio en el cambio | `Sin verificar` | — |
| `SD-10005` | `SUP-10005`, `CMP-10029`, `CMP-10037`, `CMP-10051` | El panel de trabajos del alumno existe, con su fila de trabajo, su estado vacío explicado y el diálogo de confirmación de eliminación, que sólo se abre desde una fila en estado `Borrador` | Ídem contra `Panel-De-Trabajos-Del-Alumno.html` | Ídem | **Mayor**: falta el estado vacío explicado, o la eliminación se ofrece sobre un trabajo que no está en estado `Borrador` | `Sin verificar` | — |
| `SD-10006` | `SUP-10006`, `CMP-10016` a `CMP-10024` | La superficie de envío existe con sus campos, su área de texto, su previsualización sin llamada al servicio de datos, sus dos notas inertes y su bloque de resultado | Ídem contra `Envio-De-Trabajo.html` | Ídem | **Mayor**: falta el bloque de resultado, o la previsualización emite una petición hacia el servicio de datos | `Sin verificar` | — |
| `SD-10007` | `SUP-10007`, `CMP-10065` a `CMP-10073` | La vista de trabajo existe con sus **cuatro partes** —datos, texto, escena y árbol—, más el comentario y las observaciones en la columna izquierda | Ídem contra `Vista-De-Trabajo.html` | Ídem | **Menor**: cambia el espaciado o el orden dentro de una parte. **Mayor**: falta una de las cuatro partes, o el comentario se mezcla con las observaciones | `Sin verificar` | — |
| `SD-10008` | `SUP-10008`, `CMP-10056` a `CMP-10064` | El bloque de decisión existe **dentro** de la vista de trabajo, con sus dos decisiones, su comentario opcional, su retiro separado y sus dos diálogos | Inspección de la vista de trabajo abierta como administrador sobre un trabajo en estado `Pendiente` | Ídem | **Mayor**: el bloque tiene **ruta propia**, o aparece para el alumno, o aparece sobre un trabajo que no está en estado `Pendiente`, o el comentario pasa a ser obligatorio | `Sin verificar` | — |
| `SD-10009` | `SUP-10009`, `CMP-10042` a `CMP-10050` | El panel de cuentas existe con su fila de cuenta, su insignia de **situación**, la acción de situación que ofrece sólo la transición admitida, la baja con confirmación escrita y la orientación posterior. **Alcance acotado al comportamiento validado el 2026-08-09**: esta sonda **no cubre la exhibición de la contraseña provisoria de `RN-10016`** ni la marca de cambio pendiente, que no tienen identificador en la línea de base; ver §4 | Ídem contra `Panel-De-Cuentas.html` | Ídem | **Mayor**: falta la confirmación escrita de la baja, falta el aviso de arrastre en el mismo lugar donde se confirma, o se ofrecen las tres transiciones de situación a la vez | `Sin verificar` | — |
| `SD-10010` | `SUP-10010`, `CMP-10034` a `CMP-10041` | El listado de la comisión existe, agrupado por alumno, con su barra de filtros, su nota de ausencia y su panel de resumen | Ídem contra `Listado-De-La-Comision.html` | Ídem | **Mayor**: aparece un trabajo en estado `Borrador`, o el listado ofrece aprobar o rechazar sin abrir el trabajo | `Sin verificar` | — |
| `SD-10011` | `SUP-10011`, `CMP-10025` a `CMP-10028` | El estado degradado y la reconexión existen, con el aviso que reemplaza **el contenido y no el armazón**, la acción de reintentar nombrada, el cartel de reconexión estilizado con los tokens del producto y el contenido conservado | Ídem contra `Estado-Degradado-Y-Reconexion.html` | Ídem | **Mayor**: el aviso reemplaza el armazón y la navegación desaparece, o lo escrito se pierde al caerse el servicio | `Sin verificar` | — |
| `SD-10012` | `EST-10003`, `EST-10004`, `EST-10012` | Los tres estados de piso —con datos, cargando y vacío— existen en todas las superficies que la línea de base declara | Conmutación estado por estado en el sistema construido, contra la tabla de §4 de la línea de base | `[EV-XX \| artefacto \| test de estados de 08 \| identificador del caso \| fecha]` | **Menor**: cambia el tipo de indicador de carga. **Mayor**: falta un estado aprobado en una superficie que lo declaraba | `Sin verificar` | — |
| `SD-10013` | `EST-10005`, `EST-10006`, `EST-10008`, `EST-10009` | Los cuatro estados de intento —enviando, error de entrada, error de operación y éxito— existen donde la línea de base los declara, y el error de entrada se distingue del de operación | Ídem | Ídem | **Menor**: cambia el texto del mensaje. **Mayor**: los dos errores se colapsan en uno solo | `Sin verificar` | — |
| `SD-10014` | `EST-10010`, `EST-10011`, `EST-10030` a `EST-10035` | Los estados de degradación —indisponible, reconectando, indisponible con lo escrito conservado, reconectado, transporte replegado, fallo no clasificado, sesión no restablecible y recuperado— existen | Ídem, forzando la caída del servicio de datos y el corte del circuito | Ídem | **Mayor**: falta la distinción entre indisponible y vacío, o el transporte replegado emite un aviso que la línea de base declara ausente | `Sin verificar` | — |
| `SD-10015` | `EST-10001`, `EST-10002`, `EST-10036` a `EST-10042` | Los estados de acceso —resolución de destino, ya aprovisionado, las tres bandas de confirmación, sesión cerrada, credencial rechazada, cuenta no habilitada y contraseña sin establecer— existen | Ídem | Ídem | **Mayor**: falta una de las tres bandas de confirmación, o la redirección de «ya aprovisionado» apila la entrada del historial en vez de reemplazarla | `Sin verificar` | — |
| `SD-10016` | `EST-10007`, `EST-10015` a `EST-10020` | Los estados de la credencial propia —los dos cursos, confirmación no coincidente, contraseña actual rechazada, cuenta bloqueada entre la derivación y el envío, y los dos éxitos— existen | Ídem | Ídem | **Mayor**: falta el estado de cuenta bloqueada entre la derivación y el envío, que es la carrera que la superficie declara | `Sin verificar` | — |
| `SD-10017` | `EST-10021` a `EST-10024` | Los estados del envío —verificó, verificó con advertencias, no verificó y previsualizado— existen, y **las advertencias no impiden la entrega** | Ídem, con los escenarios `E-1`, `E-3` y `E-5` del intake §20 | Ídem | **Mayor**: una advertencia impide el paso a estado `Pendiente`, o desaparece la distinción entre verificó y verificó con advertencias | `Sin verificar` | — |
| `SD-10018` | `EST-10025` a `EST-10028`, `EST-10071` a `EST-10074` | Los ocho estados que materializan las **siete condiciones del contrato de fachada**, con los dos cursos de `ELEMENTO_DE_DIBUJO_INVALIDO`, existen y usan los códigos del contrato sin renombrarlos | Conmutación de los ocho estados en la vista de trabajo, contra `Definicion-Contrato-De-Fachada.md` §6 | `[EV-XX \| linea-base \| Linea-Base-Visual.md \| EST-10025 a EST-10028 y EST-10071 a EST-10074 \| fecha]` | **Mayor**: falta un estado, aparece un código que no está en la lista cerrada de siete, o una condición se presenta como observación del trabajo | `Sin verificar` | — |
| `SD-10019` | `EST-10043` a `EST-10047`, `EST-10048` a `EST-10057` | Los estados propios de los tres paneles —filtrado sin resultados, grupo colapsado, resumen abierto, cero borradores, alumno del filtro inexistente, orientación posterior, los dos errores de operación de cuentas, las dos confirmaciones y los dos estados de aplicación— existen | Ídem | Ídem | **Menor**: cambia el texto. **Mayor**: falta un estado aprobado, o el filtrado sin resultados se confunde con el vacío | `Sin verificar` | — |
| `SD-10020` | `EST-10058` a `EST-10066` | Los nueve estados de la resolución —resoluble y sus dos formas de no resoluble, las dos confirmaciones, aplicando, los dos éxitos y el error de desenlace no ejercido por el administrador— existen | Ídem | Ídem | **Mayor**: falta la distinción entre no resoluble por papel y no resoluble por estado | `Sin verificar` | — |
| `SD-10021` | `EST-10067` a `EST-10070`, `EST-10029` | Los estados de contenido de la vista de trabajo —sin observaciones, sin comentario, con comentario, borrador con errores— y el de trabajo ajeno o inexistente existen | Ídem | Ídem | **Mayor**: «sin observaciones» se dibuja como hueco en vez de línea explícita, o el trabajo ajeno se distingue del inexistente (RN-10003) | `Sin verificar` | — |
| `SD-10022` | `EST-10013`, `EST-10014` | Los dos estados del sello de versión —preliminar y origen indeterminado— existen y se distinguen del sello publicado | Ídem, contra `Representacion-Sello-De-Version.md` §3 | Ídem | **Menor**: cambia el distintivo. **Mayor**: el origen indeterminado se dibuja igual que el publicado | `Sin verificar` | — |
| `SD-10023` | `NAV-10001` a `NAV-10007` | El recorrido de acceso cierra: entrada sin administrador, aprovisionamiento, registro, ingreso y establecimiento de la contraseña, con sus bandas de confirmación | Recorrido completo en el sistema construido | `[EV-XX \| ejecucion \| test de navegación de 08 \| identificador del recorrido \| fecha]` | **Mayor**: falta una ruta, aparece un callejón sin salida, o una banda de confirmación no llega a destino | `Sin verificar` | — |
| `SD-10024` | `NAV-10008` a `NAV-10015`, `NAV-10024` | Los destinos por papel de la barra lateral son **tres por papel** y el ingreso lleva a la ruta inicial que corresponde al papel; el cierre de sesión vuelve al ingreso con su banda | Ídem, con los dos papeles | Ídem | **Mayor**: aparece un destino que la línea de base no declara, o el alumno alcanza un destino del administrador | `Sin verificar` | — |
| `SD-10025` | `NAV-10016` a `NAV-10019`, `NAV-10023` | El recorrido del alumno cierra: abrir, editar el borrador, enviar y volver, **conservando el origen** para que «volver» vuelva a donde estaba | Ídem | Ídem | **Menor**: cambia el disparador visual. **Mayor**: se pierde el origen y «volver» lleva a un destino fijo | `Sin verificar` | — |
| `SD-10026` | `NAV-10020` a `NAV-10022` | El recorrido del administrador cierra: abrir desde la entrega de la comisión, resolver o retirar, y volver al listado con el trabajo ya actualizado | Ídem | Ídem | **Mayor**: el desenlace no vuelve al listado, o el trabajo retirado sigue figurando | `Sin verificar` | — |
| `SD-10027` | `SUP-10008`, `NAV-10001` a `NAV-10024` | **Ninguna ruta tiene por destino la superficie alojada.** No hay dirección, ni entrada de menú, ni enlace que lleve a la resolución del trabajo por fuera de la vista de trabajo | Revisión de la tabla de rutas del sistema construido y de los tres destinos por papel | `[EV-XX \| artefacto \| tabla de rutas del sistema construido \| declaración de rutas \| fecha]` | **Mayor**, sin gradación: si existe una ruta a la superficie alojada, es deriva mayor. Es el hallazgo que la validación visual expuso y la razón por la que esta sonda existe | `Sin verificar` | — |
| `SD-10028` | `DM-10001` a `DM-10007` | Los siete campos de la cuenta se exhiben con su tipo y su obligatoriedad, y el papel y la situación son conjuntos cerrados de dos y de tres valores | Inspección del esquema de datos y de la superficie de cuentas | `[EV-XX \| linea-base \| Contrato-Datos-Maqueta.md \| DM-10001 a DM-10007 \| fecha]` | **Menor**: cambia el orden de los campos en la presentación. **Mayor**: falta un campo, cambia el tipo, cambia la obligatoriedad, o un conjunto cerrado admite un valor más | `Sin verificar` | — |
| `SD-10029` | `DM-10008` a `DM-10016` | Los nueve campos del trabajo se exhiben con su tipo y su obligatoriedad, y el estado es un conjunto cerrado de cuatro valores con dos terminales | Ídem | Ídem | Ídem. **Mayor** además si el comentario pasa a ser obligatorio o admite más de uno | `Sin verificar` | — |
| `SD-10030` | `DM-10017` a `DM-10022` | Los seis campos de la observación se exhiben, con **índice de figura y campo señalado siempre presentes** y nunca un texto genérico | Ídem, con el escenario `E-5` del intake §20 | Ídem | **Mayor**: un error de validación se presenta sin índice de figura o sin campo, o con un mensaje genérico (RN-10009) | `Sin verificar` | — |
| `SD-10031` | `DM-10023` a `DM-10026` | El índice y el tipo de cada pieza, y el índice y el motivo de cada condición del dibujo, se exhiben; el índice de la pieza es **el mismo** en el árbol, en la escena y en el resultado de dibujo | Ídem, con selección cruzada entre árbol y escena | Ídem | **Mayor**: los índices dejan de coincidir, o la condición del dibujo se presenta dentro de la lista de observaciones | `Sin verificar` | — |
| `SD-10032` | `DM-10027` a `DM-10029` | Los tres campos de la identidad de versión se exhiben en el sello, y el detalle de diagnóstico expone la identidad del artefacto y **nunca la topología** | Inspección del sello en sus dos ubicaciones obligatorias | Ídem | **Mayor**: el detalle expone una dirección de servicio interno, o la versión legible se compone en la vista en vez de llegar formada | `Sin verificar` | — |
| `SD-10033` | `DM-10020`, `DM-10021`, `Contrato-Datos-Maqueta.md` §3 | Los valores `declarado` y `derivado` se muestran **exactamente como llegan, sin reformatear**, en las tres superficies que los presentan | Inspección visual con el escenario `E-1`, comparando el texto original contra la lista de observaciones | `[EV-XX \| linea-base \| Contrato-Datos-Maqueta.md \| §3, fila de DM-10020 y DM-10021 \| fecha]` | **Mayor**, por la tabla de §3 de `Deriva-Rules.md`: cambiar el formato de presentación acordado es deriva mayor aunque el dato sea el mismo. Es el hallazgo `H-2` de la validación visual | `Sin verificar` | — |
| `SD-10034` | `DM-10006`, `DM-10009`, `DM-10016`, `Contrato-Datos-Maqueta.md` §3 | Las tres fechas se muestran en formato corto `dd/mm/aaaa`, y la fecha del trabajo se rotula distinto de la fecha de registro | Ídem | Ídem | **Mayor**: una fecha aparece como marca de tiempo cruda, o las dos fechas se rotulan igual | `Sin verificar` | — |
| `SD-10035` | `DM-10005`, `DM-10011`, `CMP-10038`, `CMP-10044` | Las insignias llevan **su texto siempre presente** y el color es refuerzo; la situación de la cuenta se rotula «situación» y no «estado» | Inspección visual, y revisión en escala de grises | `[EV-XX \| ejecucion \| revisión en escala de grises \| captura de las dos superficies \| fecha]` | **Mayor**: el color queda como único canal, o `Pendiente` aparece sin calificar en un contexto donde conviven los dos referentes | `Sin verificar` | — |
| `SD-10036` | `DM-10012` | El texto original se conserva **íntegro, carácter por carácter**, y se muestra con avance uniforme por carácter, colapsado por omisión | Comparación byte a byte entre lo enviado y lo mostrado | `[EV-XX \| ejecucion \| comparación del texto enviado contra el mostrado \| escenario E-2 \| fecha]` | **Mayor**, sin gradación: cualquier normalización, reordenamiento o carácter faltante viola RN-10008 | `Sin verificar` | — |
| `SD-10037` | `Contrato-Datos-Maqueta.md` §6 punto 1 | El texto semilla del escenario `E-1` produce **exactamente 3 piezas y 2 advertencias** | Ejecución del escenario `E-1` contra el sistema construido | `[EV-XX \| ejecucion \| caso de prueba del escenario E-1 \| recuentos de piezas y de advertencias \| fecha]` | **Mayor**, sin gradación: es el caso de prueba canónico del producto | `Sin verificar` | — |
| `SD-10038` | `Contrato-Datos-Maqueta.md` §6 punto 2 | El cilindro del escenario `E-1` **no produce ninguna observación**: la diferencia de `0.01` no supera la tolerancia, que es estricta | Ídem | Ídem | **Mayor**: aparece una tercera advertencia, lo que significaría que el operador de tolerancia dejó de ser estricto | `Sin verificar` | — |
| `SD-10039` | `Contrato-Datos-Maqueta.md` §6 punto 7, `EST-10073` | Una pieza que expone una dimensión con valor `0.00` **se dibuja** y no produce `DIMENSION_NO_LEGIBLE` | Ejecución del escenario `E-6` del intake §20 | `[EV-XX \| ejecucion \| caso de prueba del escenario E-6 \| resultado de dibujo \| fecha]` | **Mayor**, sin gradación: perder esa figura vacía la garantía G-5 del contrato de fachada y contradice un escenario declarado | `Sin verificar` | — |
| `SD-10040` | `EST-10027`, `CMP-10022` | **Ninguna pieza desaparece sin quedar enumerada**: el recuento de piezas sin registro del resultado de dibujo es siempre 0 | Ejecución del escenario `E-5` y del escenario `E-7` sin la dimensión del círculo | `[EV-XX \| ejecucion \| recuento de piezas sin registro \| resultado de dibujo \| fecha]` | **Mayor**, sin gradación: es el fallo silencioso que el producto viene a eliminar (NB-00006) | `Sin verificar` | — |
| `SD-10041` | `CMP-10071`, `Definicion-Contrato-De-Fachada.md` §5.4 | La disposición es **determinista**: dos cargas del mismo texto producen la misma disposición, comparable pieza por pieza | Dos cargas sucesivas del mismo texto, con comparación pieza por pieza | `[EV-XX \| ejecucion \| doble carga del escenario E-1 \| comparación de disposiciones \| fecha]` | **Mayor**: la disposición cambia entre dos cargas del mismo texto | `Sin verificar` | — |
| `SD-10042` | `CMP-10071`, `Definicion-Contrato-De-Fachada.md` §4.5 | Diez recorridos de ida y vuelta entre trabajos **no degradan**: `destruir` libera geometrías, materiales y contexto gráfico | Diez ciclos completos, con recuento de recursos vivos al final | `[EV-XX \| ejecucion \| diez recorridos de ida y vuelta \| recuento de geometrías, materiales y contextos vivos \| fecha]` | **Mayor**: quedan instancias vivas o recursos sin liberar al final de los diez | `Sin verificar` | — |
| `SD-10043` | `CMP-10019`, `CMP-10071`, RA-01 y RA-02 | La escena se opera **exclusivamente por las seis funciones** de la fachada —`inicializar`, `cargarJson`, `seleccionarPieza`, `redimensionar`, `establecerMovimiento` y `destruir`—, y ningún guion del navegador llama al servicio de datos. **Invocar `establecerMovimiento` no es deriva: es lo que el contrato le manda hacer al anfitrión** | Revisión del código del componente anfitrión, más recuento de peticiones en la pestaña de red durante la interacción | `[EV-XX \| ejecucion \| pestaña de red durante la interacción con la escena \| recuento de peticiones \| fecha]` | **Mayor**, sin gradación: el umbral de peticiones originadas por el visor es exactamente 0, y operar la escena por fuera de las seis funciones es deriva mayor. **La sexta función no está validada visualmente** —ver la nota al pie de §4—, de modo que esta fila la sensa contra `Definicion-Contrato-De-Fachada.md` §4.6 y no contra la maqueta | `Sin verificar` | — |
| `SD-10044` | `CMP-10072`, `Definicion-Contrato-De-Fachada.md` §5.5 | Los **dos movimientos automáticos** existen, son independientes, se tildan por separado y pueden estar tildados los dos a la vez | Inspección visual de la vista de trabajo, en las cuatro combinaciones | `[EV-XX \| linea-base \| Linea-Base-Visual.md \| CMP-10072 \| fecha]` | **Menor**: cambia la velocidad o el rótulo. **Mayor**: falta uno de los dos, o los dos se colapsan en un solo control | `Sin verificar` | — |
| `SD-10045` | `CMP-10072`, `Definicion-Contrato-De-Fachada.md` §5.5 punto 4 | **Ningún movimiento altera la disposición**: cada pieza sigue cayendo en la celda que le da su índice, en las cuatro combinaciones | Comparación de disposiciones con cada combinación de las dos casillas | `[EV-XX \| ejecucion \| comparación de disposiciones en las cuatro combinaciones \| resultado de dibujo \| fecha]` | **Mayor**, sin gradación: el determinismo de la posición es garantía del contrato | `Sin verificar` | — |
| `SD-10046` | `CMP-10072`, `Definicion-Contrato-De-Fachada.md` §5.5 puntos 5 y 6 | Al apagar el giro, las piezas **vuelven a su orientación de partida**; los dos movimientos se detienen mientras la persona arrastra y con la pestaña oculta | Inspección visual, apagando cada movimiento y arrastrando la cámara | Ídem | **Menor**: cambia la duración de la detención. **Mayor**: las piezas quedan donde el tiempo las encontró, o el movimiento le pelea el control a quien arrastra | `Sin verificar` | — |
| `SD-10047` | `CMP-10072`, `Definicion-Contrato-De-Fachada.md` §5.5 punto 7 | La **preferencia de cada movimiento es del componente anfitrión**: la fachada no la conserva, no lee la preferencia de movimiento reducido del sistema y no escribe ninguna clave. El anfitrión le manda **dos valores de verdad**, uno por movimiento, y con opciones ausentes o parciales la instancia arranca con los dos **apagados** (§4.1). La exclusión de claves propias del recuento se hace por **espacio de nombres declarado**, no por prefijo | Revisión del código de la fachada, más recuento de claves escritas en el almacenamiento del navegador | `[EV-XX \| ejecucion \| recuento de claves del almacenamiento del navegador \| umbral 0 para las escritas por la fachada \| fecha]` | **Mayor**: la fachada escribe una clave, o consulta configuración por su cuenta. Viola G-2 o G-3 | `Sin verificar` | — |
| `SD-10048` | `CMP-10072`, `Experiencia-De-Uso.md` §7 | Con **preferencia de movimiento reducido** declarada por el sistema, los dos movimientos arrancan destildados y el control declara por qué | Inspección con la preferencia activa en el sistema operativo o en el navegador | `[EV-XX \| ejecucion \| revisión de accesibilidad con movimiento reducido activo \| estado inicial de las dos casillas \| fecha]` | **Mayor**, por la dimensión de accesibilidad de §3 de `Deriva-Rules.md`: un movimiento ambiental que no se puede apagar es una barrera | `Sin verificar` | — |
| `SD-10049` | `CMP-10073` | El árbol de la estructura es **navegable por teclado** —flechas arriba y abajo entre piezas, derecha e izquierda para desplegar y plegar— y arranca colapsado | Recorrido completo por teclado | `[EV-XX \| ejecucion \| recorrido por teclado del árbol \| secuencia de foco \| fecha]` | **Menor**: cambia el orden de foco dentro de un grupo. **Mayor**: se pierde el recorrido por teclado | `Sin verificar` | — |
| `SD-10050` | `CMP-10071`, `CMP-10073`, `CMP-10022` | La escena tiene **equivalente accesible**: alternativa textual compuesta desde el resultado de dibujo, árbol completo y enumeración textual de las piezas no dibujadas. La alternativa **no se lee del interior de la escena** | Inspección con lector de pantalla, más revisión del código del componente anfitrión | `[EV-XX \| ejecucion \| revisión con lector de pantalla \| alternativa textual de la escena \| fecha]` | **Mayor**: la escena queda como única vía a la información | `Sin verificar` | — |
| `SD-10051` | Las once superficies | **Foco visible** en todo elemento interactivo y recorrido completo por teclado, incluidos los diálogos, con foco confinado, escape para cerrar y foco devuelto al control que los abrió | Recorrido por teclado de las once superficies | `[EV-XX \| ejecucion \| test de accesibilidad de 08 \| identificador del caso \| fecha]` | **Mayor**: se pierde el foco visible o el recorrido por teclado | `Sin verificar` | — |
| `SD-10052` | Las once superficies | **Contraste de 4.5:1** en texto, incluido el sello de versión, que usa el token secundario y no el terciario | Medición de contraste sobre las once superficies | `[EV-XX \| ejecucion \| medición de contraste \| par de colores por token \| fecha]` | **Mayor**: el contraste cae bajo el piso. Ver la observación al framework sobre el token terciario del catálogo de diseño, hallazgo `H-1` | `Sin verificar` | — |
| `SD-10053` | Las once superficies | **Regiones activas** anuncian el cambio de estado, el resultado del envío, el aviso de indisponibilidad, el cartel de reconexión, la selección de pieza y el cambio de cada casilla de movimiento | Inspección con lector de pantalla | Ídem | **Mayor**: un cambio de estado ocurre sin anuncio | `Sin verificar` | — |
| `SD-10054` | Las once superficies | **Todo valor visual sale de un token** del catálogo de diseño. No hay literales visuales ad hoc | Revisión de las hojas de estilo del sistema construido | `[EV-XX \| artefacto \| hojas de estilo del sistema construido \| declaración de tokens \| fecha]` | **Menor**: cambia el valor de un token. **Mayor**: aparece un literal visual fuera del catálogo | `Sin verificar` | — |
| `SD-10055` | Las once superficies | **Versión angosta**: por debajo del punto de quiebre las superficies reflúyen a una columna y las filas pasan a tarjetas apiladas, conservando su nombre. En la vista de trabajo la escena va primero y se ajusta al cruzar el punto de quiebre | Inspección visual en viewport angosto | `[EV-XX \| ejecucion \| inspección en viewport angosto \| las once superficies \| fecha]` | **Menor**: cambia el orden de dos bloques. **Mayor**: una superficie deja de ser operable en ancho angosto, o la escena no se ajusta al cruzar el punto de quiebre | `Sin verificar` | — |
| `SD-10056` | `CMP-10009`, `DM-10027` a `DM-10029` | El sello de versión aparece en sus **dos ubicaciones obligatorias**: la superficie de acceso y la barra lateral del shell de trabajo | Inspección visual de las once superficies | `[EV-XX \| linea-base \| Linea-Base-Visual.md \| CMP-10009 \| fecha]` | **Menor**: cambia la tipografía. **Mayor**: falta una de las dos ubicaciones | `Sin verificar` | — |
| `SD-10057` | RA-03 | **Ningún mensaje visible** incluye la dirección de un servicio interno, un nombre de archivo de datos, una traza ni un código de error | Barrido de la microcopy del sistema construido y de los mensajes de error | `[EV-XX \| ejecucion \| barrido de mensajes visibles \| umbral 0 \| fecha]` | **Mayor**, sin gradación: es regla de arquitectura | `Sin verificar` | — |
| `SD-10058` | `DM-10011`, `DM-10005`, `Glosario-UX.md` §2 | `Pendiente` aparece **siempre calificado** en todo texto visible, y el comentario del administrador no se presenta como observación ni como calificación | Barrido de la microcopy del sistema construido | `[EV-XX \| artefacto \| microcopy del sistema construido \| ocurrencias de Pendiente \| fecha]` | **Mayor**: una forma desnuda de `Pendiente` en una superficie donde conviven los dos referentes | `Sin verificar` | — |
| `SD-10059` | `Linea-Base-Visual.md` §6 | **Ningún instrumento de la maqueta llegó al producto**: ni la barra de validación, ni el panel del contrato de fachada, ni la credencial de prueba exhibida, ni la portada de la maqueta | Barrido del sistema construido buscando cada uno de los cuatro instrumentos | `[EV-XX \| ejecucion \| barrido de instrumentos de maqueta en el sistema construido \| umbral 0 \| fecha]` | **Mayor**, sin gradación: un instrumento de validación en producción expone superficie que nadie especificó | `Sin verificar` | — |
| `SD-10060` | `Contrato-Datos-Maqueta.md` §5 | Los **valores compuestos para la maqueta** que no se propagaron no aparecen en el sistema construido: la credencial de prueba y la cuarta cuenta de ejemplo | Ídem | Ídem | **Mayor**: un dato compuesto para la maqueta figura como dato del producto | `Sin verificar` | — |
| `SD-10061` | `CMP-10004`, `CMP-10005`, `CMP-10007`, `CMP-10008` | Los cuatro **componentes transversales del recorrido de acceso** existen en **todas** las superficies que la línea de base les declara, con su comportamiento aprobado: la **banda de resultado** en las cuatro superficies de acceso, con su variante de error en rol de alerta y su variante de confirmación en rol de estado; el **campo de correo** en tres, con etiqueta visible arriba y **sin texto de ejemplo que sustituya la etiqueta**; el **requisito declarado** en dos, asociado al campo que describe y **presente desde el principio, no recién al fallar**; y la **acción primaria** en seis, con el verbo exacto de cada superficie, a ancho completo, inhabilitada con indicador durante el envío | Inspección visual superficie por superficie contra las columnas de superficie de `CMP-10004`, `CMP-10005`, `CMP-10007` y `CMP-10008` de la línea de base, más recorrido con lector de pantalla para distinguir los dos roles de la banda | `[EV-XX \| linea-base \| Linea-Base-Visual.md \| CMP-10004, CMP-10005, CMP-10007 y CMP-10008 \| fecha]` | **Menor**: cambia el texto de una etiqueta o el orden dentro de la tarjeta. **Mayor**: falta el componente en una superficie que lo declaraba; el requisito aparece recién al fallar; el campo de correo pierde la etiqueta visible y queda sólo con texto de ejemplo; o la acción primaria deja de ser única o no se inhabilita durante el envío | `Sin verificar` | — |

| `SD-10062` | `VER-10001` de [`../10-Examples/ejemplo-01-datos-seed.md`](../10-Examples/ejemplo-01-datos-seed.md) §9 | El estado de partida del guion de demostración se reproduce con un comando y sin pasar por la pantalla: los **ocho** escenarios del intake §20 se envían y quedan en **Pendiente=4, Borrador=2, Aprobado=1 y Rechazado=1**; el listado propio devuelve **8** trabajos y el de la comisión **6**, con **0** borradores visibles; y el texto original leído del detalle es **idéntico** al enviado | El comando del contrato: `bash samples/web/01-datos-seed/run.sh` | Campo `evidencia` de `VER-10001`, con su fecha | **Mayor, sin gradación** en los dos tramos que verifican regla de negocio: un solo borrador visible en el listado de la comisión (`RN-10011`) o un solo carácter cambiado en el texto original (`RN-10008`). **Menor** en el resto: cambia el texto de una línea de salida sin cambiar su semántica. **Mayor** además si el `criterio_aceptacion` falla, si cambia el comando sin actualizar el contrato, si aparecen precondiciones no declaradas, o si alguno de `CU-10005`, `CU-10006` o `CU-10008` deja de estar cubierto | `Sin verificar` | — |

**Cobertura.** Las **sesenta y una** filas de línea de base —`SD-10001` a `SD-10061`— cubren los doscientos once identificadores de la línea de base. `SD-10062` no cubre ninguno de ellos: se ancla en un contrato de verificación y no en un elemento validado visualmente. La suma, expandiendo todos los rangos:

| Familia | Cubiertos | Filas que los cubren |
| --- | --- | --- |
| `SUP-10001` a `SUP-10011` | 11 de 11 | `SD-10001` a `SD-10011`, más `SD-10027` para `SUP-10008` |
| `CMP-10001` a `CMP-10073` | 73 de 73 | `SD-10001` a `SD-10011`, `SD-10035`, `SD-10040` a `SD-10050`, `SD-10056` y **`SD-10061`** |
| `EST-10001` a `EST-10074` | 74 de 74 | `SD-10012` a `SD-10022`, más `SD-10018` y `SD-10039` para los estados de condición |
| `NAV-10001` a `NAV-10024` | 24 de 24 | `SD-10023` a `SD-10027` |
| `DM-10001` a `DM-10029` | 29 de 29 | `SD-10028` a `SD-10036` |

**Qué corrige esta versión, y por qué importa.** La versión 1.0 afirmaba esta misma cobertura con una enumeración que **no resolvía**: `CMP-10004` —banda de resultado, en cuatro superficies—, `CMP-10005` —campo de correo, en tres—, `CMP-10007` —requisito declarado, en dos— y `CMP-10008` —acción primaria, en seis— no figuraban en ninguna fila, y la suma real daba 69 de 73. La corrección **no consistió en anotar los cuatro identificadores en una fila existente para que la cuenta cerrara**: se abrió `SD-10061`, que los sensa de verdad, con método, evidencia esperada y umbral propios. Una sonda declarada que no sensa nada es peor que una ausencia declarada.

**Una fila `VER-XX`, dada de alta el 2026-08-11** al emitirse [`../10-Examples/`](../10-Examples/). Es `SD-10062`, tomada del único contrato de verificación de esa categoría, con el comando del contrato como método y sin ningún desvío. **Sin contratos huérfanos ni filas sin contrato que las respalde**, que es lo que exige `Deriva-Rules.md` §6: la correspondencia es uno a uno con `VER-10001`.

**Qué aporta `SD-10062` que las sesenta y una anteriores no aportaban, y por qué no duplica a ninguna.** Las sesenta y una se resuelven, en su mayoría, por **inspección** —visual, de accesibilidad, de código o de tráfico—, porque este proyecto de código no tiene proyecto de pruebas propio y su verificación es un guion que **una persona ejecuta** ([`Estrategia-Testing.md`](Estrategia-Testing.md) §1 y §3). `SD-10062` es la **primera fila de esta matriz que trae su propio comando y su propia aserción**, que es la asimetría que `Deriva-Rules.md` §4 declara en su cuarto momento. Donde mira lo mismo que una fila anterior —`SD-10036` para el texto original y `SD-10010`, la superficie del listado de la comisión, para los borradores— **el umbral se declaró igual y no se contradice**, y conviene decirlo con la precisión que cada fila permite: `SD-10036` declara «**Mayor**, sin gradación» y figura entre las filas sin gradación que enumera §5; `SD-10010` **no figura** en esa lista, pero declara «**Mayor**: aparece un trabajo en estado `Borrador`, o el listado ofrece aprobar o rechazar sin abrir el trabajo», sin ningún tramo menor para ese supuesto. **En los dos casos el veredicto de `SD-10062` coincide con el de la fila anterior**, que es lo que había que descartar; a `SD-10010` no se le atribuye una clasificación que la matriz no le da, y no se la agrega a la lista de §5 porque eso modificaría una de las **61** filas de línea de base.

**Total: sesenta y dos filas.** Sesenta y una ancladas en la línea de base visual y una en el contrato de verificación de `10-Examples`.

**Lo que esta matriz sensa y la maqueta no validó.** La sexta función de la fachada, `establecerMovimiento(id, opciones)`, se incorporó al contrato **después** de que el Product Owner aprobó la maqueta el 2026-08-09, y **no fue validada visualmente**: el panel del contrato de fachada exhibe cinco botones y el gobierno del movimiento se resuelve con dos métodos de instancia. `SD-10043` la sensa igual, porque la superficie pública que el sistema construido tiene que respetar es la de `Definicion-Contrato-De-Fachada.md` §4, que declara seis. La fila queda anclada al contrato y no a la maqueta, y así lo dice su columna de umbral. Ver `Linea-Base-Visual.md` §6 y `Bitacora-Validacion-Maqueta.md` §2.b.

**Lo que esta matriz todavía no sensa, y por qué no se le inventa una sonda.** La capacidad **F-26** —reseteo de contraseña por el administrador— entró con el `PRODUCT-INTAKE` **1.7**, posterior a la aprobación de la maqueta, y arrastra elementos de interfaz que **no tienen identificador en la línea de base**: la quinta operación de la fila de `SUP-10009`, su diálogo de confirmación, la comunicación de la contraseña provisoria y el **tercer curso de `SUP-10004`**, el cambio forzado. Una sonda de esta matriz se ancla en un identificador `SUP-XX`, `CMP-XX`, `EST-XX` o `NAV-XX` **validado visualmente**, y ninguno de esos elementos lo está: abrirle una fila hoy produciría una sonda que dice comparar contra una línea de base que no lo contiene. **`SD-10004` conserva su alcance** —los **dos** cursos que la maqueta validó— y no se lo amplía por decreto. Las sondas de F-26 nacen con la **iteración 5** de maqueta y la reemisión de la línea de base que `Linea-Base-Visual.md` §6.1 declara pendiente. Hasta entonces, lo que gobierna la construcción de esa capacidad son `CU-10004` §5 FA-06 y FA-07, `CU-10003` §5 FA-04 a FA-06 y sus wireframes **en su versión vigente**, con sus criterios de aceptación propios.

**Y una segunda ronda de lo mismo, que no es una capacidad nueva sino un comportamiento nuevo sobre algo ya validado.** La regla **`RN-10016`** —habilitar una cuenta produce su contraseña provisoria y **la superficie la muestra una sola vez** para que el administrador la comunique— entró con el `PRODUCT-INTAKE` **1.13**, también posterior a la aprobación de la maqueta, y `Wireframes-Panel-De-Cuentas.md` la absorbió en su **1.5**. La diferencia con F-26 importa acá: **habilitar es una de las cuatro operaciones que la maqueta sí validó**, de modo que **su sonda ya existe**. `SD-10009` sensa el panel de cuentas tal como se aprobó el 2026-08-09 —sin ninguna exhibición de provisoria, que la maqueta no tiene en ninguna parte— y por lo tanto **daría conforme sobre un panel que todavía no cumple RN-10016**. No se lo amplía por decreto y no se le inventa una sonda anclada en un identificador que la línea de base no contiene: se **acota** el alcance de `SD-10009`, se declara el hueco acá y en `../03-UX-UI-DX/Linea-Base-Visual.md` §6.1, y la sonda de la provisoria nace con la misma **iteración 5** de maqueta. Hasta entonces, lo que gobierna la construcción de ese comportamiento son `CU-10004`, `US-10009` y `Wireframes-Panel-De-Cuentas.md` en su versión vigente.

## 5. Umbrales de deriva aplicados

Los umbrales por fila se derivan de la tabla de `Deriva-Rules.md` §3, sin agregarle dimensiones:

| Dimensión | Deriva menor, se registra y no bloquea | Deriva mayor, bloquea y exige decisión | Filas |
| --- | --- | --- | --- |
| Superficies | Cambia el nombre, se reordena el contenido | Falta una superficie aprobada, aparece una no aprobada, se fusionan dos | `SD-10001` a `SD-10011` |
| Componentes | Cambia el espaciado, el orden de las columnas, el texto de una etiqueta | Falta un componente, cambia el tipo de presentación, desaparece una acción | `SD-10001` a `SD-10011`, `SD-10035`, `SD-10044`, `SD-10046`, `SD-10056`, `SD-10061` |
| Estados | Cambia el texto del mensaje, el tipo de indicador de carga | Falta un estado aprobado, un estado cambia de condición disparadora | `SD-10012` a `SD-10022` |
| Navegación | Cambia el disparador visual de una ruta | Falta una ruta, aparece un callejón sin salida, se pierde lo que debía preservarse al volver | `SD-10023` a `SD-10027` |
| Modelo de datos | Cambia el orden de los campos en la presentación | Falta un campo, cambia el tipo, cambia la obligatoriedad, **cambia el formato de presentación acordado** | `SD-10028` a `SD-10036` |
| Accesibilidad | Cambia el orden de foco dentro de un grupo | Se pierde el recorrido por teclado, se pierde el foco visible, cae el contraste bajo el piso | `SD-10048` a `SD-10053` |
| Superficies · versión angosta | Cambia el orden de dos bloques | Una superficie deja de ser operable en ancho angosto, o la escena no se ajusta al cruzar el punto de quiebre | `SD-10055` |
| Tokens visuales | Cambia el valor de un token | Aparece un literal visual fuera del catálogo | `SD-10054` |
| Contrato de fachada · determinismo, recursos y frontera | Cambia la duración de una detención | La disposición cambia entre dos cargas; quedan recursos sin liberar; la fachada escribe una clave o consulta configuración por su cuenta | `SD-10041`, `SD-10042`, `SD-10047` |
| Vocabulario | — | Una forma desnuda de `Pendiente` en una superficie donde conviven los dos referentes | `SD-10058` |
| Contratos y comportamiento (`VER-XX`) | Cambia el texto de un mensaje de salida sin cambiar su semántica, o cambia el formato de un registro | El `criterio_aceptacion` falla; cambia el comando de ejecución sin actualizar el contrato; aparecen precondiciones no declaradas; o el caso de uso que la sonda ejercita deja de estar cubierto | `SD-10062` |

**Las filas sin gradación** —`SD-10027`, `SD-10033`, `SD-10036` a `SD-10040`, `SD-10043`, `SD-10045`, `SD-10057`, `SD-10059`, `SD-10060` y los **dos tramos de regla de negocio** de `SD-10062`— declaran deriva mayor ante cualquier diferencia, porque verifican reglas de negocio, reglas de arquitectura o garantías del contrato de fachada, que no admiten tolerancia.

**Toda fila queda mapeada a una dimensión.** La tabla de arriba y la lista de filas sin gradación, entre las dos, alcanzan a las sesenta y dos. El umbral vinculante es siempre **el de la fila**; la tabla resume por dimensión para poder leer la matriz de un vistazo, y no la reemplaza.

**Toda deriva mayor se resuelve por una de dos vías y nunca por omisión** (`Deriva-Rules.md` §3): se corrige el sistema construido para volver a la línea de base, o se actualiza la línea de base con aprobación humana explícita, en cuyo caso sube versión, la maqueta se corrige y la matriz de propagación de `Maqueta-Rules.md` §3.6 se aplica de nuevo.

## 6. Control de cambios

| Versión | Fecha | Cambios |
| --- | --- | --- |
| 1.5 | 2026-08-11 | **Cierra los hallazgos `NB2-02` (P2) y `NB2-03` (P2)** del informe [`../../../Audit/B2-Maqueta-GeometriaFactory-Web-r2.md`](../../../Audit/B2-Maqueta-GeometriaFactory-Web-r2.md) 1.0. **§4**, párrafo «Lo que esta matriz todavía no sensa»: la remisión constructiva de F-26 pasa de «sus wireframes **1.1**» a «sus wireframes **en su versión vigente**». Hoy `../03-UX-UI-DX/Wireframes-Panel-De-Cuentas.md` está en **1.6** y `../03-UX-UI-DX/Wireframes-Credencial-Propia.md` en **1.4**, cinco y tres versiones por delante del número citado, y entre medio se retiró el campo de contraseña del panel: quien siguiera la remisión literal construiría lo que la 1.2 eliminó. Es el mismo defecto que `F26-Propagacion-r1.md` levantó como `F26-13` sobre `Linea-Base-Visual.md` §6.1, corregido allá del mismo modo y **no acá**. **§4**: entra un párrafo nuevo por **`RN-10016`**, que entró con el `PRODUCT-INTAKE` **1.13** —posterior a la aprobación de la maqueta— y que el párrafo anterior no alcanzaba, porque hablaba sólo de F-26 y de la ronda del intake 1.7. Lo que se declara es la diferencia que importa: **RN-10016 cambia el comportamiento de `habilitar`, que la maqueta sí validó**, de modo que **su sonda existe y daría conforme sobre un panel que no cumple la regla**. **`SD-10009`** acota su alcance al comportamiento validado el 2026-08-09 y declara que **no cubre** la exhibición de la provisoria ni la marca de cambio pendiente. **No se abre ninguna sonda nueva y no se amplía ninguna existente por decreto**: las sondas de RN-10016 nacen con la **iteración 5** de maqueta, la misma vía que ya rige para F-26. **La cobertura de la matriz no cambia**: sigue siendo 211 de 211 identificadores sobre 62 filas, y ningún umbral de §5 se toca. Sube minor. |
| 1.4 | 2026-08-11 | **Corrección del hallazgo P3-2 del informe `G-10-Examples-Siete-Proyectos-r1.md` 1.0**, sobre el `PRODUCT-INTAKE` **1.25**. El párrafo de §4 «Qué aporta `SD-10062`…» atribuía umbral «mayor **sin gradación** en los dos casos» a `SD-10036` y a la fila de superficie del listado de la comisión, y **`SD-10010` no figura en la lista de filas sin gradación de §5** —que enumera `SD-10027`, `SD-10033`, `SD-10036` a `SD-10040`, `SD-10043`, `SD-10045`, `SD-10057`, `SD-10059` y `SD-10060`—. Se nombra ahora a `SD-10010` por su identificador y se escribe el umbral que **su propia fila** declara, «Mayor» sin tramo menor para ese supuesto, que basta para descartar la contradicción sin reclasificarla. **`SD-10010` no se agrega a la lista de §5**, porque eso modificaría una de las 61 filas de línea de base. Se actualiza la trazabilidad upstream a las versiones **1.1** de los dos artefactos de `10-Examples`. **Ninguna fila cambia**: la corrección es de la prosa que las describe. Sube minor. |
| 1.3 | 2026-08-11 | **Alta de la sonda `VER-XX` por AG-10**, al cerrar la fase que genera [`../10-Examples/`](../10-Examples/). Es el segundo momento de sensado de `Deriva-Rules.md` §4, y **cierra el hueco que §1 y §4 declaraban** con la frase «ninguna fila `VER-XX`, porque este proyecto de código no tiene categoría 10 todavía». Suma **una** fila, `SD-10062`, tomada del único contrato de verificación de esa categoría, con el comando del contrato como método sin desvío, el campo `evidencia` del sample como evidencia esperada y estado `Sin verificar` sin fecha. La matriz pasa de **sesenta y una** a **sesenta y dos** filas. §5 suma la dimensión «Contratos y comportamiento (`VER-XX`)» de `Deriva-Rules.md` §3 y agrega los dos tramos de regla de negocio de `SD-10062` a las filas sin gradación. Se declara por qué `SD-10062` se solapa con `SD-10036` y con la fila del listado de la comisión **sin duplicarlas** —unas sensan el elemento de línea de base por inspección, ésta el sample por su propio comando— y que el umbral no se contradice entre ellas. **Ninguna de las sesenta y una filas anteriores cambia, y la cobertura de los doscientos once identificadores de la línea de base tampoco.** Sube minor: agrega una fila y una dimensión de umbral sin alterar ninguna afirmación ya emitida. |
| 1.2 | 2026-08-09 | **Propagación del `PRODUCT-INTAKE` 1.7**, sin abrir ni modificar ninguna sonda. §4 suma la declaración de **lo que esta matriz todavía no sensa**: los elementos de interfaz que arrastra la capacidad **F-26** no tienen identificador en la línea de base, porque son posteriores a la aprobación de la maqueta, y una sonda anclada en un identificador inexistente diría comparar contra algo que la línea de base no contiene. Se declara que **`SD-10004` conserva su alcance de dos cursos** y que las sondas de F-26 nacen con la iteración 5 de maqueta. **Nada que corregir por F-25**: la corrección del hallazgo `AB2-02` ya había dejado a `SD-10043` en **seis** funciones y a `SD-10047` con la frontera correcta —el anfitrión manda dos valores de verdad y la fachada no lee la preferencia de movimiento reducido—, que es exactamente lo que 1.7 confirma. Sube minor: declara una brecha de cobertura, sin cambiar ningún umbral ni ninguna sonda. |
| 1.1 | 2026-08-09 | Corrección de los hallazgos **`AB2-01`** (P0), **`AB2-02`**, **`AB2-12`** y **`AB2-18`** de la auditoría `B2-Maqueta-GeometriaFactory-Web-r1.md`. `AB2-01`: la nota de cobertura de §4 afirmaba cubrir los doscientos once identificadores de la línea de base y la enumeración que la respaldaba **no resolvía** para `CMP-10004`, `CMP-10005`, `CMP-10007` y `CMP-10008`, que no figuraban en ninguna fila. Se abre la sonda **`SD-10061`**, que sensa de verdad los cuatro componentes transversales del recorrido de acceso, y se rehace la nota de cobertura como tabla verificable, con la declaración explícita de por qué no se resolvió anotando los cuatro identificadores en una fila existente. `AB2-02`: `SD-10043` pasa de cinco a **seis** funciones de la fachada, con la declaración de que la sexta —`establecerMovimiento`— se decidió después de la aprobación, **no fue validada visualmente**, y que la fila la sensa contra el contrato y no contra la maqueta. `AB2-12`: §5 mapea las siete filas que no quedaban en ninguna dimensión —`SD-10041`, `SD-10042`, `SD-10046`, `SD-10047`, `SD-10054`, `SD-10055` y `SD-10058`— y declara que el umbral vinculante es el de la fila. `AB2-18`: `SD-10047` precisa la frontera bundle/anfitrión —dos valores de verdad, arranque apagado ante opciones ausentes— y exige que la exclusión de claves propias del recuento sea por espacio de nombres declarado y no por prefijo. |
| 1.0 | 2026-08-09 | Emisión inicial por la Fase B2, al cerrar con la maqueta aprobada por el Product Owner. Sesenta sondas `SD-XX`, todas en estado `Sin verificar`, con su método de verificación, su evidencia esperada y su umbral; el criterio de agrupación que hace que sesenta filas cubran los doscientos once identificadores de la línea de base; y la declaración de que este archivo es el **único** artefacto de la categoría `08-Calidad-Y-Pruebas` emitido por esta fase, que el resto de la categoría se genera en la Fase E y que la matriz no nace con ninguna sonda `VER-XX` porque el proyecto de código no tiene categoría `10-Examples` todavía. |
