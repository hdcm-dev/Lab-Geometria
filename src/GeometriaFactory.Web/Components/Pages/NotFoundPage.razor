@page "/no-encontrado"

@* Punto de reejecución de `UseStatusCodePagesWithReExecute`. Sólo enruta: el contenido es
   `NotFoundSurface`, para que la misma pantalla sirva a la reejecución y a cualquier otro
   camino que la necesite. *@

<NotFoundSurface />
