@page "/credencial-propia/establecer"
@layout AccessShell

@* SUP-04 `Credencial-Propia` — CU-03, curso de establecimiento.
   Ruta: `/credencial-propia/establecer`. Shell: acceso.
   Wireframe: `SDD/Docs/Proyectos/GeometriaFactory-Web/03-UX-UI-DX/Wireframes-Credencial-Propia.md`.
   Maqueta: `SDD/Maquetas/GeometriaFactory-Web/Credencial-Propia.html`.

   MARCADOR DE POSICIÓN DE LA ETAPA `b`: estructura, título, navegación y sistema visual.
   Sin lógica de negocio, sin llamadas al servicio de datos y sin formularios que hagan nada. *@

<PageTitle>Credencial propia</PageTitle>

<h1>Elegí tu contraseña</h1>
    <p class="gf-subtitle">El laboratorio nunca te envió una contraseña. La elegís vos, ahora.</p>

<StagePlaceholder SurfaceId="SUP-04" SurfaceName="Credencial-Propia" UseCase="CU-03, curso de establecimiento" />

<SurfaceOutline Components="@Outline" />

<section class="gf-card gf-mt-5">
    <h2 class="gf-title">Salidas de esta superficie</h2>
    <p class="gf-caption gf-mt-2">Las rutas de <code>Linea-Base-Visual.md</code> §5 que nacen acá.
       Ninguna superficie es un callejón sin salida.</p>
    <ul class="gf-flat-list gf-mt-3">
        <li><a href="/ingreso"><strong>NAV-07</strong> — éxito del establecimiento, con su banda de confirmación</a></li>
    </ul>
</section>

@code {
    private static readonly KeyValuePair<string, string>[] Outline =
    [
        new("CMP-04", "Banda de resultado"),
        new("CMP-07", "Requisito declarado"),
        new("CMP-08", "Acción primaria"),
        new("CMP-09", "Sello de versión"),
        new("CMP-11", "Tarjeta de credencial"),
        new("CMP-12", "Subtítulo de motivo"),
        new("CMP-14", "Campos de contraseña nueva y repetición"),
    ];
}
