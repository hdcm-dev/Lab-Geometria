@page "/"
@layout AccessShell

@* LA RAÍZ. `NAV-01` y `NAV-03` de `Linea-Base-Visual.md` §5 son las dos únicas filas cuyo origen
   es «entrada al producto» y cuyo disparador es la RESOLUCIÓN DEL DESTINO INICIAL:

     NAV-01 · entrada sin administrador constituido  → SUP-01 `Aprovisionamiento-Inicial`
     NAV-03 · entrada con administrador constituido  → SUP-03 `Ingreso`, con redirección neutra
              que REEMPLAZA la entrada del historial

   De modo que la raíz no es una superficie: es el punto donde corre el GUARDIÁN DE
   APROVISIONAMIENTO que `Arquitectura-Proyecto-Codigo.md` §3.1 le asigna al componente «Armazón
   y encaminamiento». La especificación declara los dos destinos y no declara la dirección: que
   sea `/` es lo único que esta etapa PROPONE, y lo propone porque `/` es la única dirección que
   una persona escribe sin que nadie se la dé.

   POR QUÉ NO REDIRIGE TODAVÍA. El guardián necesita saber si hay administrador constituido, y
   eso es una consulta al servicio de datos. La etapa `b` no implementa comportamiento, así que
   deja acá el lugar del guardián, con sus dos destinos alcanzables a mano. Redirigir a uno de
   los dos por defecto sería inventar la respuesta que el guardián todavía no puede dar.

   QUÉ SE MUESTRA MIENTRAS TANTO — PROPUESTA DE LA ETAPA `b`. Ninguna fuente declara texto para
   esta dirección, porque en el producto terminado nadie la ve: el guardián resuelve y redirige
   antes de que haya nada que mirar. Lo que la raíz tiene que hacer hoy es no dejar a nadie
   parado: se ofrecen las dos puertas, y se las nombra con el TÍTULO DE LA SUPERFICIE A LA QUE
   LLEVAN —«Configurar el laboratorio» y «Ingresar al laboratorio», los dos `<h1>` de la maqueta
   aprobada—, con la condición que la línea de base le asigna a cada una escrita en castellano
   llano. Ni `NAV-01` ni `NAV-03` aparecen en pantalla: viven en este comentario. *@

<PageTitle>Fábrica de Geometría</PageTitle>

<StagePlaceholder />

<h1 class="gf-mt-5">Fábrica de Geometría</h1>
<p class="gf-subtitle">Laboratorio de geometría de Programación 2.</p>

<ul class="gf-flat-list gf-mt-5">
    <li><a href="/aprovisionamiento-inicial">Configurar el laboratorio</a>
        <span class="gf-caption"> — la primera vez, para crear la cuenta del docente.</span></li>
    <li><a href="/ingreso">Ingresar al laboratorio</a>
        <span class="gf-caption"> — si el laboratorio ya está configurado.</span></li>
</ul>
