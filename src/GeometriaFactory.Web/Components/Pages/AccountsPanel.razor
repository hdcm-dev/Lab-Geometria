@page "/cuentas"
@layout WorkShell

@* SUP-09 `Panel-De-Cuentas` — CU-04 flujo principal, FA-01, FA-02, FA-05, FA-06 y FA-07.
   Ruta: `/cuentas`. Shell: trabajo.
   Wireframe: `SDD/Docs/Proyectos/GeometriaFactory-Web/03-UX-UI-DX/Wireframes-Panel-De-Cuentas.md`.
   Maqueta: `SDD/Maquetas/GeometriaFactory-Web/Panel-De-Cuentas.html`.

   MARCADOR DE POSICIÓN DE LA ETAPA `b`: estructura, título, navegación y sistema visual.
   Sin lógica de negocio, sin llamadas al servicio de datos y sin formularios que hagan nada. *@

<PageTitle>Panel de cuentas</PageTitle>

<div class="gf-page-header">
    <div>
        <h1>Cuentas de la comisión</h1>
    <p class="gf-subtitle">Habilitá las cuentas que se registran y controlá el acceso.</p>
    </div>
</div>

<StagePlaceholder SurfaceId="SUP-09" SurfaceName="Panel-De-Cuentas" UseCase="CU-04 flujo principal, FA-01, FA-02, FA-05, FA-06 y FA-07" />

<SurfaceOutline Components="@Outline" />

<section class="gf-card gf-mt-5">
    <h2 class="gf-title">Salidas de esta superficie</h2>
    <p class="gf-caption gf-mt-2">Las rutas de <code>Linea-Base-Visual.md</code> §5 que nacen acá.
       Ninguna superficie es un callejón sin salida.</p>
    <ul class="gf-flat-list gf-mt-3">
        <li><a href="/cuentas"><strong>NAV-15</strong> — destino «Cuentas» de la barra lateral del administrador</a></li>
    </ul>
</section>

@code {
    private static readonly KeyValuePair<string, string>[] Outline =
    [
        new("CMP-34", "Encabezado de la superficie"),
        new("CMP-35", "Barra de filtros"),
        new("CMP-42", "Fila de cuenta"),
        new("CMP-43", "Iniciales de la cuenta"),
        new("CMP-44", "Insignia de situación"),
        new("CMP-45", "Acción de situación"),
        new("CMP-46", "Acción de baja"),
        new("CMP-47", "Diálogo de confirmación escrita"),
        new("CMP-48", "Aviso de arrastre"),
        new("CMP-49", "Orientación posterior"),
        new("CMP-50", "Banda de confirmación"),
    ];
}
