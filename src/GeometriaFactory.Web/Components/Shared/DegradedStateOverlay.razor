@* SUP-11 `Estado-Degradado-Y-Reconexion` — CU-10.

   NO TIENE RUTA, Y ES DELIBERADO. `Linea-Base-Visual.md` §5 lo dice con todas las letras:
   «`SUP-11` no es un destino: se superpone al armazón de la superficie en curso y devuelve el
   control a la misma superficie al recuperarse». Por eso vive acá, dibujado por los DOS shells,
   y no en `Components/Pages/`.

   ETAPA `b`. Los dos tramos que `CU-10` distingue —el servicio de datos que no responde y el
   circuito que se corta— se sostienen con estado que esta etapa no tiene, así que el aviso
   nunca se enciende. Lo que queda construido es el punto de superposición, su marcado y SU
   TEXTO REAL, el de la maqueta aprobada (`Datos-Maqueta.js`, `TEXTOS.reconexion`, que cita
   `Wireframes-Estado-Degradado-Y-Reconexion.md` §2). *@

@if (Visible)
{
    <div class="gf-reconnect-notice" role="status">
        <Icon Name="Alert" />
        <span>Se cortó la conexión con el laboratorio. Reintentando…</span>
    </div>
}

@code {
    /// La etapa `b` no lo enciende nunca: no hay estado que lo dispare.
    [Parameter] public bool Visible { get; set; }
}
