# Destino del bundle del visor

**Artefacto generado. No se edita a mano.** `scripts/build-visor.sh` empaqueta
`visor/` y copia el resultado acá. El `.gitignore` ignora los `.js` de esta carpeta por la
decisión de `Visor/09 Entornos-Deploy.md` §2, que la canalización ejecuta antes de publicar
(apartamientos `AP-02` y `AP-04` de `Plan-Etapa-A.md` §2.3).
