namespace GeometriaFactory.Domain.Entities;

/// <summary>
/// Observación emitida sobre un trabajo: advertencia o error de validación.
/// </summary>
/// <remarks>
/// ETAPA `a`: sin atributos ni invariantes; se modela en la etapa `c` (`Domain BT-06`).
/// Concepto declarado en `Definicion-Modelo-De-Dominio.md` §2.5.
/// Glosario: `Norma-De-Nomenclatura.md` §6.4 (`Observacion` ⟶ `Observation`).
/// </remarks>
public sealed class Observation
{
}
