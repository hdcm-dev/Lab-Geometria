namespace GeometriaFactory.Domain.Entities;

/// <summary>
/// Pieza de un trabajo.
/// </summary>
/// <remarks>
/// ETAPA `a`: sin atributos ni invariantes; se modela en la etapa `c` (`Domain BT-06`).
/// Concepto declarado en `Definicion-Modelo-De-Dominio.md` §2.3.
/// Glosario: `Norma-De-Nomenclatura.md` §6.4 (`Pieza` ⟶ `Piece`).
/// </remarks>
public sealed class Piece
{
}
