namespace GeometriaFactory.Domain.Entities;

/// <summary>
/// Trabajo entregado por un alumno.
/// </summary>
/// <remarks>
/// ETAPA `a`: sin atributos ni invariantes; se modela en la etapa `c` (`Domain BT-06`).
/// Concepto declarado en `Definicion-Modelo-De-Dominio.md` §2.2.
/// Glosario: `Norma-De-Nomenclatura.md` §6.4 (`Trabajo` ⟶ `Work`).
/// </remarks>
public sealed class Work
{
}
