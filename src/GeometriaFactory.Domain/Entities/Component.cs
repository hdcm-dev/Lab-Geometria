namespace GeometriaFactory.Domain.Entities;

/// <summary>
/// Componente de una pieza. Es término del glosario del cliente y no se renombra en el texto.
/// </summary>
/// <remarks>
/// ETAPA `a`: sin atributos ni invariantes; se modela en la etapa `c` (`Domain BT-06`).
/// Concepto declarado en `Definicion-Modelo-De-Dominio.md` §2.4.
/// Glosario: `Norma-De-Nomenclatura.md` §6.4 (`Componente` ⟶ `Component`).
/// </remarks>
public sealed class Component
{
}
