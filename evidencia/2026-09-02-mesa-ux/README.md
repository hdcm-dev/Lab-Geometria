# Evidencia — Mesa UX/UI del 2026-09-02

Registros que fundan el ciclo de `PROMPTs/Fixs/01-Ajuste-UX/` (expediente en
`Lab-Geometria.Documentacion/PROMPTs/Fixs/01-Ajuste-UX/OUTPUTs/`). Todos producidos con el medio A
de `/IA/PROMPTs/IA.Prompts/Base/Medios.md` —Chromium sin escritorio en contenedor—, a 1440x900 y
390x844.

## `antes/` — el laboratorio publicado, antes de este ciclo

| Archivo | Qué muestra |
|---|---|
| `viv-ingreso-1440.png` | El `h1` con anillo de foco al cargar, sin que nadie haya interactuado |
| `adm-comision-1440.png` | Contenido de borde a borde; cinco tablas hermanas sin retícula común; cabecera de grupo en versalitas de 11 px |
| `adm-cuentas-1440.png` | «ADOLFO VERA» en versalitas grises; acciones envueltas con borde dentado; marcador de posición recortado |
| `adm-cuentas-390.png` | La misma pantalla en teléfono |

## `despues/` — con el ciclo aplicado

Las cuatro primeras son **el producto**, servido desde el artefacto publicado contra un servicio de
datos propio de la mesa (no se tocó ningún contenedor del Product Owner). Las dos `z-maq-*` son la
maqueta aprobada, con la misma corrección, al mismo encuadre.

`z-verde-ilegible.png` y `z-verde-legible.png` son el par que verifica el hallazgo `S1`.

## `peritaje/` — la contradicción entre tres observadores, resuelta midiendo

`p-20-visor-contrato-1440.png` y `p-20-visor-inventadas-1440.png` son **el mismo cubo mal declarado
en la misma pantalla, con un minuto de diferencia**, cambiando una sola variable: las claves del
texto. Con las del contrato dibuja y advierte la discrepancia; con `Lado` queda vacío, dice que no
hay observaciones, y **afirma igual que dibujó**.

`afirmaciones-en-rojo-sobre-el-sitio-publicado.txt` es la corrida de las tres afirmaciones de
`FiguraQueNoSePudoLeerTests` contra el sitio publicado **sin el arreglo**: las tres en rojo. Una
prueba que nunca se vio fallar no es una prueba.
