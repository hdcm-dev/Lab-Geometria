<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Sample `domain/01-basico`, la pasada de EJECUCIÓN de `Rules-Examples.md` §0.2.
    Gobernado por `SDD/Docs/Unidades-Entrega/GeometriaFactory-Api/10-Examples/ejemplo-01-basico-dominio.md`:
    su §5 declara este árbol de archivos y su §6 la salida exacta, las dos escritas
    ANTES que este código y no ajustadas después.

    ENTRA EN `GeometriaFactory.sln` DESDE EL 2026-09-12, y se construye con ella: es la
    forma de cumplir `Rules-Examples.md` §3.4 («su CI debe garantizar que siempre compila
    contra la versión actual del producto»), y la decisión del Product Owner de que todo
    proyecto quede bajo el árbol de solución. El temor anterior —que su ensamblado moviera
    `QG-03`— se midió antes y después de entrar: la cobertura no cambia, porque `coverlet`
    instrumenta lo que carga el proceso de pruebas y ningún test referencia un sample.
    Se sigue corriendo por su ruta, que es lo que declara el comando previsto de §9.
  -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <RootNamespace>GeometriaFactory.Samples.Domain.Basico</RootNamespace>
    <AssemblyName>sample-domain-01-basico</AssemblyName>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="../../../src/GeometriaFactory.Domain/GeometriaFactory.Domain.csproj" />
  </ItemGroup>

</Project>
